# Интеграция с Gemini Files API

Автоматическая синхронизация библиотеки знаний LightRAG с Google Gemini через Files API.

## 📁 Файлы проекта

- `export_for_gemini.py` — Экспорт данных из LightRAG кэша в текстовые файлы
- `upload_to_gemini.py` — Загрузка файлов в Gemini Files API
- `sync_library_to_gemini.ps1` — PowerShell скрипт автоматической синхронизации

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
# Активация виртуального окружения (если еще не создано)
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# Установка пакета Google AI
pip install google-generativeai
```

### 2. Ручная синхронизация

**Экспорт данных:**
```powershell
python export_for_gemini.py --cache-dir E:/AI_Librarian_Core/lightrag_cache
```

Создает файлы:
- `gemini_export/library_documents.txt` — Полные тексты документов
- `gemini_export/library_entities.txt` — Сущности и связи из графа знаний

**Загрузка в Gemini:**
```powershell
python upload_to_gemini.py --input-dir ./gemini_export
```

### 3. Автоматическая синхронизация

**Одноразовый запуск:**
```powershell
.\sync_library_to_gemini.ps1
```

**С автоматической очисткой старых файлов:**
```powershell
.\sync_library_to_gemini.ps1 -AutoCleanup
```

## ⏰ Настройка автозапуска через Task Scheduler

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте **новую задачу**:
   - **Имя:** "Sync Library to Gemini"
   - **Триггер:** Ежедневно в 03:00
   - **Действие:** 
     - Программа: `powershell.exe`
     - Аргументы: `-File "E:\WORLD_OLLAMA\workbench\sandbox_main\connector\python\sync_library_to_gemini.ps1" -AutoCleanup`
   - **Условия:** Запускать при простое (опционально)
3. Сохраните задачу

## 📋 Использование в Gemini Gem

После загрузки файлов:

1. Откройте Gemini Gem: https://gemini.google.com/gems/edit/f5848adf456f
2. В чате нажмите кнопку **📎** (прикрепить файлы)
3. Выберите загруженные файлы из раздела **"Files API"**
4. Файлы станут доступны для всех запросов в этом чате

**Пример запроса:**
```
На основе загруженной библиотеки знаний, расскажи о принципах ТРИЗ 
для решения задачи герметизации космического аппарата
```

## ⚠️ Важные ограничения Gemini Files API

| Параметр | Лимит |
|----------|-------|
| Максимальный размер файла | 2 GB |
| Максимальное количество активных файлов | 20 |
| Срок жизни файлов | 48 часов |

**Рекомендация:** Настройте автоматическую синхронизацию каждые 24 часа, чтобы файлы всегда были актуальными.

## 🔧 Дополнительные команды

**Список активных файлов в Gemini:**
```powershell
python upload_to_gemini.py --list-only
```

**Экспорт в другую папку:**
```powershell
python export_for_gemini.py --cache-dir E:/AI_Librarian_Core/lightrag_cache --output-dir ./custom_export
```

## 🔐 Безопасность API ключа

**Текущее состояние:** API ключ жестко закодирован в `upload_to_gemini.py` (строка 36).

**Для production использования:**

1. **Переместите ключ в переменную окружения:**
   ```powershell
   [System.Environment]::SetEnvironmentVariable('GOOGLE_API_KEY', 'YOUR_KEY_HERE', 'User')
   ```

2. **Измените код в `upload_to_gemini.py`:**
   ```python
   import os
   API_KEY = os.getenv('GOOGLE_API_KEY')
   if not API_KEY:
       raise ValueError("GOOGLE_API_KEY environment variable not set")
   ```

3. **Или создайте новый ключ** только для этого проекта через [Google AI Studio](https://aistudio.google.com/app/apikey)

## 📊 Структура экспортированных данных

### library_documents.txt
```
================================================================================
БИБЛИОТЕКА ЗНАНИЙ - ПОЛНЫЕ ДОКУМЕНТЫ
Экспортировано: 2025-11-24 12:00:00
================================================================================

--------------------------------------------------------------------------------
ДОКУМЕНТ ID: doc_12345
--------------------------------------------------------------------------------

[Полный текст документа...]
```

### library_entities.txt
```
================================================================================
БИБЛИОТЕКА ЗНАНИЙ - ГРАФ СУЩНОСТЕЙ И СВЯЗЕЙ
Экспортировано: 2025-11-24 12:00:00
================================================================================

============================================================
СУЩНОСТЬ: ТРИЗ
============================================================
Описание: Теория решения изобретательских задач...

[Информация о связях...]
```

## 🐛 Troubleshooting

**Проблема:** `ModuleNotFoundError: No module named 'google.generativeai'`
```powershell
# Решение: Убедитесь, что venv активировано и пакет установлен
.\.venv\Scripts\Activate.ps1
pip install google-generativeai
```

**Проблема:** `❌ Отсутствуют файлы кэша`
```powershell
# Решение: Проверьте путь к кэшу LightRAG
ls E:\AI_Librarian_Core\lightrag_cache\kv_store_full_docs.json
```

**Проблема:** `⚠️ Превышен лимит активных файлов!`
```powershell
# Решение: Используйте автоматическую очистку
python upload_to_gemini.py --auto-cleanup
```

## 📝 Логи выполнения

При запуске `sync_library_to_gemini.ps1` создается детальный лог в консоли:

```
[2025-11-24 03:00:00] [INFO] ========================================
[2025-11-24 03:00:00] [INFO] СИНХРОНИЗАЦИЯ БИБЛИОТЕКИ С GEMINI
[2025-11-24 03:00:00] [INFO] ========================================
[2025-11-24 03:00:01] [INFO] Активация Python venv: ...
[2025-11-24 03:00:02] [SUCCESS] Пакет google-generativeai установлен
[2025-11-24 03:00:03] [INFO] ШАГ 1: ЭКСПОРТ ДАННЫХ ИЗ LIGHTRAG
...
[2025-11-24 03:02:15] [SUCCESS] ✅ СИНХРОНИЗАЦИЯ ЗАВЕРШЕНА
```

## 🔄 Интеграция с SESA3002a Agent

Библиотека, загруженная в Gemini, полностью совместима с агентом SESA3002a для ТРИЗ задач:

```python
# В агенте SESA3002a можно использовать оба источника:

# 1. Локальный LightRAG (через library_client.py)
context = agent_bridge.retrieve_engineering_context("герметизация космических аппаратов")

# 2. Gemini Files API (через чат с Gemini Gem)
# Вопрос в Gemini: "На основе библиотеки, найди решения по герметизации"
```

---

**Статус:** ✅ Готово к использованию  
**Версия:** 1.0  
**Дата создания:** 24 ноября 2025 г.
