# Интеграция LightRAG библиотеки с Open WebUI и VS Code

Комплексное руководство по подключению локальной библиотеки знаний к Open WebUI и GitHub Copilot в VS Code.

## 📋 Содержание

1. [Установка Tool в Open WebUI](#1-установка-tool-в-open-webui)
2. [Автоматическая индексация новых документов](#2-автоматическая-индексация-новых-документов)
3. [MCP Server для VS Code Copilot](#3-mcp-server-для-vs-code-copilot)
4. [Устранение неполадок](#4-устранение-неполадок)

---

## 1. Установка Tool в Open WebUI

### Шаг 1: Убедиться, что LightRAG сервер запущен

```powershell
# Проверить статус
curl http://localhost:8003/health

# Если не работает — запустить
cd E:\AI_Librarian_Core
Start-Process powershell -ArgumentList "-NoExit", "-Command", "python lightrag_server.py"
```

### Шаг 2: Установить Tool в базу данных Open WebUI

```powershell
cd E:\AGENTS

# Установить инструмент
python agents_tools\install_library_tool_to_webui.py

# С принудительной переустановкой
python agents_tools\install_library_tool_to_webui.py --force

# Включить глобально для всех чатов
python agents_tools\install_library_tool_to_webui.py --global
```

**Требования:**
- Docker контейнер `open-webui` с bridge-доступом к БД
- Путь к БД: `E:\AGENTS\open-webui-bridge\data\webui.db`

**Если bridge не настроен:**
```powershell
# Пересоздать контейнер с bridge
E:\AGENTS\agents_tools\recreate_openwebui_with_bridge.ps1
```

### Шаг 3: Активировать инструмент в чате

1. Открыть Open WebUI → **http://localhost:3000**
2. Создать новый чат или открыть существующий
3. Нажать **⚙️** (настройки чата)
4. Раздел **Tools** → включить **"Библиотека Знаний"**
5. Готово! Теперь можно использовать:

```python
# В чате с агентом
search_library("Как настроить Ollama?")
list_floors()
get_floor_content(1)
check_status()
```

### Доступные методы:

| Метод | Описание | Пример |
|-------|----------|--------|
| `search_library(query)` | Поиск в библиотеке | `search_library("GPU overclock")` |
| `list_floors()` | Список этажей библиотеки | `list_floors()` |
| `get_floor_content(floor)` | Содержимое этажа | `get_floor_content(1)` |
| `check_status()` | Статус библиотеки | `check_status()` |
| `reload_indexes()` | Перезагрузить индексы | `reload_indexes()` |

---

## 2. Автоматическая индексация новых документов

### Установка зависимостей

```powershell
cd E:\AGENTS
pip install watchdog
```

### Запуск мониторинга

```powershell
# Мониторинг Documents_cleaned/ с автоиндексацией
python agents_tools\auto_index_new_documents.py

# Кастомная папка
python agents_tools\auto_index_new_documents.py --watch-dir "E:\MyDocuments"

# Другой API endpoint
python agents_tools\auto_index_new_documents.py --api-url "http://localhost:9000"
```

### Как это работает?

```
[Новый файл] → Documents_cleaned/
      ↓
FileSystemWatcher (обнаружение)
      ↓
Debounce Buffer (группировка изменений, 5 сек)
      ↓
LightRAG API /api/insert (индексация)
      ↓
kv_store_doc_status.json (статус "processed")
      ↓
[Файл проиндексирован, доступен для поиска]
```

### Поддерживаемые форматы:

- `.txt`, `.md`, `.json`, `.yaml`, `.xml`, `.html`
- Автоматическая фильтрация
- Размер файла: без ограничений (chunking автоматический)

### Мониторинг работы:

```powershell
# В отдельном терминале — запустить мониторинг
python agents_tools\auto_index_new_documents.py

# В другом терминале — добавить файл
Copy-Item "my_document.txt" "E:\AGENTS\Documents_cleaned\"

# Вывод в консоли:
# 📄 Обнаружен новый файл: my_document.txt
# 🔄 Обработка 1 новых файлов...
# ✅ Проиндексирован: my_document.txt
#    Chunks: 12
#    Doc ID: e45a7f3b-8d2c-4a1e-9f6b-3c4d5e6f7a8b
```

---

## 3. MCP Server для VS Code Copilot

### Установка зависимостей

```powershell
cd E:\AGENTS
pip install mcp requests
```

### Запуск MCP сервера

```powershell
# Запустить MCP сервер
python agents_tools\mcp_library_server.py
```

**MCP сервер работает через STDIO**, поэтому VS Code будет запускать его автоматически.

### Конфигурация в VS Code

#### Вариант 1: Через settings.json (рекомендуется)

1. Открыть **VS Code Settings** (`Ctrl+Shift+P` → `Preferences: Open User Settings (JSON)`)
2. Добавить конфигурацию MCP сервера:

```json
{
  "github.copilot.advanced": {
    "mcp.servers": {
      "lightrag-library": {
        "command": "python",
        "args": [
          "E:\\AGENTS\\agents_tools\\mcp_library_server.py"
        ],
        "env": {
          "LIGHTRAG_API_URL": "http://localhost:8003"
        }
      }
    }
  }
}
```

#### Вариант 2: Через MCP Client Extension

1. Установить расширение **"MCP Client"** (если доступно в Marketplace)
2. **Settings** → **Extensions** → **MCP Client** → **Add Server**
3. Заполнить:
   - **Name:** `LightRAG Library`
   - **Command:** `python`
   - **Args:** `E:\AGENTS\agents_tools\mcp_library_server.py`
   - **Env:** `LIGHTRAG_API_URL=http://localhost:8003`

### Использование в VS Code Copilot

После настройки MCP сервера, GitHub Copilot сможет автоматически использовать библиотеку знаний:

```
User: Как настроить Ollama для работы с LightRAG?

Copilot: [вызывает MCP tool search_knowledge_base]
         [получает ответ из библиотеки]
         
         Согласно локальной библиотеке знаний...
         [детальный ответ с источниками]
```

### Доступные MCP Tools:

| Tool | Описание | Параметры |
|------|----------|-----------|
| `search_knowledge_base` | Поиск в библиотеке | `query` (обязательно), `mode` (optional) |
| `get_library_status` | Статус библиотеки | — |
| `insert_document` | Добавить документ | `text` (обязательно), `metadata` (optional) |
| `explain_library_architecture` | Объяснение архитектуры | — |

### Тестирование MCP интеграции:

```powershell
# 1. Убедиться, что LightRAG сервер работает
curl http://localhost:8003/health

# 2. Запустить MCP сервер вручную для проверки
python agents_tools\mcp_library_server.py

# Вывод должен быть:
# INFO - Starting LightRAG Library MCP Server
# INFO - API URL: http://localhost:8003
# INFO - Library status: 377/390 documents
```

---

## 4. Устранение неполадок

### Проблема: Tool не отображается в Open WebUI

**Решение:**
```powershell
# 1. Проверить, что контейнер с bridge
docker inspect open-webui | Select-String "open-webui-bridge"

# 2. Проверить БД
Test-Path E:\AGENTS\open-webui-bridge\data\webui.db

# 3. Переустановить Tool
python agents_tools\install_library_tool_to_webui.py --force

# 4. Перезапустить контейнер
docker restart open-webui
```

### Проблема: Автоиндексация не работает

**Решение:**
```powershell
# 1. Проверить LightRAG API
curl http://localhost:8003/health

# 2. Проверить зависимость watchdog
pip show watchdog

# Если не установлен:
pip install watchdog

# 3. Запустить с verbose логами
python agents_tools\auto_index_new_documents.py --watch-dir "E:\AGENTS\Documents_cleaned"
```

### Проблема: MCP сервер не подключается в VS Code

**Решение:**
```powershell
# 1. Проверить Python путь
where.exe python
# Использовать полный путь в settings.json

# 2. Проверить зависимость mcp
pip show mcp

# Если не установлен:
pip install mcp

# 3. Проверить логи VS Code
# Ctrl+Shift+P → Developer: Show Logs → Extension Host
```

### Проблема: Библиотека не отвечает на запросы

**Решение:**
```powershell
# 1. Проверить статус индексации
curl http://localhost:8003/status

# 2. Проверить VRAM (должно быть >6GB для работающих моделей)
nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits

# Если < 6000 MB — модели не загружены, перезапустить сервер
Get-Process python | Where-Object {$_.Path -like "*lightrag*"} | Stop-Process
cd E:\AI_Librarian_Core
python lightrag_server.py

# 3. Проверить графовые файлы
Get-ChildItem E:\AI_Librarian_Core\lightrag_cache\*.graphml
# Должен быть graph_chunk_entity_relation.graphml > 100KB
```

---

## 5. Полная установка (с нуля)

### Шаг 1: Подготовить окружение

```powershell
cd E:\AGENTS

# Установить все зависимости
pip install watchdog mcp requests
```

### Шаг 2: Настроить Open WebUI bridge

```powershell
# Пересоздать контейнер с bridge-доступом
E:\AGENTS\agents_tools\recreate_openwebui_with_bridge.ps1
```

### Шаг 3: Запустить LightRAG сервер

```powershell
cd E:\AI_Librarian_Core
Start-Process powershell -ArgumentList "-NoExit", "-Command", "python lightrag_server.py"
```

### Шаг 4: Установить Tool в Open WebUI

```powershell
cd E:\AGENTS
python agents_tools\install_library_tool_to_webui.py --global
```

### Шаг 5: Запустить автоиндексацию (опционально)

```powershell
# В отдельном терминале
python agents_tools\auto_index_new_documents.py
```

### Шаг 6: Настроить MCP в VS Code

Добавить в `settings.json`:

```json
{
  "github.copilot.advanced": {
    "mcp.servers": {
      "lightrag-library": {
        "command": "python",
        "args": ["E:\\AGENTS\\agents_tools\\mcp_library_server.py"],
        "env": {"LIGHTRAG_API_URL": "http://localhost:8003"}
      }
    }
  }
}
```

### Шаг 7: Проверить работу

```powershell
# Open WebUI
# Открыть http://localhost:3000
# Создать чат → Tools → Enable "Библиотека Знаний"
# Вызвать: search_library("test")

# VS Code Copilot
# Спросить: "Найди в библиотеке информацию про Ollama"
# Copilot должен использовать MCP tool автоматически
```

---

## 📊 Архитектура интеграции

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACES                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │  Open WebUI      │              │  VS Code Copilot │         │
│  │  (Port 3000)     │              │  (GitHub Copilot)│         │
│  └────────┬─────────┘              └────────┬─────────┘         │
│           │                                 │                   │
│           │ REST API (Tools)                │ MCP Protocol      │
│           │                                 │                   │
│  ┌────────▼─────────┐              ┌────────▼─────────┐         │
│  │ Librarian Tool   │              │ MCP Library      │         │
│  │ (librarian_tool) │              │ Server (STDIO)   │         │
│  └────────┬─────────┘              └────────┬─────────┘         │
│           │                                 │                   │
│           └─────────────┬───────────────────┘                   │
│                         │                                       │
├─────────────────────────▼───────────────────────────────────────┤
│                  LIGHTRAG API (Port 8003)                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  FastAPI Server (lightrag_server.py)                    │   │
│  │  - /query (search)                                       │   │
│  │  - /status (indexing status)                             │   │
│  │  - /api/insert (add document)                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                         │                                       │
├─────────────────────────▼───────────────────────────────────────┤
│                  LIGHTRAG GRAPHRAG ENGINE                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - Knowledge Graph (graph_chunk_entity_relation.graphml)│   │
│  │  - Vector Database (vdb_entities.json, vdb_chunks.json) │   │
│  │  - Persistent State (kv_store_doc_status.json)          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                         │                                       │
├─────────────────────────▼───────────────────────────────────────┤
│                    OLLAMA (Port 11434)                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - LLM: qwen2.5:14b-instruct-q4_k_m                     │   │
│  │  - Embeddings: nomic-embed-text                          │   │
│  │  - GPU: RTX 5060 Ti 16GB                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    AUTO-INDEXING PIPELINE                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Documents_cleaned/ (FileSystemWatcher)                         │
│           │                                                     │
│           ▼                                                     │
│  Debounce Buffer (5 sec window)                                 │
│           │                                                     │
│           ▼                                                     │
│  LightRAG API /api/insert                                       │
│           │                                                     │
│           ▼                                                     │
│  kv_store_doc_status.json (status: processed)                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Итоговый чеклист

- ✅ LightRAG сервер запущен (`python lightrag_server.py`)
- ✅ Open WebUI Tool установлен (`install_library_tool_to_webui.py`)
- ✅ Tool активирован в чатах (Settings → Tools)
- ✅ Автоиндексация запущена (`auto_index_new_documents.py`)
- ✅ MCP сервер настроен в VS Code (`settings.json`)
- ✅ Зависимости установлены (`watchdog`, `mcp`, `requests`)

**Статус библиотеки:**
- 377 документов проиндексировано
- Knowledge Graph: 3.7 MB
- Vector Database: 27.7 MB

**Готово к использованию! 🚀**
