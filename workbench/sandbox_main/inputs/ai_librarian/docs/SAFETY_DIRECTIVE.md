# 🛑 ДИРЕКТИВА БЕЗОПАСНОСТИ ДЛЯ АГЕНТА-ПИЛОТА

**КРИТИЧЕСКИЙ ЗАПРЕТ:**

Тебе **ЗАПРЕЩЕНО** перезапускать само приложение Docker Desktop или службу Docker в Windows. 
Это убивает среду и вызывает циклы перезагрузки.

## ✅ РАЗРЕШЕННЫЕ ОПЕРАЦИИ

Если нужно обновить агента или перезапустить контейнеры:

```powershell
# Перезапуск одного контейнера
docker restart librarian_brain
docker restart librarian_interface

# Перезапуск всех контейнеров через docker-compose
docker-compose restart

# Полная пересборка (если изменились Modelfile или docker-compose.yaml)
docker-compose down
docker-compose up -d
```

## ❌ ЗАПРЕЩЕННЫЕ ОПЕРАЦИИ

**НИКОГДА НЕ ВЫПОЛНЯЙ:**

```powershell
# Перезапуск службы Docker (убивает ВСЁ)
Restart-Service Docker
Restart-Service com.docker.service

# Перезапуск Docker Desktop
Stop-Process -Name "Docker Desktop"
& "C:\Program Files\Docker\Docker\Docker Desktop.exe"

# Любые действия с Docker как службой Windows
sc stop docker
sc start docker
```

## 📋 ПОСЛЕДОВАТЕЛЬНОСТЬ БЕЗОПАСНОГО ОБНОВЛЕНИЯ АГЕНТА

1. **Обновить Modelfile** (на хост-системе)
2. **Скопировать в контейнер**: `docker cp Librarian_Lite.Modelfile librarian_brain:/tmp/`
3. **Пересобрать модель**: `docker exec librarian_brain ollama create librarian-lite -f /tmp/Librarian_Lite.Modelfile`
4. **Обновить Open WebUI** (опционально): `docker restart librarian_interface`

**ПРАВИЛО:** Работай ВНУТРИ контейнеров, не трогай хост-систему Docker.

## 🔧 ЧТО ДЕЛАТЬ ПРИ ЗАВИСАНИИ

Если контейнер завис:

```powershell
# 1. Проверь логи
docker logs librarian_brain --tail 50
docker logs librarian_interface --tail 50

# 2. Перезапусти ТОЛЬКО проблемный контейнер
docker restart <имя_контейнера>

# 3. Если не помогло - перезапусти весь стек
docker-compose restart

# 4. КРАЙНИЙ СЛУЧАЙ (если docker-compose не отвечает)
docker-compose down --timeout 30
docker-compose up -d
```

**НЕ ПЕРЕЗАПУСКАЙ DOCKER DESKTOP.**

## ⚠️ ПОСЛЕДСТВИЯ НАРУШЕНИЯ

Перезапуск Docker Desktop приводит к:
- Остановке ВСЕХ контейнеров
- Потере незакоммиченных данных в памяти
- Циклам перезагрузки (контейнеры пытаются стартовать, Docker не готов)
- Необходимости ручного восстановления всего стека

---

**Эта директива ОБЯЗАТЕЛЬНА для всех агентов с доступом к Docker.**
