# 🎯 Инструкция по переиндексации библиотеки в Open WebUI

## Предварительные условия

- ✅ Контейнеры запущены (`docker-compose ps` показывает статус Up)
- ✅ Open WebUI доступен: http://localhost:3001
- ✅ Консолидация выполнена: 3 файла в `CONSOLIDATED_LIBRARY/`

---

## Шаг 1: Удаление старых документов (если загружали ранее)

1. Откройте **http://localhost:3001**
2. Войдите в систему (или зарегистрируйтесь, если еще не сделали)
3. Перейдите в **Workspace** (иконка 💼 в левом меню)
4. Выберите вкладку **Documents**
5. Если там есть документы:
   - Выберите все документы (галочка слева вверху)
   - Нажмите **Delete** (корзина)
   - Подтвердите удаление

> 💡 Если документов нет — переходите к Шагу 2.

---

## Шаг 2: Загрузка консолидированных файлов

1. В разделе **Workspace → Documents** нажмите **Upload Files** (или **+ Add Document**)

2. Выберите **3 файла** из:
   ```
   E:\AI_Librarian_Core\library_source\CONSOLIDATED_LIBRARY\
   ```
   
   **Файлы для загрузки:**
   - ✅ `01_Architecture_Methodology.md` (3.3 MB, 13,610 строк)
   - ✅ `05_Prompts_Instructions.md` (523 KB, 1,722 строки)
   - ✅ `Uncategorized.md` (3.1 MB, 16,027 строк)

3. Нажмите **Upload** / **Загрузить**

4. Дождитесь завершения загрузки (индикатор прогресса)

---

## Шаг 3: Включение эмбеддинга (если не включено)

1. Перейдите в **Settings** (⚙️ в правом верхнем углу)

2. Выберите вкладку **Documents**

3. Найдите секцию **Embeddings**:
   - **Enable RAG**: ✅ (должно быть включено)
   - **Embedding Model**: `nomic-embed-text:latest`

4. Если модель не загружена:
   - Open WebUI автоматически скачает её при первой индексации
   - Размер: ~274 MB
   - Время загрузки: 2-5 минут

5. Нажмите **Save** внизу страницы

---

## Шаг 4: Ожидание индексации

**Процесс индексации происходит автоматически после загрузки файлов.**

**Индикаторы прогресса:**
- В разделе **Documents** напротив каждого файла будет статус:
  - 🔄 **Processing** — индексируется
  - ✅ **Indexed** — готово

**Ожидаемое время:**
- `01_Architecture_Methodology.md`: ~3-4 минуты
- `05_Prompts_Instructions.md`: ~1 минуту
- `Uncategorized.md`: ~3-4 минуты
- **Итого: 7-10 минут**

> ⏳ Можно продолжать работу в других вкладках. Индексация идёт в фоне.

---

## Шаг 5: Тестирование RAG

### Тест 1: Поиск по архитектуре

1. Создайте **New Chat** (+ в левом меню)

2. Выберите модель:
   - Если создали `librarian` — используйте её
   - Если нет — используйте базовую модель (будет доступна после загрузки через `.\download_model.ps1`)

3. **Включите RAG:**
   - В поле ввода сообщения найдите иконку 📄 (Documents)
   - Кликните, чтобы активировать RAG
   - Должна подсветиться синим цветом

4. **Тестовый запрос:**
   ```
   Найди информацию об архитектуре агентов и методологии ТРИЗ.
   Используй только данные из библиотеки.
   Укажи источник информации.
   ```

5. **Ожидаемый результат:**
   - Агент использует содержимое `01_Architecture_Methodology.md`
   - Упоминает конкретные разделы (Floor_01, Floor_02 и т.д.)
   - Указывает `[ИСТОЧНИК: БИБЛИОТЕКА]`

### Тест 2: Поиск промптов

**Запрос:**
```
Покажи примеры системных промптов для агентов.
Используй библиотеку.
```

**Ожидаемый источник:** `05_Prompts_Instructions.md`

### Тест 3: Проверка uncategorized

**Запрос:**
```
Найди информацию о реализации кода и разработке.
Используй библиотеку.
```

**Ожидаемый источник:** `Uncategorized.md` (содержит Floor_04, 05, 07 — Код и Реализация)

---

## Шаг 6: Верификация индексации

**Проверка статуса в WebUI:**

1. **Workspace → Documents**
2. Убедитесь, что все 3 файла имеют статус ✅ **Indexed**
3. Кликните на любой файл — должен открыться превью содержимого

**Проверка через API (опционально):**

```powershell
# Проверка загруженных документов через REST API
$response = Invoke-RestMethod -Uri "http://localhost:3001/api/v1/documents" -Headers @{"Authorization"="Bearer YOUR_API_KEY"}
$response | ConvertTo-Json -Depth 3
```

> 💡 API ключ можно получить в Settings → Account → API Keys

---

## 🔧 Troubleshooting

### Проблема: Индексация зависла

**Решение:**
```powershell
# Перезапуск контейнера WebUI
docker restart librarian_interface

# Проверка логов
docker logs -f librarian_interface
```

### Проблема: RAG не использует документы

**Проверьте:**
1. Иконка 📄 активна (синяя) в поле ввода
2. В Settings → Documents → RAG включен
3. Документы имеют статус "Indexed", а не "Processing"

**Временное решение:**
- Переключите RAG выкл/вкл в чате
- Создайте новый чат

### Проблема: Медленная индексация (>15 минут)

**Причины:**
- Большие файлы (3+ MB)
- Модель эмбеддинга ещё загружается
- Недостаточно RAM/VRAM

**Решение:**
```powershell
# Проверка ресурсов
.\status.ps1

# Проверка загрузки модели эмбеддинга
docker exec -it librarian_brain ollama list
# Должна быть модель: nomic-embed-text:latest
```

---

## ✅ Критерии успешной индексации

- [x] Все 3 файла загружены в Documents
- [x] Статус всех файлов: ✅ Indexed
- [x] Модель эмбеддинга `nomic-embed-text:latest` загружена
- [x] RAG активирован в чате (иконка 📄 синяя)
- [x] Агент находит информацию из библиотеки
- [x] Агент указывает источники: [ИСТОЧНИК: БИБЛИОТЕКА]

---

## 📊 Следующие шаги после успешной индексации

1. **Создать кастомного агента "librarian"** (если ещё не сделали):
   - Используйте содержимое `Librarian.Modelfile`
   - См. `NEXT_STEPS.md` → ШАГ 3

2. **Протестировать все категории:**
   - Архитектура и методология
   - Промпты и инструкции
   - Код и реализация (через Uncategorized)

3. **Оптимизировать поиск:**
   - Настроить Chunk Size в Settings → Documents
   - Текущие значения: Chunk Size = 1500, Overlap = 100
   - Можно увеличить до 2000/150 для лучшего контекста

4. **Расширить библиотеку:**
   - Добавить новые документы через Upload Files
   - Они автоматически проиндексируются

---

**Готово к работе!** 🚀

Библиотека консолидирована, индексирована и готова к использованию через RAG.
