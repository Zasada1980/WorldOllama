# 🚀 Инструкция по созданию агента AI LIBRARIAN CORE

## Статус выполнения

### ✅ Выполнено автоматически

1. **Modelfile обновлен** → `E:\AI_Librarian_Core\Librarian.Modelfile`
   - Температура: 0.2 (строгая точность)
   - Контекст: 16384 токена
   - Личность: AI LIBRARIAN CORE v1.0
   - Конституция: 4 незыблемых закона

2. **Контейнер Ollama запущен** → `librarian_brain`
   - Порт: 11434
   - Статус: Up
   - GPU: Включен

3. **Базовая модель загружается** → `qwen2.5:32b-instruct-q4_k_m`
   - Прогресс: ~40-50% (автоматически)
   - Размер: 19 GB
   - Время: ~15-20 минут с начала

---

## ⏳ Требуется ваше участие

### ШАГ 1: Дождаться загрузки модели

**Проверка статуса:**
```powershell
docker exec librarian_brain ollama list
```

**Ожидаемый результат:**
```
NAME                           ID              SIZE
qwen2.5:32b-instruct-q4_k_m   ...             19 GB
```

**Если модели нет** — дождитесь завершения загрузки (следите за терминалом VS Code).

---

### ШАГ 2: Создать кастомного агента `librarian-core`

**Автоматическая команда:**
```powershell
cd E:\AI_Librarian_Core
Get-Content .\Librarian.Modelfile -Raw | docker exec -i librarian_brain ollama create librarian-core -f -
```

**Что произойдет:**
- Ollama возьмет базовую модель `qwen2.5:32b-instruct-q4_k_m`
- Вшьет в неё системный промпт из `Librarian.Modelfile`
- Создаст новую модель: `librarian-core:latest`

**Ожидаемый вывод:**
```
transferring model data
using existing layer sha256:...
creating new layer sha256:...
writing manifest
success
```

**Проверка:**
```powershell
docker exec librarian_brain ollama list
```

Должна появиться модель `librarian-core`.

---

### ШАГ 3: Настроить Open WebUI для подключения к librarian_brain

**Проблема:** Open WebUI на порту 3000 настроен на внутренний прокси `/ollama`, а не на наш `librarian_brain:11434`.

**Решение A: Через переменные окружения (требует перезапуск)**

```powershell
# Остановить контейнер
docker stop open-webui

# Запустить с новой переменной
docker run -d \
  -p 3000:8080 \
  --add-host=host.docker.internal:host-gateway \
  -e OLLAMA_BASE_URL=http://host.docker.internal:11434 \
  -v E:\OpenWebUI_Data:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:main
```

**Решение B: Через веб-интерфейс (проще)**

1. Откройте http://localhost:3000
2. Зарегистрируйтесь или войдите
3. **Settings** (⚙️) → **Admin Settings** → **Connections**
4. В поле **Ollama API URL** введите:
   ```
   http://host.docker.internal:11434
   ```
5. Нажмите **Verify Connection**
6. Должно появиться ✅ (галочка)
7. Нажмите **Save**

---

### ШАГ 4: Загрузить консолидированную библиотеку

**Файлы для загрузки:**
```
E:\AI_Librarian_Core\library_source\CONSOLIDATED_LIBRARY\
├── 01_Architecture_Methodology.md (3.3 MB)
├── 05_Prompts_Instructions.md (523 KB)
└── Uncategorized.md (3.1 MB)
```

**Процесс:**

1. Open WebUI → **Workspace** → **Documents**
2. Нажмите **Upload Files**
3. Выберите все 3 файла из `CONSOLIDATED_LIBRARY\`
4. В поле **Collection Name** введите: `Library_V1`
5. Нажмите **Upload**

**Индексация:**
- Open WebUI автоматически загрузит модель эмбеддинга `nomic-embed-text:latest`
- Создаст векторные индексы (~7-10 минут)

---

### ШАГ 5: Создать чат с агентом

1. **New Chat** (+ в левом меню)
2. В **Model Selector** выберите `librarian-core`
3. Активируйте **RAG**: кликните иконку 📄 в поле ввода
4. Выберите коллекцию: `Library_V1`

---

## 🧪 Тестовый запрос

**Проверка "профпригодности" библиотекаря:**

```
Найди информацию об архитектуре агентов и методологии ТРИЗ.
Используй только библиотеку.
Структурируй ответ.
```

**Ожидаемое поведение:**

1. Агент использует данные из `01_Architecture_Methodology.md`
2. Ответ структурирован (маркеры, таблицы, разделы)
3. Указаны источники (имена файлов или разделы)
4. Стиль: лаконичный, технический, без лишней вежливости

**Если агент говорит "В библиотеке нет данных":**
- Проверьте, что документы проиндексированы (статус ✅ Indexed)
- Убедитесь, что RAG активирован (иконка 📄 синяя)
- Попробуйте переключить RAG выкл/вкл

---

## 🔧 Команды для отладки

### Проверка моделей в Ollama
```powershell
docker exec librarian_brain ollama list
```

### Проверка логов Ollama
```powershell
docker logs -f librarian_brain
```

### Проверка логов Open WebUI
```powershell
docker logs -f open-webui
```

### Пересоздание агента (если нужно обновить промпт)
```powershell
# 1. Отредактируйте Librarian.Modelfile
# 2. Удалите старую модель
docker exec librarian_brain ollama rm librarian-core

# 3. Пересоздайте
Get-Content .\Librarian.Modelfile -Raw | docker exec -i librarian_brain ollama create librarian-core -f -
```

---

## 📊 Архитектура "Сущность вместо настроек"

**Традиционный подход:**
```
Модель Qwen → Open WebUI → Настройки чата → System Prompt
```
❌ Промпт теряется при смене UI или экспорте чата

**Наш подход:**
```
Модель Qwen → ollama create → librarian-core (с вшитым промптом)
```
✅ Промпт навсегда привязан к модели
✅ Работает из консоли: `ollama run librarian-core "Вопрос"`
✅ Переносимость: можно экспортировать и импортировать

---

## 🎯 Что дальше?

После успешного теста:

1. **Оптимизация RAG:**
   - Настроить Chunk Size (Settings → Documents)
   - Увеличить Top K для более релевантного поиска

2. **Расширение библиотеки:**
   - Добавить новые документы через Upload Files
   - Автоматическая индексация

3. **Создание специализированных агентов:**
   - `librarian-architect` (фокус на архитектуре)
   - `librarian-coder` (фокус на коде)
   - Каждый с своим Modelfile и температурой

4. **Интеграция с инструментами:**
   - Python Functions в Open WebUI
   - Подключение к скриптам из `E:\AGENTS\scripts\`

---

**Текущий статус:** ⏳ Ожидание загрузки базовой модели → ШАГ 2

**Следующее действие:** Выполнить команду создания агента после завершения загрузки.
