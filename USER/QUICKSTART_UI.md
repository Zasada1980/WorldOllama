# 🚀 WORLD_OLLAMA — Быстрый старт через интерфейс

**Версия:** 1.0  
**Дата:** 02 декабря 2025 г.  
**Для:** Пользователей Desktop Client (v0.3.1)

---

## 📋 Что это?

Эта инструкция поможет вам **запустить WORLD_OLLAMA Desktop Client** — графическое приложение для работы с локальным AI через удобный интерфейс.

**Что получите:**
- 💬 Чат с локальными AI моделями (Ollama)
- 📚 Доступ к базе знаний (486+ документов ТРИЗ/AI)
- 🔧 Мониторинг системы в реальном времени
- ⚙️ Настройки моделей и параметров
- ⚡ Автоматизация через Flows (проверка, индексация, обучение)

---

## ✅ Системные требования

| Компонент | Минимум | Рекомендовано |
|-----------|---------|---------------|
| **OS** | Windows 10 64-bit | Windows 11 |
| **GPU** | RTX 3060 (12 GB VRAM) | RTX 5060 Ti (16 GB) |
| **RAM** | 16 GB | 32 GB |
| **Диск** | 50 GB SSD | 100 GB NVMe SSD |

**Предустановленное ПО:**
- ✅ **Ollama** (для LLM моделей) — должен быть установлен и запущен
- ✅ **Python 3.11+** (для CORTEX сервера)
- ✅ **Node.js 20+** (для Desktop Client)

---

## 🎯 Пошаговая инструкция

### Шаг 1: Проверка сервисов

Перед запуском Desktop Client необходимо убедиться, что все backend-сервисы работают.

**Откройте PowerShell** и выполните:

```powershell
pwsh E:\WORLD_OLLAMA\scripts\CHECK_STATUS.ps1 -Detailed
```

**Ожидаемый результат:**
```
========================================
WORLD_OLLAMA Service Status
========================================

✅ Ollama (Port 11434): Running
   Details: Response: 45ms

✅ CORTEX (LightRAG) (Port 8004): Running
   Details: Response: 120ms

⚠️ Neuro-Terminal (Port 8501): Down
   Details: Port not listening

========================================
✓ All services operational (2/3)
========================================
```

**Что означают статусы:**
- ✅ **Running** — Сервис работает нормально
- ⚠️ **Down** — Сервис остановлен (Neuro-Terminal опционален, можно игнорировать)
- ❌ **Error** — Проблема с сервисом (нужно исправить)

---

### Шаг 2: Запуск сервисов (если что-то не работает)

Если `CHECK_STATUS.ps1` показал, что **Ollama** или **CORTEX** не запущены:

**Вариант A: Автоматический запуск всех сервисов**

```powershell
pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1
```

Скрипт автоматически:
1. Проверит Ollama (если не запущен — покажет инструкцию)
2. Запустит CORTEX (LightRAG) в отдельном окне PowerShell
3. (Опционально) Запустит Neuro-Terminal UI

**Вариант B: Ручной запуск**

Если автоматический запуск не сработал:

**Ollama** (если не запущен):
- Откройте Ollama из меню Пуск, ИЛИ
- В PowerShell: `ollama serve`

**CORTEX** (LightRAG):
```powershell
cd E:\WORLD_OLLAMA\services\lightrag
.\venv\Scripts\Activate.ps1
python lightrag_server.py
```

---

### Шаг 3: Запуск Desktop Client

Когда сервисы запущены (Ollama + CORTEX), откройте **новое окно PowerShell**:

```powershell
cd E:\WORLD_OLLAMA\client
npm run tauri dev
```

**Что произойдёт:**
1. Запустится процесс сборки (может занять 30-60 секунд при первом запуске)
2. Откроется окно Desktop Client автоматически
3. В терминале будет отображаться лог работы приложения

**Окно выглядит так:**

```
┌─────────────────────────────────────────┐
│  WORLD_OLLAMA Desktop Client v0.3.1     │
├─────────────────────────────────────────┤
│  Вкладки:                               │
│  💬 Chat  📡 Status  ⚙️ Settings       │
│  📚 Library  🎯 Commands  🏋️ Training  │
│  ⚡ Flows  🔐 Git Push                   │
└─────────────────────────────────────────┘
```

---

## 🎨 Работа с интерфейсом

### Панель 1: 💬 Chat (Чат)

**Назначение:** Общение с AI моделями (Ollama) и база знаний (CORTEX)

**Как использовать:**

1. **Выберите режим:**
   - 🤖 **Ollama** — прямой запрос к LLM (быстрые ответы без контекста)
   - 🧠 **CORTEX** — запрос с поиском по базе знаний (ТРИЗ документы)

2. **Напишите вопрос в поле ввода** (внизу панели)

3. **Нажмите Enter** или кнопку **Send**

**Пример запроса (CORTEX режим):**
```
Объясни принцип дробления в ТРИЗ с примерами
```

**Ответ содержит:**
- 📝 Текст ответа от модели
- 📚 Источники (документы из базы знаний, которые использовались)
- 🔗 Ссылки на оригинальные файлы

**Переключение моделей:**
- Перейдите на вкладку **Settings** → выберите модель из списка
- Доступные модели: `qwen2.5:14b`, `qwen2.5:3b`, `llama3.1:8b`

---

### Панель 2: 📡 System Status (Мониторинг)

**Назначение:** Проверка статуса сервисов в реальном времени

**Что показывает:**

| Сервис | Порт | Описание |
|--------|------|----------|
| **Ollama** | 11434 | LLM модели (qwen2.5:14b, nomic-embed-text) |
| **CORTEX** | 8004 | GraphRAG база знаний (486+ документов) |
| **Neuro-Terminal** | 8501 | Веб-интерфейс Chainlit (опционально) |

**Индикаторы:**
- 🟢 **UP** — Сервис работает нормально
- 🔴 **DOWN** — Сервис остановлен или недоступен
- ⚪ **UNKNOWN** — Не удалось определить статус

**Auto-refresh:** Статусы обновляются каждые **15 секунд** автоматически

**Действия при проблемах:**
- Если CORTEX 🔴 DOWN → запустите `START_ALL.ps1` (см. Шаг 2)
- Если Ollama 🔴 DOWN → запустите Ollama из меню Пуск

---

### Панель 3: ⚙️ Settings (Настройки)

**Назначение:** Конфигурация моделей, параметров поиска, профилей агентов

**Основные настройки:**

**Ollama:**
- **Model** — выбор LLM модели (qwen2.5:14b, llama3.1:8b и т.д.)
- **Max Tokens** — максимальная длина ответа (оставьте пустым для авто)

**CORTEX (Knowledge Base):**
- **Base URL** — адрес CORTEX сервера (обычно `http://localhost:8004`)
- **API Key** — ключ доступа (по умолчанию: `sesa-secure-core-v1`)
- **Top K** — количество документов для поиска (20-30 рекомендовано)
- **Mode** — режим поиска:
  - `local` — быстрый локальный поиск
  - `global` — глубокий поиск по всему графу
  - `hybrid` — **рекомендуется** (адаптивный)

**Agent Profiles:**
- **Active Profile** — текущий профиль агента:
  - `default` — базовый ассистент
  - `triz_engineer` — ТРИЗ-инженер (специализация на изобретательских задачах)
  - `triz_researcher` — ТРИЗ-исследователь (анализ паттернов)

**Сохранение настроек:**
- Изменения **сохраняются автоматически** при переключении вкладок
- Хранятся в: `%APPDATA%\WorldOllama\settings.json`

---

### Панель 4: 📚 Library (Библиотека)

**Назначение:** Управление базой знаний (индексация документов)

**Функции:**

**📊 Статус индексации:**
- Общее количество документов в базе
- Количество проиндексированных документов
- Прогресс индексации (%)

**🔄 Индексация документов:**

1. Нажмите кнопку **"Start Indexation"**
2. Процесс может занять **10-30 минут** (зависит от количества документов)
3. Прогресс отображается в реальном времени

**Что происходит:**
- Документы из `library/raw_documents/` читаются и разбиваются на чанки (~10 KB)
- Создаются векторные эмбеддинги (nomic-embed-text)
- Строится граф знаний (entities + relations)
- Данные сохраняются в `services/lightrag/data/`

**⚠️ Важно:**
- **Не запускайте индексацию повторно** без необходимости (данные уже проиндексированы)
- Проверьте текущий статус перед индексацией
- При ошибках проверьте логи: `services/lightrag/logs/cortex.log`

---

### Панель 5: 🎯 Commands (Командная панель)

**Назначение:** Выполнение системных команд через DSL (Domain-Specific Language)

**Доступные команды:**

| Команда | Описание | Пример |
|---------|----------|--------|
| **STATUS** | Проверка статуса всех сервисов | `STATUS` |
| **INDEX** | Запуск индексации базы знаний | `INDEX` |
| **TRAIN** | Запуск обучения модели | `TRAIN profile=triz_engineer epochs=3` |
| **GIT_PUSH** | Безопасный push в Git с валидацией | `GIT_PUSH` |

**Как использовать:**

1. Введите команду в поле ввода
2. Нажмите **Execute** или Enter
3. Результат отобразится ниже

**Пример (проверка статуса):**
```
Команда: STATUS
Результат:
✅ Ollama: Running
✅ CORTEX: Running
⚠️ Neuro-Terminal: Down (optional)
```

**Пример (обучение модели):**
```
Команда: TRAIN profile=triz_engineer epochs=2
Результат:
🚀 Training started (Job ID: train-20251202-143022)
📊 Profile: triz_engineer
📈 Epochs: 2/5
📁 Output: models/triz_engineer_20251202
```

---

### Панель 6: 🏋️ Training (Обучение моделей)

**Назначение:** Fine-tuning моделей через UI

**Параметры обучения:**

**Profile** (профиль обучения):
- `default` — базовая модель
- `triz_engineer` — ТРИЗ-инженер (300 примеров диалогов)
- `triz_researcher` — ТРИЗ-исследователь
- `lightweight` — облегчённая модель (для тестов)

**Dataset Path** (путь к датасету):
- Автоматически подставляется для выбранного профиля
- Пример: `E:\WORLD_OLLAMA\services\llama_factory\data\triz_synthesis_v1.jsonl`

**Epochs** (эпохи обучения):
- Допустимый диапазон: **1-5** (рекомендуется 2-3)
- Больше эпох = дольше обучение, но лучше качество

**Процесс:**

1. Выберите профиль из списка
2. Укажите количество эпох (1-5)
3. Нажмите **"Start Training"**
4. Мониторьте прогресс через **PULSE v1** (обновляется каждые 2-10 секунд)

**PULSE v1 индикаторы:**

| Статус | Описание |
|--------|----------|
| **idle** | Обучение не запущено |
| **running** | Обучение в процессе (прогресс-бар + текущий loss) |
| **done** | Обучение завершено успешно ✅ |
| **error** | Ошибка во время обучения ❌ |

**Логи обучения:**
- Реальное время: `services/llama_factory/training_status.json`
- Полные логи: `logs/training/train-YYYYMMDD-HHMMSS.log`

**⚠️ Требования:**
- GPU с 16 GB VRAM (RTX 5060 Ti / RTX 4060 Ti)
- Свободное место на диске: 10-20 GB
- Время обучения: 30-120 минут (зависит от epochs)

---

### Панель 7: ⚡ Flows (Автоматизация)

**Назначение:** Pre-built workflow для автоматизации многошаговых задач

**Доступные Flow:**

| Flow | Шаги | Описание |
|------|------|----------|
| **Quick Status** | 1 | Быстрая проверка здоровья системы (STATUS) |
| **Smoke Test** | 2 | STATUS + проверка Git |
| **Git Check** | 1 | Валидация состояния репозитория |
| **Train Default** | 2 | STATUS + запуск обучения (профиль default) |
| **Index & Train** | 3 | STATUS → INDEX → TRAIN (полный цикл) |

**Как использовать:**

1. Выберите нужный Flow (клик на карточку)
2. Нажмите **▶️ Run Flow**
3. Наблюдайте выполнение шагов в реальном времени
4. Проверяйте историю выполнения внизу панели

**Пример (Index & Train Flow):**
```
Flow: Index & Train
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1/3: STATUS ✅ (2s)
  ✓ Ollama: Running
  ✓ CORTEX: Running

Step 2/3: INDEX 🔄 (estimating 15 min)
  ⏳ Indexing 486 documents...
  ⏳ Progress: 45% (220/486)

Step 3/3: TRAIN ⏸️ (waiting)
  ⏳ Pending INDEX completion
```

**Observability:**
- Каждое выполнение логируется в `logs/flows/flow_{id}_{timestamp}.jsonl`
- История последних 10 запусков отображается в таблице
- Можно отследить длительность каждого шага и общее время

**Ошибки:**
- Если шаг провалился → Flow останавливается (если не указано `continue_on_error`)
- Детали ошибки показываются в UI + в логах

---

### Панель 8: 🔐 Git Push Safety (Безопасный Git)

**Назначение:** Валидация перед push в удалённый репозиторий (предотвращение ошибок)

**Two-Phase Workflow:**

**Phase 1: PLAN (только чтение)**

1. Нажмите **"Plan Push"**
2. Система проверит **7 блокирующих сценариев**:
   - ❌ Unstaged changes (изменения не в staging area)
   - ❌ Uncommitted changes (staged но не закоммичены)
   - ❌ Branch mismatch (текущая ветка не `main`)
   - ❌ Remote not found (удалённый репозиторий не найден)
   - ❌ Branch not on remote (ветка не существует на remote)
   - ❌ Remote ahead (remote содержит новые коммиты → нужен `git pull`)
   - ❌ No upstream (у ветки нет upstream конфигурации)

3. Отобразится **Git Push Plan:**
   - **Status:** `ready` / `blocked` / `pending_verification`
   - **Outgoing Commits:** список коммитов для push
   - **Files Changed:** список изменённых файлов
   - **Blocked Reasons:** причины блокировки (если есть)

**Phase 2: EXECUTE (запись)**

4. Если **Status = ready** → кнопка **"Execute Push"** активна
5. Нажмите **"Execute Push"**
6. Система **повторно проверит** все условия (двойная валидация)
7. Если всё OK → выполнится `git push`

**Принципы ТРИЗ:**
- **№10 (Preliminary Action):** План создаётся ДО выполнения
- **№3 (Local Quality):** Учитывается локальное И удалённое состояние
- **№13 (Inversion):** "Pending Verification" как требование, а не проблема

**⚠️ Ограничения:**
- Требуется предварительный `git fetch` для проверки "Remote ahead"
- Нет preview содержимого изменений (только список файлов)
- Нет автоматической детекции API keys в коде

---

## 🔧 Устранение проблем

### Проблема 1: Desktop Client не открывается

**Симптомы:**
- `npm run tauri dev` завершается с ошибкой
- Окно не появляется после запуска

**Решения:**

1. **Проверьте зависимости:**
   ```powershell
   cd E:\WORLD_OLLAMA\client
   npm install
   ```

2. **Проверьте Rust toolchain:**
   ```powershell
   rustc --version
   cargo --version
   ```
   Если ошибка → установите Rust: https://rustup.rs/

3. **Проверьте логи компиляции** в терминале
   - Ошибки TypeScript → проверьте `src/` файлы
   - Ошибки Rust → проверьте `src-tauri/src/` файлы

4. **Очистка кэша:**
   ```powershell
   npm run clean
   npm install
   npm run tauri dev
   ```

---

### Проблема 2: Chat не отвечает (Ollama/CORTEX)

**Симптомы:**
- Вопросы в чате не получают ответ
- Ошибка "Connection refused" или "Service unavailable"

**Решения:**

1. **Проверьте статус сервисов:**
   ```powershell
   pwsh E:\WORLD_OLLAMA\scripts\CHECK_STATUS.ps1
   ```

2. **Если Ollama 🔴 DOWN:**
   - Откройте Ollama из меню Пуск
   - ИЛИ в PowerShell: `ollama serve`

3. **Если CORTEX 🔴 DOWN:**
   ```powershell
   pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1
   ```

4. **Проверьте настройки в Settings:**
   - Ollama Base URL: `http://localhost:11434`
   - CORTEX Base URL: `http://localhost:8004`
   - CORTEX API Key: `sesa-secure-core-v1`

5. **Проверьте модель:**
   ```powershell
   ollama list
   ```
   Если модель отсутствует:
   ```powershell
   ollama pull qwen2.5:14b
   ollama pull nomic-embed-text
   ```

---

### Проблема 3: CORTEX не находит документы (пустые ответы)

**Симптомы:**
- Chat (CORTEX режим) возвращает пустые ответы
- Нет источников (sources) в ответах

**Решения:**

1. **Проверьте индексацию:**
   - Перейдите на панель **Library**
   - Проверьте статус индексации

2. **Если документы не проиндексированы:**
   - Нажмите **"Start Indexation"**
   - Дождитесь завершения (10-30 минут)

3. **Проверьте директорию документов:**
   ```powershell
   dir E:\WORLD_OLLAMA\library\raw_documents\
   ```
   Должно быть 486+ .txt файлов

4. **Проверьте логи CORTEX:**
   ```powershell
   Get-Content E:\WORLD_OLLAMA\services\lightrag\logs\cortex.log -Tail 50
   ```

5. **Измените режим поиска:**
   - Settings → CORTEX Mode → попробуйте `hybrid` или `global`

---

### Проблема 4: Training не запускается

**Симптомы:**
- Кнопка "Start Training" неактивна
- Ошибка при запуске обучения

**Решения:**

1. **Проверьте VRAM:**
   ```powershell
   nvidia-smi
   ```
   Требуется минимум **12 GB VRAM** (рекомендуется 16 GB)

2. **Проверьте параметры:**
   - Профиль должен быть из whitelist: `default`, `triz_engineer`, `triz_researcher`, `lightweight`
   - Epochs должны быть 1-5

3. **Проверьте датасет:**
   ```powershell
   Test-Path E:\WORLD_OLLAMA\services\llama_factory\data\triz_synthesis_v1.jsonl
   ```

4. **Проверьте HuggingFace модель (ORDER 43):**
   - Если модель gated → выполните:
     ```powershell
     huggingface-cli login
     ```
   - ИЛИ переключитесь на open модель в конфигурации

5. **Проверьте логи:**
   ```powershell
   Get-Content E:\WORLD_OLLAMA\services\llama_factory\training_status.json
   Get-Content E:\WORLD_OLLAMA\logs\training\train-*.log -Tail 50
   ```

---

### Проблема 5: Flows застревают на шагах

**Симптомы:**
- Flow выполняется, но один из шагов висит без прогресса
- Таймаут на INDEX или TRAIN

**Решения:**

1. **Проверьте логи Flow:**
   ```powershell
   Get-Content E:\WORLD_OLLAMA\logs\flows\flow_*_*.jsonl | Select-Object -Last 20
   ```

2. **Если застрял INDEX:**
   - Проверьте VRAM: `nvidia-smi` (должно быть >6 GB используется)
   - Проверьте CORTEX логи: `services/lightrag/logs/cortex.log`

3. **Если застрял TRAIN:**
   - Проверьте `training_status.json` (должен обновляться каждые 2-10 секунд)
   - Если обучение повисло → убейте процесс Python вручную:
     ```powershell
     Get-Process python | Where-Object {$_.CommandLine -like "*llamafactory*"} | Stop-Process
     ```

4. **Перезапустите Flow:**
   - Отмените текущий
   - Проверьте систему: `pwsh scripts\CHECK_STATUS.ps1`
   - Запустите заново

---

## 📞 Поддержка

**Документация:**
- **Полный индекс:** `DOCUMENTATION_INDEX.md`
- **Руководство:** `MANUAL.md`
- **Проект:** `PROJECT_MAP.md`
- **Статус:** `PROJECT_STATUS_SNAPSHOT_v4.0.md`

**Консолидированные отчёты:**
- Desktop Client (TASK 4-16): `docs/tasks/TASKS_CONSOLIDATED_REPORT.md`
- Модели/обучение: `docs/models/MODELS_CONSOLIDATED_REPORT.md`
- Инфраструктура: `docs/infrastructure/INFRASTRUCTURE_CONSOLIDATED_REPORT.md`

**Логи:**
- MCP Shell: `logs/mcp/mcp-events.log`
- CORTEX: `services/lightrag/logs/cortex.log`
- Training: `logs/training/train-*.log`
- Flows: `logs/flows/flow_*_*.jsonl`

**GitHub:**
- Репозиторий: https://github.com/Zasada1980/WorldOllama
- Issues: https://github.com/Zasada1980/WorldOllama/issues
- Релизы: https://github.com/Zasada1980/WorldOllama/releases

---

## 🎓 Дополнительные ресурсы

**Обучающие материалы:**
- Пример работы с CORTEX: `docs/reports/RAG_QUALITY_REPORT.md`
- Профили агентов: `client/TASK5_REPORT.md`
- Command DSL: `client/TASK_8_COMPLETION_REPORT.md`
- Flows automation: `docs/tasks/TASKS_CONSOLIDATED_REPORT.md` (TASK 22)

**Видео демонстрации:**
- (планируется) Базовая работа с Desktop Client
- (планируется) Настройка и обучение модели

---

**Версия документа:** 1.0  
**Последнее обновление:** 02 декабря 2025 г.  
**Статус проекта:** v0.3.1 (Production Ready)

---

_Приятной работы с WORLD_OLLAMA! 🚀_
