# Task 8 — Testing Guide

**Дата:** 27 ноября 2025 г.  
**Версия:** 1.0  
**Назначение:** Руководство по тестированию Command Slot системы

---

## 1. Overview

Этот документ описывает **5 обязательных сценариев** для тестирования Task 8 (Command Slot + Training UI).

**Цель тестирования:**

1. Проверить корректность парсинга DSL команд
2. Убедиться в работе заглушек TRAIN/GIT (безопасность)
3. Проверить интеграцию Chat → CommandSlot
4. Убедиться в корректной обработке ошибок
5. Проверить UX (уведомления, навигация, состояния UI)

---

## 2. Подготовка к тестированию

### 2.1 Требования

**Системные:**

- ✅ Windows 10/11
- ✅ Cargo installed (Rust)
- ✅ Node.js 18+ (npm)
- ✅ PowerShell 7+

**Приложение:**

- ✅ Таури приложение скомпилировано (`cargo check` прошёл успешно)
- ✅ Vite dev server работает
- ✅ Все зависимости установлены (`npm install`)

---

### 2.2 Запуск приложения

```powershell
# 1. Проверка PATH (если cargo не найден)
$env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"

# 2. Переход в директорию клиента
cd E:\WORLD_OLLAMA\client

# 3. Запуск Tauri dev mode
npm run tauri dev
```

**Ожидаемый результат:**

```
VITE v6.4.1 ready in 500-700ms
➜ Local: http://localhost:1420/

Running DevCommand (`cargo run --no-default-features --color always --`)
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.2-1s
Running `target\debug\tauri_fresh.exe`
```

**Приложение открывается на:** `http://localhost:1420/`

---

### 2.3 Инструменты отладки

**Browser DevTools:**

```
F12 или Ctrl+Shift+I
```

**Проверка логов Rust:**

- Вывод в терминале (где запущен `npm run tauri dev`)
- Ошибки парсинга, validation errors

**Проверка файлов статуса:**

```powershell
# Training status (TRAIN AGENT)
notepad $env:APPDATA\tauri_fresh\training_status.json

# Indexation status (INDEX KNOWLEDGE)
notepad $env:APPDATA\tauri_fresh\indexation_status.json
```

---

## 3. Тестовые сценарии

---

## 📌 Сценарий 1: INDEX KNOWLEDGE (ручной ввод)

**Цель:** Проверить корректность парсинга и выполнения INDEX команды вручную.

---

### Шаги

1. **Открыть приложение**
2. **Перейти на вкладку 🔧 Commands**
3. **Очистить поле** (кнопка "🗑️ Очистить")
4. **Ввести команду:**

```
INDEX KNOWLEDGE
PATH="E:\WORLD_OLLAMA\library\raw_documents"
MODE="local"
PROFILE="triz_engineer"
```

5. **Ввести описание:** "Тест индексации библиотеки ТРИЗ"
6. **Нажать "▶️ Запустить команду"**

---

### Ожидаемый результат

**✅ Успех:**

1. **Notification (toast):**
   - Тип: Success (зелёная)
   - Сообщение: "Команда INDEX_KNOWLEDGE выполнена"
   - Детали: "INDEX KNOWLEDGE команда принята: PATH: E:\WORLD_OLLAMA\library\raw_documents..."

2. **Result block:**
   - Зелёная рамка
   - Badge: `INDEX_KNOWLEDGE`
   - Иконка: ✅
   - Сообщение:
     ```
     INDEX KNOWLEDGE команда принята:
     PATH: E:\WORLD_OLLAMA\library\raw_documents
     MODE: local
     PROFILE: triz_engineer
     ```

3. **Console (DevTools):**
   - Нет ошибок
   - Возможно: `[CommandSlot] Executing command...`

---

### ❌ Возможные ошибки

**Ошибка 1: "Команда пуста"**

- **Причина:** Поле commandText пустое
- **Решение:** Ввести команду

**Ошибка 2: "Ошибка парсинга команды"**

- **Причина:** Неправильный формат (например, `PATH=E:\...` без кавычек)
- **Решение:** Добавить кавычки: `PATH="E:\..."`

**Ошибка 3: "Ошибка валидации"**

- **Причина:** Отсутствует обязательный ключ PATH
- **Решение:** Добавить `PATH="..."`

---

### Проверка логов

**PowerShell (терминал с Tauri):**

```
# Должен быть вывод от execute_agent_command
# (Пока только логирование, реальной индексации нет)
```

---

## 📌 Сценарий 2: INDEX через Chat (генерация команды)

**Цель:** Проверить интеграцию Chat → CommandSlot (keyword-based генерация).

---

### Шаги

1. **Открыть приложение**
2. **Перейти на вкладку 💬 Chat**
3. **Написать сообщение:**

```
Проиндексируй папку E:\WORLD_OLLAMA\library\raw_documents
```

4. **Нажать кнопку "🔧 Команда"** (НЕ "➤ Отправить"!)
5. **Дождаться notification**
6. **Перейти на вкладку 🔧 Commands**

---

### Ожидаемый результат

**✅ Успех:**

1. **Notification (toast) в Chat:**
   - Тип: Info (синяя)
   - Сообщение: "Команда INDEX сгенерирована"
   - Детали: "Проверьте вкладку 🔧 Commands"

2. **Вкладка Commands:**
   - Textarea заполнен:
     ```
     INDEX KNOWLEDGE
     PATH="E:\WORLD_OLLAMA\library\raw_documents"
     MODE="local"
     PROFILE="triz_engineer"
     ```
   - Description:
     ```
     Индексация папки E:\WORLD_OLLAMA\library\raw_documents под профилем triz_engineer
     ```

3. **Кнопка "▶️ Запустить команду" активна**
4. **Можно выполнить команду** (см. Сценарий 1, шаг 6)

---

### ❌ Возможные ошибки

**Ошибка 1: "Нет сообщений для анализа"**

- **Причина:** Кнопка "🔧 Команда" нажата до отправки сообщения
- **Решение:** Написать хотя бы одно сообщение

**Ошибка 2: "Не удалось распознать команду"**

- **Причина:** Сообщение не содержит ключевых слов (`индекс`, `index`, `папк`, `folder`)
- **Решение:** Изменить сообщение на "индексация папки ..." или "index folder ..."

---

### Проверка логов

**DevTools Console:**

```javascript
// Должен быть вызов:
commandSlotStore.setCommand("INDEX KNOWLEDGE\n...", "Индексация папки ...");
```

---

## 📌 Сценарий 3: TRAIN AGENT (stub)

**Цель:** Проверить заглушку TRAIN (безопасность — не запускаем реальное обучение).

---

### Шаги

1. **Открыть приложение**
2. **Перейти на вкладку 🔧 Commands**
3. **Нажать кнопку "🧠 TRAIN"** (Quick Access шаблон)
4. **Проверить заполненную команду:**

```
TRAIN AGENT
PROFILE="triz_engineer"
DATA_PATH="E:\WORLD_OLLAMA\training_data"
EPOCHS="5"
MODE="llama_factory"
```

5. **Нажать "▶️ Запустить команду"**
6. **Дождаться результата**

---

### Ожидаемый результат

**✅ Успех:**

1. **Notification (toast):**
   - Тип: Success
   - Сообщение: "Команда TRAIN_AGENT выполнена"
   - Детали: "TRAIN AGENT команда принята (STUB): PROFILE: triz_engineer..."

2. **Result block:**
   - Зелёная рамка
   - Badge: `TRAIN_AGENT`
   - Иконка: ✅
   - Сообщение:
     ```
     TRAIN AGENT команда принята (STUB):
     PROFILE: triz_engineer
     DATA_PATH: E:\WORLD_OLLAMA\training_data
     EPOCHS: 5
     MODE: llama_factory
     
     Статус сохранён в training_status.json
     ```

3. **Файл статуса создан:**

```powershell
# Проверка файла
cat $env:APPDATA\tauri_fresh\training_status.json
```

**Ожидаемое содержимое:**

```json
{
  "state": "received",
  "profile": "triz_engineer",
  "data_path": "E:\\WORLD_OLLAMA\\training_data",
  "epochs": 5,
  "started_at": "2025-11-27T20:30:00Z",
  "last_error": null
}
```

4. **НЕТ реального обучения:**
   - GPU VRAM не растёт (проверка `nvidia-smi`)
   - Нет процесса Python с LLaMA Factory

---

### ❌ Возможные ошибки

**Ошибка 1: "Ошибка валидации TRAIN AGENT"**

- **Причина:** Отсутствует PROFILE или DATA_PATH
- **Решение:** Добавить обязательные ключи

**Ошибка 2: "status_save_failed"**

- **Причина:** Нет прав на запись в `%APPDATA%`
- **Решение:** Проверить права доступа

---

### Проверка логов

**PowerShell (терминал с Tauri):**

```
# Должен быть вызов save_training_status()
# Без ошибок сериализации JSON
```

---

## 📌 Сценарий 4: GIT PUSH с неразрешённым путём (безопасность)

**Цель:** Проверить security check для GIT (whitelist путей).

---

### Шаги

1. **Открыть приложение**
2. **Перейти на вкладку 🔧 Commands**
3. **Очистить поле**
4. **Ввести команду с НЕРАЗРЕШЁННЫМ путём:**

```
GIT PUSH
REPO_PATH="C:\Users\Admin\SecretProject"
BRANCH="main"
SUMMARY="Test commit"
```

5. **Нажать "▶️ Запустить команду"**

---

### Ожидаемый результат

**✅ Успех (ошибка безопасности):**

1. **Notification (toast):**
   - Тип: Error (красная)
   - Сообщение: "Ошибка выполнения команды"
   - Детали: "REPO_PATH должен начинаться с E:\WORLD_OLLAMA (безопасность)"

2. **Result block:**
   - Красная рамка
   - Badge: `UNKNOWN`
   - Иконка: ❌
   - Сообщение: "REPO_PATH должен начинаться с E:\WORLD_OLLAMA..."

3. **НЕТ выполнения git команд:**
   - Никаких изменений в репозиториях
   - Нет вызова git.exe

---

### ❌ НЕожиданный результат (ОШИБКА!)

**Если команда выполнилась успешно:**

- 🔴 **КРИТИЧЕСКАЯ ОШИБКА БЕЗОПАСНОСТИ**
- Security check не работает
- Необходимо исправить `execute_agent_command` (проверка пути)

---

### Проверка логов

**PowerShell (терминал с Tauri):**

```
# Должна быть ошибка:
ApiResponse::error("security_error", "REPO_PATH должен начинаться с E:\\WORLD_OLLAMA")
```

---

## 📌 Сценарий 5: Invalid DSL (обработка ошибок)

**Цель:** Проверить корректность обработки некорректного синтаксиса.

---

### Шаги

1. **Открыть приложение**
2. **Перейти на вкладку 🔧 Commands**
3. **Очистить поле**
4. **Ввести НЕПРАВИЛЬНУЮ команду:**

```
INDEX KNOWLEDGE
PATH=E:\WORLD_OLLAMA\library\raw_documents
MODE=local
```

**(Обратите внимание: нет кавычек вокруг значений!)**

5. **Нажать "▶️ Запустить команду"**

---

### Ожидаемый результат

**✅ Успех (parse error):**

1. **Notification (toast):**
   - Тип: Error
   - Сообщение: "Ошибка выполнения команды"
   - Детали: "Ошибка парсинга команды: InvalidFormat(\"Значение должно быть в кавычках: PATH=E:\\...\")"

2. **Result block:**
   - Красная рамка
   - Badge: `UNKNOWN`
   - Иконка: ❌
   - Сообщение: "Ошибка парсинга команды: InvalidFormat(...)"

3. **Команда НЕ выполнена:**
   - Нет записи в статусные файлы
   - Нет вызова бэкенд-логики

---

### Дополнительные варианты ошибок

**Тест 5.1: Неизвестная команда**

```
DEPLOY PRODUCTION
SERVER="prod.example.com"
```

**Ожидаемый результат:**

- `UnknownCommandType("DEPLOY PRODUCTION")`

---

**Тест 5.2: Отсутствующий обязательный ключ**

```
INDEX KNOWLEDGE
MODE="local"
```

**(Отсутствует PATH)**

**Ожидаемый результат:**

- `MissingArgument("PATH")`

---

**Тест 5.3: Пустая команда**

- Textarea пустой
- Нажать "▶️ Запустить команду"

**Ожидаемый результат:**

- Notification: "Команда пуста"

---

## 4. Checklist приёмки

После прохождения всех 5 сценариев:

- [ ] **Сценарий 1:** INDEX KNOWLEDGE (ручной ввод) — ✅ Success
- [ ] **Сценарий 2:** INDEX через Chat — ✅ Команда сгенерирована
- [ ] **Сценарий 3:** TRAIN AGENT (stub) — ✅ Статус сохранён, обучение НЕ запущено
- [ ] **Сценарий 4:** GIT PUSH (безопасность) — ✅ Ошибка security_error
- [ ] **Сценарий 5:** Invalid DSL — ✅ Ошибка парсинга

**Дополнительные проверки:**

- [ ] Навигация между вкладками работает (Chat ↔ Commands)
- [ ] Notifications отображаются корректно (toast появляется, исчезает)
- [ ] Result block обновляется после каждой команды
- [ ] Кнопка "Очистить" работает (очищает commandText и description)
- [ ] Quick Access шаблоны работают (INDEX, TRAIN, GIT)
- [ ] Справка по DSL (details блок) открывается/закрывается

---

## 5. Известные ограничения

### 5.1 Ограничения MVP

1. **INDEX KNOWLEDGE пока не запускает реальную индексацию:**
   - Команда принимается, логируется
   - Интеграция с `start_indexation` — TODO для следующих версий

2. **TRAIN AGENT — заглушка:**
   - Сохраняет статус в JSON
   - Реальное обучение не запускается (безопасность)

3. **GIT PUSH — заглушка:**
   - Проверяет путь
   - Реальный git не вызывается (безопасность)

4. **Chat генерация — keyword-based:**
   - Простой regex поиск
   - Нет NLP/LLM парсинга сообщений

---

### 5.2 Warnings (допустимы для MVP)

**Rust:**

```
warning: function `execute_agent_command` is never used
```

- **Пояснение:** UI только что создан, после первого запуска warning исчезнет

**TypeScript:**

```
warning: unused CSS selector ".message"
```

- **Пояснение:** Некоторые CSS из шаблона не используются в текущей вёрстке

---

## 6. Troubleshooting

### Проблема: "Cannot find command execute_agent_command"

**Причина:** Команда не зарегистрирована в `lib.rs`

**Решение:**

```powershell
# Проверка регистрации
Select-String "execute_agent_command" E:\WORLD_OLLAMA\client\src-tauri\src\lib.rs
```

Должно быть:

```rust
use commands::execute_agent_command;

invoke_handler![
    // ...
    execute_agent_command,
]
```

---

### Проблема: "Parse error: InvalidFormat"

**Причина:** Значения не в кавычках

**Решение:**

```diff
- PATH=E:\WORLD_OLLAMA
+ PATH="E:\WORLD_OLLAMA"
```

---

### Проблема: Notification не появляется

**Причина:** notifications store не инициализирован

**Решение:**

1. Проверить `NotificationCenter.svelte` в `+page.svelte`
2. Проверить консоль DevTools на ошибки

---

### Проблема: Result block не обновляется

**Причина:** `lastResult` не обновляется после выполнения

**Решение:**

```typescript
// В executeCommand():
if (response && response.success) {
  lastResult = response; // ← Должно быть здесь
}
```

---

## 7. Формат отчёта о тестировании

После прохождения всех сценариев, заполните:

```markdown
# Task 8 — Отчёт о тестировании

**Дата:** [дата]
**Тестировщик:** [имя]

## Результаты

| Сценарий | Статус | Комментарий |
|----------|--------|-------------|
| 1. INDEX (ручной) | ✅ / ❌ | ... |
| 2. INDEX (Chat) | ✅ / ❌ | ... |
| 3. TRAIN (stub) | ✅ / ❌ | ... |
| 4. GIT (безопасность) | ✅ / ❌ | ... |
| 5. Invalid DSL | ✅ / ❌ | ... |

## Найденные баги

1. [Описание бага]
   - Воспроизведение: [шаги]
   - Ожидаемое: [...]
   - Фактическое: [...]
   - Severity: Critical / High / Medium / Low

## Общие впечатления

[Комментарии о UX, производительности, качестве кода]

## Рекомендации

[Список улучшений для следующих версий]
```

---

## 8. Заключение

**Task 8 Testing Guide** содержит **5 обязательных сценариев** для проверки корректности Command Slot системы.

**Ключевые аспекты тестирования:**

1. ✅ **Парсинг DSL** — корректность обработки команд
2. ✅ **Безопасность** — заглушки и whitelist путей
3. ✅ **Интеграция** — Chat → CommandSlot мост
4. ✅ **Обработка ошибок** — валидные error messages
5. ✅ **UX** — notifications, навигация, состояния

**Критерий приёмки:**

Все 5 сценариев должны пройти успешно (✅) без критических багов.

**Версия:** 1.0  
**Статус:** ✅ **ГОТОВО К ТЕСТИРОВАНИЮ**
