# 🚀 TASK 15.2 — Quick Start Guide

## Цель
Запустить smoke-test обучения TRIZ-агента через LLaMA Factory с использованием Desktop UI.

## Предварительные требования

✅ Все сервисы 15.2.1–15.2.4 уже реализованы:
- Конфиг `triz_td010v3_smoketest.yaml` создан
- PowerShell интеграция готова
- Rust whitelist обновлён
- TRIZ датасет `triz_dataset.jsonl` доступен (3448 примеров, 8.15 MB)

## Шаг 1: Запустить сервисы

```powershell
# Запуск Ollama + CORTEX (без Neuro-Terminal)
pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1 -SkipNeuroTerminal
```

**Проверка:**
```powershell
# Ollama должен быть доступен
curl http://localhost:11434/api/tags

# CORTEX должен быть доступен
curl http://localhost:8004/health
```

## Шаг 2: Запустить Desktop UI

```powershell
cd E:\WORLD_OLLAMA\client
npm run tauri dev
```

**Ожидаемый результат:** Откроется окно Desktop приложения.

## Шаг 3: Запустить smoke-test через UI

1. В Desktop UI перейти на вкладку **🧪 Training**

2. В поле команды ввести:
   ```
   TRAIN AGENT PROFILE="triz_td010v3_smoketest" DATA_PATH="E:\WORLD_OLLAMA\services\training\data" EPOCHS=1
   ```

3. Нажать **Execute**

4. **Ожидаемое поведение:**
   - В TrainingPanel статус изменится: `idle` → `queued` → `running`
   - Откроется отдельное окно PowerShell с логами обучения
   - Через ~5-15 минут статус изменится на `done`

## Шаг 4: Проверить результаты

### 4.1 Проверить статус в UI
- TrainingPanel должен показывать `state: "done"`
- Прогресс должен быть 100%

### 4.2 Проверить артефакты обучения

```powershell
# Перейти в output-директорию
cd E:\WORLD_OLLAMA\services\llama_factory\outputs\triz_td010v3_smoketest

# Проверить наличие checkpoint-файлов
Get-ChildItem -Recurse | Select-Object Name, Length

# Должны быть файлы:
# - checkpoint-*/adapter_config.json
# - checkpoint-*/adapter_model.safetensors
# - trainer_state.json
# - training_log.jsonl
```

### 4.3 Проверить логи

```powershell
# Просмотреть training log
Get-Content E:\WORLD_OLLAMA\services\llama_factory\outputs\triz_td010v3_smoketest\training_log.jsonl | ConvertFrom-Json | Format-Table

# Ключевые метрики:
# - loss (должна снижаться)
# - learning_rate
# - epoch / step
```

## Шаг 5: Запустить полное обучение (после успешного smoke-test)

Если smoke-test прошёл успешно, можно запустить полное обучение:

```
TRAIN AGENT PROFILE="triz_td010v3_full" DATA_PATH="E:\WORLD_OLLAMA\services\training\data" EPOCHS=1
```

**Отличия от smoke-test:**
- Использует весь датасет (3448 примеров вместо 100)
- Полная эпоха (вместо 0.1 эпохи / 50 шагов)
- Время выполнения: ~2-4 часа (зависит от GPU)
- Output: `outputs/triz_td010v3` (вместо `smoketest`)

## Troubleshooting

### Проблема: "Profile not found in whitelist"
**Решение:** Пересобрать Rust backend:
```powershell
cd E:\WORLD_OLLAMA\client
cargo build
```

### Проблема: "Config file not found"
**Решение:** Проверить наличие конфига:
```powershell
Test-Path E:\WORLD_OLLAMA\services\llama_factory\triz_td010v3_smoketest.yaml
# Должно вернуть True
```

### Проблема: "Out of memory" в логах
**Решение:**
1. Закрыть другие приложения, использующие GPU
2. Проверить VRAM: `nvidia-smi`
3. Если нужно, уменьшить `per_device_train_batch_size: 1` → понизить невозможно, но можно уменьшить `max_samples` в конфиге

### Проблема: Статус зависает в "queued"
**Решение:**
1. Проверить, не запущен ли уже другой процесс обучения
2. Проверить логи PowerShell-окна
3. Перезапустить через UI кнопкой "Clear Status"

## Критерии успеха

✅ Smoke-test завершился со статусом `done`  
✅ Созданы checkpoint-файлы с LoRA-адаптерами  
✅ В `training_log.jsonl` видна динамика метрик  
✅ Нет фатальных ошибок в логах

## Следующие шаги (после TASK 15.2)

После успешного завершения smoke-test и полного обучения:

1. **TASK 15.3** — Экспорт обученной модели в Ollama:
   - Конвертация LoRA-адаптеров в GGUF
   - Создание Modelfile для Ollama
   - Регистрация модели `triz-td010v3`

2. **TASK 15.4** — Привязка к Agent Profile:
   - Обновление профиля `triz_engineer` на новую модель
   - Интеграция с Chat UI

3. **TASK 15.5** — A/B тестирование:
   - Сравнение ответов до/после обучения
   - Документирование улучшений

---

**Создано:** 28 ноября 2025  
**Версия:** 1.0  
**Автор:** CODEX Agent  
**Статус:** READY FOR EXECUTION
