# 📋 TASK 7: LIBRARY & INDEXATION UI — TESTING GUIDE

**Дата:** 27 ноября 2025  
**Версия:** 1.0  
**Статус:** MVP Реализация

---

## 🎯 ЦЕЛЬ ТЕСТИРОВАНИЯ

Проверить работу новой вкладки **📚 Library** с функциями:
- Запуск индексации новых документов
- Отображение статуса индексации
- Визуальная связь с активным agent profile
- Корректная обработка ошибок через систему уведомлений (Task 6)

---

## 🛠️ ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### Сервисы
✅ **Ollama** — запущен на `http://localhost:11434`  
✅ **CORTEX (LightRAG)** — запущен на `http://localhost:8004`  
✅ **Tauri Desktop Client** — собран и запущен (`npm run tauri dev`)

### Файлы
✅ **Скрипт индексации:** `E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1` существует  
✅ **Data Tray:** `E:\WORLD_OLLAMA\workbench\sandbox_main\inputs\data_tray\` существует  
✅ **Library:** `E:\WORLD_OLLAMA\library\raw_documents\` существует

### Статус файл
📁 `%APPDATA%\tauri_fresh\indexation_status.json` — создаётся автоматически

---

## 📊 ТЕСТОВЫЕ СЦЕНАРИИ

### 🟢 СЦЕНАРИЙ 1: Happy Path (нормальная работа)

**Цель:** Проверить успешный запуск индексации

**Предусловия:**
- Все сервисы запущены
- В Data Tray есть хотя бы 1 новый файл (например, `test_document.txt`)
- Desktop Client открыт

**Шаги:**

1. **Открыть вкладку Library**
   ```
   Desktop Client → нажать "📚 Library"
   ```

2. **Проверить начальный статус**
   - **Ожидается:**
     - Текущее состояние: `⏸️ Ожидание`
     - Последний запуск: либо дата предыдущего запуска, либо "Никогда"
     - Кнопка "▶️ Запустить индексацию" активна

3. **Нажать "Запустить индексацию"**
   ```
   Клик на кнопку "▶️ Запустить индексацию"
   ```

4. **Проверить Toast уведомление**
   - **Ожидается:**
     - В правом верхнем углу появился toast с типом `info` (синяя граница)
     - Текст: "Индексация запущена"
     - Details: "Старт: [дата и время]"
     - Toast исчезает через 6 секунд

5. **Проверить обновление статуса**
   - **Ожидается:**
     - Текущее состояние: `⚙️ Выполняется` (синий badge)
     - Последний запуск: обновлён на текущее время
     - Кнопка изменилась на "⚙️ Индексация уже идёт…" (disabled)

6. **Подождать 10-15 секунд** (время работы скрипта)
   - **Ожидается:**
     - Статус автоматически обновляется каждые 10 секунд
     - После завершения скрипта состояние вернётся в `⏸️ Ожидание`

7. **Проверить Agent Profile**
   - **Ожидается:**
     - Внизу страницы блок "🤖 Активный профиль агента"
     - Показан активный профиль (например, "qwen2-main")
     - Hint текст про использование индексированных данных

**Результат:** ✅ Пройден / ❌ Провален

**Скриншоты:**
- [ ] Library вкладка (начальный статус)
- [ ] Toast уведомление "Индексация запущена"
- [ ] Статус "Выполняется"
- [ ] Agent Profile блок

---

### 🔴 СЦЕНАРИЙ 2: Скрипт индексации отсутствует

**Цель:** Проверить обработку ошибки отсутствия скрипта

**Предусловия:**
- Временно переименовать скрипт:
  ```powershell
  Rename-Item E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1 `
               E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1.bak
  ```

**Шаги:**

1. **Открыть Library вкладку**
2. **Нажать "Запустить индексацию"**

**Ожидается:**

- ❌ **Error Toast** (красная граница):
  - Тип: `error`
  - Сообщение: "Ошибка: start_indexation"
  - Details: "Скрипт индексации не найден: E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1"
  - Timeout: 8 секунд

- 📊 **Обновление статуса:**
  - Текущее состояние: `❌ Ошибка` (красный badge)
  - Последняя ошибка: красный блок с текстом ошибки
  - Кнопка остаётся активной (можно попробовать снова)

**Восстановление:**
```powershell
Rename-Item E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1.bak `
             E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1
```

**Результат:** ✅ Пройден / ❌ Провален

**Скриншоты:**
- [ ] Error Toast
- [ ] Статус "Ошибка" с деталями

---

### 🟡 СЦЕНАРИЙ 3: Повторный запуск при выполнении

**Цель:** Проверить защиту от двойного запуска

**Предусловия:**
- Скрипт существует
- Индексация уже запущена (статус "running")

**Шаги:**

1. **Запустить индексацию** (Сценарий 1, шаг 3)
2. **Сразу же нажать кнопку повторно**

**Ожидается:**

- ⚠️ **Error Toast**:
  - Тип: `error`
  - Сообщение: "Ошибка: start_indexation"
  - Details: "Индексация уже выполняется"

- 🔒 **Кнопка disabled:**
  - Текст: "⚙️ Индексация уже идёт…"
  - Невозможно нажать

**Результат:** ✅ Пройден / ❌ Провален

---

### 🔄 СЦЕНАРИЙ 4: Перезапуск приложения (персистентность)

**Цель:** Проверить сохранение статуса после закрытия

**Шаги:**

1. **Запустить индексацию** (Сценарий 1)
2. **Подождать 5 секунд** (статус "running")
3. **Закрыть Tauri приложение** (Alt+F4 или крестик)
4. **Запустить снова:**
   ```powershell
   cd E:\WORLD_OLLAMA\client
   npm run tauri dev
   ```
5. **Открыть Library вкладку**

**Ожидается:**

- 📂 **Статус файл существует:**
  ```powershell
  Test-Path "$env:APPDATA\tauri_fresh\indexation_status.json"
  # True
  
  Get-Content "$env:APPDATA\tauri_fresh\indexation_status.json"
  # JSON с state, last_run
  ```

- 📊 **UI показывает сохранённый статус:**
  - Последний запуск: дата из файла
  - Текущее состояние: либо "running", либо "idle" (зависит от завершения скрипта)

**Результат:** ✅ Пройден / ❌ Провален

**Скриншоты:**
- [ ] Содержимое `indexation_status.json`
- [ ] Library UI после перезапуска

---

### 🧪 СЦЕНАРИЙ 5: Интеграция с Task 6 (Error Handling)

**Цель:** Убедиться, что все ошибки показывают toast через NotificationCenter

**Предусловия:**
- NotificationCenter работает (Task 6 пройден)

**Тесты:**

1. **Error от Rust команды** (Сценарий 2) → Toast ❌
2. **Success notification** (Сценарий 1, шаг 4) → Toast ℹ️
3. **Двойной запуск** (Сценарий 3) → Toast ⚠️

**Ожидается:**
- ✅ Все уведомления появляются в правом верхнем углу
- ✅ Цвета border соответствуют типу (error: red, info: blue)
- ✅ Можно закрыть крестиком
- ✅ Auto-dismiss работает (4-8 секунд)

**Результат:** ✅ Пройден / ❌ Провален

---

### 🎯 СЦЕНАРИЙ 6: Agent Profile отображение

**Цель:** Проверить визуальную связь с настройками

**Шаги:**

1. **Открыть Settings вкладку**
2. **Выбрать Agent Profile:** например, "helper-lite"
3. **Сохранить настройки**
4. **Переключиться на Library вкладку**

**Ожидается:**

- 🤖 **Блок Agent Profile:**
  - Иконка: 🎯
  - Название: "helper-lite" (обновлено)
  - Hint: текст про использование данных

- 🔄 **Без перезагрузки страницы** (реактивность)

**Результат:** ✅ Пройден / ❌ Провален

**Скриншоты:**
- [ ] Settings с выбором "helper-lite"
- [ ] Library с обновлённым профилем

---

## 🐛 ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ MVP

### В текущей версии НЕ реализовано:

1. ⏸️ **Остановка индексации**
   - Нельзя прервать процесс через UI
   - Нужно закрыть PowerShell окно вручную

2. ⏸️ **Прогресс-бар**
   - Нет визуального индикатора процента выполнения
   - Только статусы: idle / running / error

3. ⏸️ **Список документов**
   - Placeholder вместо реального списка
   - Нельзя увидеть, что было проиндексировано

4. ⏸️ **Детализация ошибок**
   - Нет логов скрипта в UI
   - Ошибки видны только в toast и статусе

### Это будет добавлено в будущих версиях

---

## 📝 ЧЕКЛИСТ ФИНАЛЬНОЙ ПРИЁМКИ

### Функциональность
- [ ] Кнопка "Запустить индексацию" работает
- [ ] Статус обновляется автоматически (каждые 10 сек)
- [ ] Error handling через Task 6 notifications
- [ ] Agent Profile отображается корректно
- [ ] Персистентность статуса (переживает перезапуск)

### UI/UX
- [ ] Навигация: вкладка "📚 Library" доступна
- [ ] Цветовая индикация статусов (синий/красный/серый)
- [ ] Disabled state кнопки при running
- [ ] Toast уведомления появляются и исчезают
- [ ] Responsive layout (не ломается при resize)

### Интеграция
- [ ] Rust команды `start_indexation` / `get_indexation_status` работают
- [ ] PowerShell скрипт запускается через Command::spawn
- [ ] Статус файл создаётся в `%APPDATA%\tauri_fresh\`
- [ ] apiClient методы используются во всех компонентах

### Документация
- [ ] TASK_7_COMPLETION_REPORT.md создан
- [ ] TASK_7_TESTING_GUIDE.md (этот файл) проверен
- [ ] Скриншоты всех сценариев прикреплены

---

## 🚀 ЗАПУСК ТЕСТИРОВАНИЯ

### Быстрый старт:

```powershell
# 1. Убедиться, что сервисы запущены
pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1

# 2. Запустить Tauri Desktop Client
cd E:\WORLD_OLLAMA\client
npm run tauri dev

# 3. Открыть Library вкладку
# → Выполнить Сценарии 1-6 из этого документа

# 4. Собрать скриншоты и результаты
```

### Проверка статуса файла:

```powershell
# Путь к статусу
$statusPath = "$env:APPDATA\tauri_fresh\indexation_status.json"

# Посмотреть содержимое
if (Test-Path $statusPath) {
    Get-Content $statusPath | ConvertFrom-Json | Format-List
} else {
    Write-Host "Статус файл ещё не создан"
}

# Удалить статус (для чистого теста)
Remove-Item $statusPath -ErrorAction SilentlyContinue
```

---

## 📊 ФОРМАТ ОТЧЁТА О ТЕСТИРОВАНИИ

После прохождения всех сценариев заполнить:

```markdown
# TASK 7 Testing Results

**Дата:** [дата]  
**Тестировщик:** [имя]  
**Версия:** MVP 1.0

## Результаты

| Сценарий | Статус | Комментарий |
|----------|--------|-------------|
| 1. Happy Path | ✅/❌ | ... |
| 2. Скрипт отсутствует | ✅/❌ | ... |
| 3. Двойной запуск | ✅/❌ | ... |
| 4. Перезапуск приложения | ✅/❌ | ... |
| 5. Task 6 Integration | ✅/❌ | ... |
| 6. Agent Profile | ✅/❌ | ... |

## Найденные баги

1. [описание]
2. ...

## Скриншоты

- scenario1_start.png
- scenario2_error.png
- ...
```

---

**Статус документа:** ✅ Готов к использованию  
**Версия:** 1.0 (27.11.2025)
