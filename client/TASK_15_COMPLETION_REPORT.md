# TASK 15: TRIZ-AGENT ДЛЯ OLLAMA (ОБУЧЕННЫЙ LLM + RAG)

> Черновой отчёт. Будет заполняться по мере выполнения подпунктов 15.1–15.5.

## 15.1 TRIZ Dataset Builder

_Status: DONE (mini-run + full-run TRIZ corpus)_

### Mini-run (pipeline verification)

- Скрипт: `services/training/build_triz_dataset.py`
- Источник: `library/raw_documents/*.txt` (TRIZ-документы)
- Режим: **mini-run** (ограниченный поднабор для проверки пайплайна)

Фактические метрики mini-run:

- Количество обработанных файлов: **30**
- Количество сгенерированных примеров (строк JSONL): **647**
- Итоговый файл датасета: `E:\WORLD_OLLAMA\services\training\data\triz_dataset.jsonl`
- Размер файла: **1 803 977 байт (~1.72 MB)**

Примеры реальных записей из `triz_dataset.jsonl` (первые строки):

Пример 1:
instruction: Сделай сжатое объяснение ключевой идеи из этого текста.
input: ""
output: Архитектоника декомпозиции: Исчерпывающий анализ эволюции технических и когнитивных систем через призму Принципа ТРИЗ №1 «Дробление» и Принципа №5 «Объединение»\n\nАннотация

Пример 2:
instruction: Сформулируй кратко основной смысл этого фрагмента ТРИЗ.
input: ""
output: В современной инженерной и научной парадигме наблюдается фундаментальный сдвиг от монолитных, жестко интегрированных структур к распределенным, гранулярным и адаптивным системам. Этот переход, прослеживаемый от проектирования космических аппаратов до архитектуры больших языковых моделей (LLM), является не случайным набором технологических трендов, а проявлением объективных законов развития технических систем, сформулированных в Теории решения изобретательских задач (ТРИЗ). Данный отчет представляет собой всестороннее исследование применения Принципа ТРИЗ №1 «Дробление» (Segmentation) в критически важных доменах: аэрокосмической отрасли, искусственном интеллекте и системном анализе.\nИсследование базируется на анализе обширного корпуса технической документации, патентов и научных публикаций. Мы рассматриваем дробление не как изолированный акт разделения, а как диалектический процесс, неразрывно связанный с Принципом №5 «Объединение» (Merging). В отчете подробно анализируются механизмы фракционирования космических архитектур (программа DARPA F6), концепции цифровых материалов (NASA MADCAT), архитектуры Mixture of Experts (MoE) в нейронных сетях и агентные рои. Особое внимание уделяется экономическим и теоретическим границам дробления, определяемым теорией транзакционных издержек и законом оптимальной гранулярности.

Пример 3:
instruction: Сформулируй кратко основной смысл этого фрагмента ТРИЗ.
input: ""
output: Теория решения изобретательских задач (ТРИЗ), зародившаяся в 1940-х годах в Советском Союзе благодаря трудам Генриха Альтшуллера, представляет собой уникальную эпистемологическую систему. В отличие от западных методик мозгового штурма, опирающихся на психологическую инерцию и случайный перебор, ТРИЗ постулирует наличие объективных законов развития технических систем.1 Альтшуллер, проанализировав массив патентного фонда, выявил, что наиболее сильные изобретения возникают не в результате "озарения", а через разрешение фундаментальных противоречий с использованием ограниченного набора эвристических приемов.\nСреди 40 выявленных принципов Принцип №1 «Дробление» (Segmentation) занимает стартовую позицию не случайно. Он символизирует первый шаг в борьбе со сложностью монолита. В своей сущности этот принцип отражает стремление материи к увеличению степеней свободы. Если рассматривать эволюцию любой технической системы — от каменного топора до микропроцессора — мы неизменно обнаруживаем вектор движения от цельного куска материи к составному, затем к гибкому, и, наконец, к полевому взаимодействию.

Mini-run выводы:

- Пайплайн «TRIZ-тексты → чанки → JSONL-инструкции» работает стабильно на реальном подмножестве библиотеки.
- Формат выходных данных (`{"instruction", "input", "output"}`) совместим с типичным форматом инструктивного обучения в LLaMA Factory / аналогичных фреймворках.
- В выборке из первых строк не обнаружено технического мусора (служебные маркеры разметки, пути файлов, явно битые фрагменты); текст осмысленный и связный, что достаточно для перехода к подготовке full-run и шагу 15.2.

### Full-run (TRIZ full corpus)

- Скрипт: `services/training/build_triz_dataset.py`
- Режим: **full-run** (`--mode full`)

Фактические метрики full-run:

- Количество обработанных файлов: **179**
- Количество сгенерированных примеров (строк JSONL): **3448**
- Итоговый файл датасета: `E:\WORLD_OLLAMA\services\training\data\triz_dataset.jsonl`
- Размер файла после full-run: **8 539 988 байт (~8.15 MB)**

Особенности full-run:

- Датасет теперь покрывает практически весь доступный TRIZ-корпус в `library/raw_documents` (179 исходных файлов вместо 30 в mini-run), сохраняя тот же формат и структуру.
- Случайная выборка 5–10 строк из разных частей файла показывает стабильный паттерн: человекочитаемые `instruction`, пустой `input` (для чистых инструкций) и длинные связные фрагменты в `output` без очевидного «мусора» (служебные заголовки, пути, HTML-артефакты и т.п.).
- Выявленных критических артефактов, требующих немедленной фильтрации перед обучением, нет; возможную дальнейшую нормализацию (например, агрессивное удаление длинных списков источников/сносок) можно оформить как технический долг после MVP.

## 15.2 LLaMA Factory Integration

_Status: IN PROGRESS_

**Infrastructure Note (28.11.2025):**

CORTEX (LightRAG) для TASK 15 работает в режиме **baseline без rerank** по следующим параметрам:
- LLM Model: `qwen2.5:14b` (production-verified, совпадает с моделью обучения в 15.2)
- Embeddings: `nomic-embed-text:latest`
- Rerank: Disabled (baseline stable mode, enable_rerank=False)
- Bind: `127.0.0.1:8004` (local-first Windows architecture)
- Search: `local → global → naive` (hybrid excluded due to LightRAG v1.4.9.8 instability)

Данная конфигурация обеспечивает 100% uptime для RAG-поиска во время обучения и тестирования агента.

### 15.2.1 Existing LLaMA Factory Setup

- Каталог LLaMA Factory: `E:\WORLD_OLLAMA\services\llama_factory`
- Entry point для обучения: `src/train.py`, функция `main()` вызывает `llamafactory.train.tuner.run_exp()`
- Конфиги обучения TRIZ (существующие примеры):
  - `E:\WORLD_OLLAMA\services\llama_factory\triz_safe_config.yaml` (LoRA, Qwen/Qwen2-7B-Instruct, датасет `triz_synthesis_v1`)
  - `E:\WORLD_OLLAMA\services\llama_factory\triz_full_config_qwen15b.yaml` (LoRA, Qwen/Qwen2.5-1.5B-Instruct, тот же датасет)
- Venv: отдельный каталог `venv` или `.venv` внутри `services/llama_factory` в текущем дереве не обнаружен; запуск LLaMA Factory предполагается через уже существующую venv верхнего уровня проекта или будет оформлен отдельным шагом при интеграции `start_agent_training.ps1`.

### 15.2.2 Training Config: `triz_td010v3_full.yaml`

_Status: DONE_

**Файл:** `E:\WORLD_OLLAMA\services\llama_factory\triz_td010v3_full.yaml`

**Ключевые параметры:**

- **Базовая модель:**
  - `model_name_or_path: Qwen/Qwen2.5-14B-Instruct` (HuggingFace ID для Qwen 2.5, 14B instruct-версия)

- **Метод обучения:**
  - `stage: sft` (Supervised Fine-Tuning)
  - `finetuning_type: lora` (Low-Rank Adaptation, НЕ full finetune)
  - `lora_target: all` (применяем LoRA ко всем слоям)
  - `lora_rank: 64`, `lora_alpha: 16`, `lora_dropout: 0.05`
  - `quantization_bit: 8` (квантизация для экономии VRAM)

- **Датасет:**
  - `data_path: E:/WORLD_OLLAMA/services/training/data/triz_dataset.jsonl` (прямой путь к файлу из 15.1)
  - `dataset_format: alpaca` (формат instruction/input/output)
  - `template: qwen` (токенизация для Qwen-моделей)
  - `cutoff_len: 2048` (макс. длина последовательности)
  - `max_samples: 0` (использовать весь датасет — 3448 примеров)
  - `preprocessing_num_workers: 1` (безопасно для Windows)

- **Выходные артефакты:**
  - `output_dir: E:/WORLD_OLLAMA/services/llama_factory/outputs/triz_td010v3`
  - `logging_steps: 10`, `save_steps: 100`, `warmup_steps: 20`
  - `overwrite_output_dir: true`

- **Параметры обучения:**
  - `per_device_train_batch_size: 1`
  - `gradient_accumulation_steps: 4` (эффективный batch = 4)
  - `learning_rate: 2.0e-5` (консервативное значение)
  - `num_train_epochs: 1.0` (одна эпоха для боевого режима; для smoke-test будет уменьшено)
  - `lr_scheduler_type: cosine`
  - `fp16: true` (mixed precision)

- **Evaluation:**
  - `val_size: 0.05` (5% датасета для валидации)
  - `eval_strategy: steps`, `eval_steps: 100`

**Примечания:**
- Для mini-run (15.2.5) параметры `num_train_epochs` и/или `max_samples` будут временно уменьшены для быстрого smoke-test.
- Конфиг ориентирован на RTX 5060 Ti 16GB с квантизацией 8bit для VRAM-эффективности.

### 15.2.3 PowerShell Integration (`start_agent_training.ps1`)

_Status: DONE_

**Файл:** `E:\WORLD_OLLAMA\scripts\start_agent_training.ps1`

**Изменения:**

1. **Добавлена ветка профиля `triz_td010v3_full`:**
   ```powershell
   switch ($Profile) {
       "triz_td010v3_full" {
           $ConfigPath = "$llamaFactoryPath\triz_td010v3_full.yaml"
           $OutputDir  = "$llamaFactoryPath\outputs\triz_td010v3"
       }
       "triz_engineer" { ... }  # Legacy profile
       default { exit 1 }
   }
   ```

2. **Реальный запуск LLaMA Factory:**
   - Заменён MVP-stub на фактический вызов `llamafactory-cli train <config>`
   - Запуск в отдельном окне PowerShell через `Start-Process` (не блокирует UI)
   - Активация venv перед запуском: `. .\venv\Scripts\Activate.ps1`
   - Команда: `llamafactory-cli train "E:\WORLD_OLLAMA\services\llama_factory\triz_td010v3_full.yaml"`

3. **Валидация:**
   - Проверка существования конфига перед запуском
   - При неизвестном профиле возвращается ошибка с перечнем допустимых профилей

### 15.2.4 Rust Whitelist (`commands.rs`)

_Status: DONE_

**Файл:** `E:\WORLD_OLLAMA\client\src-tauri\src\commands.rs`

**Изменения:**

Добавлен профиль `"triz_td010v3_full"` в список допустимых профилей (строка ~547):
```rust
let valid_profiles = [
    "triz_engineer", 
    "triz_researcher", 
    "triz_td010v3_full",  // 15.2.4: New profile for full TRIZ dataset
    "default"
];
```

**Интеграционная схема:**
1. Tauri UI (TrainingPanel.svelte) → вызывает команду `execute_agent_command` с `TRAIN AGENT PROFILE="triz_td010v3_full" ...`
2. Rust backend (commands.rs) → валидирует профиль по whitelist → вызывает `start_training_job`
3. PowerShell (start_agent_training.ps1) → маппит профиль на конфиг → запускает `llamafactory-cli train`
4. LLaMA Factory (src/train.py) → читает `triz_td010v3_full.yaml` → обучает модель на `triz_dataset.jsonl`

### 15.2.5 Mini-Run Smoke Test

_Status: READY FOR EXECUTION_

**Подготовка:**

Создан отдельный smoke-test конфиг для безопасной проверки пайплайна без длительного обучения:

**Файл:** `E:\WORLD_OLLAMA\services\llama_factory\triz_td010v3_smoketest.yaml`

**Smoke-test параметры:**
- `max_samples: 100` — только первые 100 примеров из датасета
- `num_train_epochs: 0.1` — 10% одной эпохи
- `max_steps: 50` — максимум 50 шагов обучения
- `save_steps: 10` — частое сохранение для проверки артефактов
- `logging_steps: 5` — детальное логирование
- `output_dir: outputs/triz_td010v3_smoketest` — отдельный выходной каталог

**Smoke-test профиль добавлен:**
- PowerShell: `"triz_td010v3_smoketest"` в `start_agent_training.ps1`
- Rust: `"triz_td010v3_smoketest"` в whitelist `commands.rs`

**Инструкции для запуска smoke-test:**

1. **Запустить сервисы (без Neuro-Terminal):**
   ```powershell
   pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1 -SkipNeuroTerminal
   ```

2. **Запустить Desktop UI:**
   ```powershell
   cd E:\WORLD_OLLAMA\client
   npm run tauri dev
   ```

3. **В Desktop приложении:**
   - Перейти на вкладку **🧪 Training**
   - В поле команды ввести:
     ```
     TRAIN AGENT PROFILE="triz_td010v3_smoketest" DATA_PATH="E:\WORLD_OLLAMA\services\training\data" EPOCHS=1
     ```
   - Нажать **Execute**

4. **Проверить выполнение:**
   - **TrainingPanel статусы:** `idle` → `queued` → `running` → `done` (или `error`)
   - **Артефакты:** проверить наличие файлов в `E:\WORLD_OLLAMA\services\llama_factory\outputs\triz_td010v3_smoketest\`
   - **Логи:** окно PowerShell с процессом обучения (открывается автоматически)
   - **Время выполнения:** ожидаемое ~5-15 минут (зависит от GPU)

5. **После успешного smoke-test:**
   - Для полного обучения использовать профиль `triz_td010v3_full` (вместо `smoketest`)
   - Проверить VRAM-потребление (должно быть < 14-15 GB на RTX 5060 Ti 16GB)

**Критерии успеха smoke-test:**
- ✅ Статус меняется с `idle` → `queued` → `running` → `done`
- ✅ Появляются файлы в `outputs/triz_td010v3_smoketest/`:
  - `checkpoint-*` (LoRA адаптеры)
  - `trainer_state.json` (состояние обучения)
  - `training_log.jsonl` (лог метрик)
- ✅ Процесс завершается без фатальных ошибок
- ✅ В логах видна загрузка `triz_dataset.jsonl` (100 примеров) и прогресс шагов

**Результаты mini-run будут задокументированы здесь после выполнения.**

---

## Критерии завершения TASK 15.2

✅ **15.2.1** — LLaMA Factory setup зафиксирован в отчёте  
✅ **15.2.2** — Конфиг `triz_td010v3_full.yaml` создан и задокументирован  
✅ **15.2.3** — Профиль интегрирован в `start_agent_training.ps1` с реальным запуском LLaMA Factory  
✅ **15.2.4** — Профиль добавлен в Rust whitelist (`commands.rs`)  
✅ **15.2.5** — Smoke-test конфиг создан, инструкции подготовлены

**TASK 15.2 готова к исполнению smoke-test.**  
После успешного smoke-test можно переходить к полному обучению и дальнейшим этапам (15.3–15.5).

## 15.3 Ollama TRIZ Model

_Status: TODO_

- План: подготовить Modelfile и создать модель `triz-td010v3` в Ollama.
- Здесь будут зафиксированы:
  - команда создания модели,
  - проверка через `ollama list` и пример ответа на контрольный вопрос.

## 15.4 Agent Profiles & UI

_Status: TODO_

- План: связать TRIZ-модель с профилем (например, `triz_engineer`) и Chat UI.
- Здесь будут:
  - описание привязки профиля к `ollama_model` и `use_rag`,
  - краткое описание UX в Training/Settings/Chat.

## 15.5 A/B-тесты (до/после)

_Status: TODO_

- План: сравнить ответы до/после обучения на вопросах про Альтшуллера и приёмы 3, 11, 25.
- Здесь будут представлены пары ответов (старый/новый профиль) и краткий вывод о качестве.
