# TASK 9: TESTING GUIDE

**Дата создания:** 27.11.2025  
**Версия:** v1.0  
**Цель:** Проверить реальное выполнение команд INDEX, TRAIN, GIT через Desktop Client

---

## 🎯 Цель тестирования

Убедиться, что:
1. ✅ Команды выполняют **реальные действия** (не просто логирование)
2. ✅ Валидация параметров работает корректно (отсекает невалидные значения)
3. ✅ Безопасность сохранена (whitelist paths, dry-run для GIT)
4. ✅ Фоновое выполнение не блокирует UI
5. ✅ Ошибки обрабатываются и отображаются в CommandSlot
6. ✅ Статусы обновляются в JSON файлах

---

## 🛠️ Подготовка к тестированию

### Шаг 1: Убедитесь в готовности окружения

**Проверьте наличие файлов:**

```powershell
# 1. PowerShell скрипт для обучения
Test-Path "E:\WORLD_OLLAMA\scripts\start_agent_training.ps1"
# Expected: True

# 2. PowerShell скрипт для индексации
Test-Path "E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1"
# Expected: True

# 3. Директория для статусов
Test-Path "$env:APPDATA\tauri_fresh"
# Expected: True (или создайте: New-Item -ItemType Directory -Force)

# 4. Git доступен
git --version
# Expected: git version 2.x.x
```

**Убедитесь в компиляции:**

```powershell
cd E:\WORLD_OLLAMA\client\src-tauri
cargo check
# Expected: Finished `dev` profile [unoptimized + debuginfo] target(s) in X.XXs
```

### Шаг 2: Запустите Desktop Client

```powershell
cd E:\WORLD_OLLAMA\client
npm run tauri dev
```

**Ожидается:**
- Окно приложения открывается
- UI отображается корректно
- Вкладки: 🏠 Home, 💬 Chat, 🔧 Commands доступны

### Шаг 3: Откройте Developer Tools (опционально)

**Windows:** `Ctrl + Shift + I`

**Для просмотра:**
- Console logs (ошибки JavaScript)
- Network requests (если будут API calls)

---

## 🧪 Тестовые сценарии

### SCENARIO 1: INDEX KNOWLEDGE — Real Indexation

**Цель:** Проверить, что индексация реально запускается (не stub)

**Шаги:**

1. **Перейдите на вкладку 💬 Chat**

2. **Введите команду:**
   ```
   INDEX KNOWLEDGE
   PATH: E:\WORLD_OLLAMA\library\raw_documents
   MODE: hybrid
   PROFILE: default
   ```

3. **Нажмите Send**

**Ожидается в Chat:**
- ИИ распознаёт команду
- Отображается кнопка "Execute INDEX KNOWLEDGE" в CommandSlot

4. **Нажмите "Execute" в CommandSlot**

**Ожидается:**
- ✅ CommandSlot показывает:
  ```
  ✅ Индексация запущена!
  
  Параметры:
  PATH: E:\WORLD_OLLAMA\library\raw_documents
  MODE: hybrid
  PROFILE: default
  
  Время старта: 2025-11-27T21:30:00Z
  
  Статус можно отслеживать на вкладке 📚 Library.
  ```

5. **Проверьте фоновый процесс:**

```powershell
# Проверьте статус индексации
Get-Content "$env:APPDATA\tauri_fresh\indexation_status.json" | ConvertFrom-Json

# Expected output:
# {
#   "state": "running",
#   "last_run": "2025-11-27T21:30:00Z",
#   ...
# }
```

6. **Проверьте PowerShell процесс:**

```powershell
Get-Process -Name powershell | Where-Object {$_.MainWindowTitle -like "*ingest_watcher*"}
# Expected: Должен быть активный процесс
```

**Критерий успеха:**
- ✅ Сообщение "Индексация запущена" появилось
- ✅ Статус в JSON = "running"
- ✅ PowerShell процесс запущен

---

### SCENARIO 2: TRAIN AGENT — Valid Parameters

**Цель:** Проверить успешный запуск обучения с валидными параметрами

**Шаги:**

1. **Создайте тестовую директорию с данными:**

```powershell
# Создайте fake DATA_PATH для теста (или используйте реальные данные)
$testPath = "E:\WORLD_OLLAMA\test_training_data"
if (-not (Test-Path $testPath)) {
    New-Item -ItemType Directory -Path $testPath -Force
    # Скопируйте несколько файлов для теста (опционально)
    Copy-Item "E:\WORLD_OLLAMA\library\raw_documents\*.txt" $testPath -Force
}
```

2. **Введите команду в Chat:**
   ```
   TRAIN AGENT
   PROFILE: default
   DATA_PATH: E:\WORLD_OLLAMA\test_training_data
   EPOCHS: 3
   MODE: llama_factory
   ```

3. **Нажмите "Execute" в CommandSlot**

**Ожидается:**
- ✅ Сообщение:
  ```
  ✅ Обучение профиля default запущено!
  
  📋 Параметры:
  • DATA_PATH: E:\WORLD_OLLAMA\test_training_data
  • EPOCHS: 3
  • MODE: llama_factory
  
  🆔 Job ID: train-20251127-213000
  
  ⚠️ Обучение выполняется в фоне. Статус отслеживается в `training_status.json`.
  
  💡 Для мониторинга используйте команду `STATUS: TRAINING` или вкладку 🔧 Commands.
  ```

4. **Проверьте статус обучения:**

```powershell
Get-Content "$env:APPDATA\tauri_fresh\training_status.json" | ConvertFrom-Json

# Expected output:
# {
#   "state": "queued",
#   "profile": "default",
#   "data_path": "E:\\WORLD_OLLAMA\\test_training_data",
#   "epochs": 3,
#   "started_at": "2025-11-27T21:30:00Z",
#   "last_error": null
# }
```

5. **Проверьте PowerShell процесс:**

```powershell
Get-Process -Name powershell | Where-Object {$_.CommandLine -like "*start_agent_training*"}
# Expected: Должен быть активный процесс
```

**Критерий успеха:**
- ✅ Сообщение "Обучение запущено" появилось
- ✅ Статус в JSON = "queued"
- ✅ PowerShell процесс запущен
- ✅ Параметры сохранены корректно (profile, data_path, epochs)

---

### SCENARIO 3: TRAIN AGENT — Invalid DATA_PATH

**Цель:** Проверить валидацию DATA_PATH (несуществующий путь)

**Шаги:**

1. **Введите команду с несуществующим путём:**
   ```
   TRAIN AGENT
   PROFILE: default
   DATA_PATH: E:\NONEXISTENT_PATH_12345
   EPOCHS: 3
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ❌ Ошибка в CommandSlot:
  ```
  ❌ DATA_PATH не существует:
  E:\NONEXISTENT_PATH_12345
  ```

- Статус обучения **не изменился** (всё ещё "idle" или предыдущее состояние)

**Критерий успеха:**
- ✅ Ошибка валидации показана в CommandSlot
- ✅ PowerShell скрипт **НЕ запущен**
- ✅ Статус в JSON **НЕ изменился**

---

### SCENARIO 4: TRAIN AGENT — Invalid EPOCHS Range

**Цель:** Проверить валидацию EPOCHS (вне диапазона 1-5)

**Шаги:**

1. **Введите команду с EPOCHS=100:**
   ```
   TRAIN AGENT
   PROFILE: default
   DATA_PATH: E:\WORLD_OLLAMA\test_training_data
   EPOCHS: 100
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ❌ Ошибка:
  ```
  ❌ EPOCHS должен быть в диапазоне 1-5
  ```

**Критерий успеха:**
- ✅ Ошибка валидации показана
- ✅ Обучение **НЕ запущено**

---

### SCENARIO 5: TRAIN AGENT — Invalid PROFILE

**Цель:** Проверить whitelist профилей

**Шаги:**

1. **Введите команду с невалидным профилем:**
   ```
   TRAIN AGENT
   PROFILE: hacker_profile
   DATA_PATH: E:\WORLD_OLLAMA\test_training_data
   EPOCHS: 3
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ❌ Ошибка:
  ```
  ❌ Недопустимый PROFILE: hacker_profile
  Допустимые значения: ["triz_engineer", "triz_researcher", "default"]
  ```

**Критерий успеха:**
- ✅ Ошибка валидации показана с перечислением допустимых профилей
- ✅ Обучение **НЕ запущено**

---

### SCENARIO 6: GIT PUSH — Dry-Run with Changes

**Цель:** Проверить dry-run git status (с изменениями в репозитории)

**Подготовка:**

```powershell
# Создайте тестовый файл в репозитории
cd E:\WORLD_OLLAMA
New-Item -ItemType File -Path "test_file_for_git.txt" -Value "Test content" -Force
```

**Шаги:**

1. **Введите команду:**
   ```
   GIT PUSH
   REPO_PATH: E:\WORLD_OLLAMA
   BRANCH: main
   SUMMARY: Test commit message
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ✅ Сообщение с перечислением изменённых файлов:
  ```
  ✅ Git dry-run выполнен.
  
  📁 Изменённые файлы (X):
    ?? test_file_for_git.txt
    M client/src-tauri/src/commands.rs
    ... (остальные файлы)
  
  📋 Параметры:
  • РЕПОЗИТОРИЙ: E:\WORLD_OLLAMA
  • ВЕТКА: main
  • СООБЩЕНИЕ: Test commit message
  
  ⚠️ Реальный push не производится (безопасный режим).
  
  💡 Для реального коммита используйте `git` в терминале.
  ```

**Критерий успеха:**
- ✅ Список изменённых файлов показан
- ✅ Предупреждение "Реальный push не производится" присутствует
- ✅ Реальный `git push` **НЕ выполнен** (проверьте: `git log origin/main` не изменился)

---

### SCENARIO 7: GIT PUSH — Dry-Run without Changes

**Цель:** Проверить поведение при отсутствии изменений

**Подготовка:**

```powershell
cd E:\WORLD_OLLAMA
git add test_file_for_git.txt
git commit -m "Test commit"
# Теперь рабочая директория чистая
```

**Шаги:**

1. **Введите команду:**
   ```
   GIT PUSH
   REPO_PATH: E:\WORLD_OLLAMA
   BRANCH: main
   SUMMARY: Nothing to commit
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ✅ Сообщение:
  ```
  ✅ Git dry-run выполнен.
  
  📁 Репозиторий: E:\WORLD_OLLAMA
  🌿 Ветка: main
  
  ℹ️ **Нет изменённых файлов.**
  
  ⚠️ Реальный push не производится (безопасный режим).
  ```

**Критерий успеха:**
- ✅ Сообщение "Нет изменённых файлов" показано
- ✅ Предупреждение про dry-run присутствует

---

### SCENARIO 8: GIT PUSH — Invalid REPO_PATH (Security Check)

**Цель:** Проверить whitelist проверку REPO_PATH

**Шаги:**

1. **Введите команду с невалидным путём:**
   ```
   GIT PUSH
   REPO_PATH: C:\Windows\System32
   BRANCH: main
   SUMMARY: Attempt to push outside whitelist
   ```

2. **Нажмите "Execute"**

**Ожидается:**
- ❌ Ошибка безопасности:
  ```
  ❌ REPO_PATH должен начинаться с E:\WORLD_OLLAMA (безопасность)
  
  Получено: C:\Windows\System32
  ```

**Критерий успеха:**
- ✅ Ошибка безопасности показана
- ✅ `git status` **НЕ выполнен** (безопасность)

---

## 📊 Итоговая таблица тестирования

| Scenario | Команда | Ожидаемый результат | Статус |
|----------|---------|---------------------|--------|
| 1 | INDEX KNOWLEDGE (valid) | Индексация запущена, статус "running" | ⬜ |
| 2 | TRAIN AGENT (valid) | Обучение запущено, статус "queued" | ⬜ |
| 3 | TRAIN AGENT (invalid DATA_PATH) | Ошибка валидации | ⬜ |
| 4 | TRAIN AGENT (invalid EPOCHS) | Ошибка валидации "1-5" | ⬜ |
| 5 | TRAIN AGENT (invalid PROFILE) | Ошибка с whitelist | ⬜ |
| 6 | GIT PUSH (with changes) | Список файлов, dry-run warning | ⬜ |
| 7 | GIT PUSH (no changes) | "Нет изменений", dry-run warning | ⬜ |
| 8 | GIT PUSH (invalid path) | Ошибка безопасности | ⬜ |

**Заполните ✅ после успешного выполнения каждого сценария**

---

## 🐛 Что делать при ошибках

### Ошибка: "Command failed to execute"

**Возможные причины:**
1. PowerShell скрипт не найден
2. Неправильный путь в `Command::new()`
3. Недостаточно прав для выполнения PowerShell

**Диагностика:**

```powershell
# Проверьте наличие скриптов
Test-Path "E:\WORLD_OLLAMA\scripts\start_agent_training.ps1"
Test-Path "E:\WORLD_OLLAMA\scripts\ingest_watcher.ps1"

# Проверьте права выполнения PowerShell
Get-ExecutionPolicy
# Expected: RemoteSigned или Bypass
```

**Решение:**

```powershell
# Если ExecutionPolicy = Restricted
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### Ошибка: "training_status.json not found"

**Причина:** Директория `%APPDATA%\tauri_fresh` не существует

**Решение:**

```powershell
New-Item -ItemType Directory -Path "$env:APPDATA\tauri_fresh" -Force
```

---

### Ошибка: "git not found"

**Причина:** Git не установлен или не в PATH

**Решение:**

```powershell
# Проверьте Git
git --version

# Если не найден → установите Git:
# https://git-scm.com/downloads
```

---

### Ошибка: "Compilation failed"

**Причина:** Изменения в `commands.rs` содержат синтаксические ошибки

**Решение:**

```powershell
cd E:\WORLD_OLLAMA\client\src-tauri
cargo check --verbose
# Смотрите подробные ошибки компиляции
```

---

## 📝 Отчёт о тестировании

**После завершения всех сценариев:**

1. Заполните таблицу выше (✅ или ❌ для каждого сценария)
2. Сохраните скриншоты ошибок (если есть)
3. Отправьте результаты:

```markdown
# Результаты тестирования Task 9

**Дата:** 27.11.2025  
**Тестировщик:** [Ваше имя]

**Сценарии:**
- ✅ Scenario 1: INDEX KNOWLEDGE (valid) → Passed
- ✅ Scenario 2: TRAIN AGENT (valid) → Passed
- ✅ Scenario 3: TRAIN invalid DATA_PATH → Passed
- ✅ Scenario 4: TRAIN invalid EPOCHS → Passed
- ✅ Scenario 5: TRAIN invalid PROFILE → Passed
- ✅ Scenario 6: GIT with changes → Passed
- ✅ Scenario 7: GIT no changes → Passed
- ✅ Scenario 8: GIT invalid path → Passed

**Общий результат:** 8/8 (100%) ✅

**Комментарии:**
[Любые заметки, ошибки, предложения]
```

---

## 🎓 Дополнительные проверки (опционально)

### Проверка 1: Concurrent Training Blocked

**Цель:** Убедиться, что нельзя запустить 2 обучения одновременно

**Шаги:**
1. Запустите TRAIN AGENT (valid)
2. Сразу же запустите ещё один TRAIN AGENT
3. **Ожидается:** Ошибка "⚠️ Обучение уже выполняется!"

---

### Проверка 2: Concurrent Indexation Blocked

**Цель:** Убедиться, что нельзя запустить 2 индексации одновременно

**Шаги:**
1. Запустите INDEX KNOWLEDGE
2. Сразу же запустите ещё один INDEX KNOWLEDGE
3. **Ожидается:** Ошибка "Индексация уже выполняется"

---

### Проверка 3: UI Responsiveness

**Цель:** Убедиться, что UI не зависает при запуске фоновых процессов

**Шаги:**
1. Запустите TRAIN AGENT или INDEX KNOWLEDGE
2. Сразу же переключитесь между вкладками (Home → Chat → Commands)
3. **Ожидается:** UI отзывчив, нет зависаний

---

**Конец тестового гайда**

*Автоматически сгенерировано AI Agent 27.11.2025 21:45 UTC*
