# TASK 10: PRE-PUSH AUDIT REPORT

**Дата:** 27.11.2025  
**Статус:** 🟡 В процессе  
**Цель:** Классификация всех директорий перед первым пушем в облако

---

## 📊 ОБЩАЯ СТАТИСТИКА (по размерам)

| Директория | Размер (MB) | Категория | Действие |
|------------|-------------|-----------|----------|
| **services/** | 23,600.81 | ⚠️ Mixed | Очистить venv/cache, оставить код |
| **models/** | 17,500.45 | ❌ HEAVY | НЕ ПУШИТЬ (большие модели) |
| **client/** | 8,387.83 | ⚠️ Mixed | Очистить node_modules, оставить код |
| **archive/** | 1,800.37 | ❌ HEAVY | НЕ ПУШИТЬ (старые чекпоинты) |
| **production/** | 1,141.27 | ❌ HEAVY | НЕ ПУШИТЬ (большие модели) |
| **workbench/** | 602.39 | 🧪 SANDBOX | Проверить перед удалением |
| **saves/** | 105.80 | ⚠️ Mixed | Оставить финальные адаптеры |
| **library/** | 7.77 | ✅ KEEP | База знаний (документы ТРИЗ) |
| **scripts/** | 0.14 | ✅ KEEP | PowerShell скрипты |
| **docs/** | 0.15 | ✅ KEEP | Документация |
| **UX_SPEC/** | 0.16 | ✅ KEEP | UX спецификации |
| **USER/** | 0.06 | ✅ KEEP | Пользовательские скрипты |
| **.github/** | 0.06 | ✅ KEEP | GitHub конфигурация |
| **logs/** | 0.08 | ❌ RUNTIME | НЕ ПУШИТЬ (логи) |
| **backups/** | 0.13 | ⚠️ Mixed | Оставить структуру, очистить daily/weekly |
| **agents/** | 0.00 | ✅ KEEP | Агентские конфиги (пустые data/logs) |
| **llamaboard_cache/** | 0.00 | ❌ RUNTIME | НЕ ПУШИТЬ (кеш) |
| **client_backup_...** | 0.10 | ❌ TEMP | УДАЛИТЬ (временный бэкап) |

**ИТОГО:** ~53 GB  
**После очистки (оценка):** ~50 MB (код + docs + library)

---

## 1️⃣ КАТЕГОРИЯ: MUST KEEP (Основной продукт)

### ✅ client/ (8,387.83 MB → ~15 MB после очистки)

**Что оставить:**
- `src/` — исходный код Svelte (компоненты, stores, routes)
- `src-tauri/` — Rust backend (commands, parser, типы)
- `docs/` — документация по задачам (TASK_9_COMPLETION_REPORT.md и т.п.)
- `package.json`, `tsconfig.json`, `vite.config.js`, `svelte.config.js` — конфигурация
- `.gitignore`, `README_CLIENT.md`
- Тестовые скрипты: `run_auto_tests.ps1`, `test_task*.ps1`

**Что удалить/игнорировать:**
- ❌ `node_modules/` (~8,300 MB) — уже в `.gitignore`
- ❌ `.svelte-kit/` — генерируемая директория
- ❌ `client_backup_20251127_160935/` — временный бэкап (УДАЛИТЬ)

**Статус:** ✅ Готово (`.gitignore` покрывает `node_modules`)

---

### ✅ services/ (23,600.81 MB → ~10 MB после очистки)

**Структура:**
```
services/
├── lightrag/          (CORTEX — LightRAG knowledge server)
├── llama_factory/     (LLaMA Factory — обучение моделей)
├── neuro_terminal/    (Legacy Chainlit UI)
├── connectors/        (SYNAPSE connector)
└── fastapi-gateways/  (FastAPI endpoints)
```

**Что оставить:**
- Весь Python код (`.py` файлы)
- `requirements.txt`, `pyproject.toml`, `setup.py`
- Конфигурационные файлы (`.yaml`, `.json`)
- `README.md` файлы в каждом сервисе

**Что удалить/игнорировать:**
- ❌ `venv/`, `.venv/` в каждом сервисе (~20 GB) — уже в `.gitignore`
- ❌ `lightrag/data/` — индексы и кеш (~3 GB) — уже в `.gitignore`
- ❌ `llama_factory/.cache/` — Hugging Face кеш
- ❌ `neuro_terminal/.chainlit/` — сессии Chainlit — уже в `.gitignore`
- ❌ `__pycache__/` — уже в `.gitignore`

**Статус:** ✅ Готово (`.gitignore` покрывает основное)

---

### ✅ library/ (7.77 MB)

**Содержимое:**
- `raw_documents/` — 486 текстовых файлов (принципы ТРИЗ, AI методологии)
- `cleaned_documents/` — обработанные документы
- `backups/` — бэкапы библиотеки

**Решение:** **ОСТАВИТЬ полностью** (база знаний проекта, небольшой размер)

**Статус:** ✅ Оставить как есть

---

### ✅ scripts/ (0.14 MB)

**Содержимое:**
- `start_lightrag.ps1`, `start_neuro_terminal.ps1`, `start_training_ui.ps1`
- `ingest_watcher.ps1` (индексация документов)
- `start_agent_training.ps1` (TASK 9 — запуск обучения)
- `generate_map.ps1` (генерация PROJECT_MAP)
- Каталоги: `maintenance/`, `monitoring/`, `setup/`

**Решение:** **ОСТАВИТЬ всё** (рабочие скрипты проекта)

**Статус:** ✅ Оставить как есть

---

### ✅ docs/ (0.15 MB)

**Содержимое:**
- Технические отчёты, спецификации

**Решение:** **ОСТАВИТЬ** (базовая документация)

**Статус:** ✅ Оставить как есть

---

### ✅ USER/ (0.06 MB)

**Содержимое:**
- `START_ALL.ps1` — главный launcher сервисов
- Пользовательские конфигурации

**Решение:** **ОСТАВИТЬ**

**Статус:** ✅ Оставить как есть

---

### ✅ .github/ (0.06 MB)

**Содержимое:**
- `copilot-instructions.md` — инструкции для GitHub Copilot

**Решение:** **ОСТАВИТЬ**

**Статус:** ✅ Оставить как есть

---

### ✅ UX_SPEC/ (0.16 MB)

**Содержимое:**
- UX спецификации (design docs)

**Решение:** **ОСТАВИТЬ**

**Статус:** ✅ Оставить как есть

---

### ✅ agents/ (0.00 MB)

**Структура:**
```
agents/
├── qwen2-main/    (configs, data, logs, scripts, world)
└── helper-lite/   (configs, data, logs, scripts, world)
```

**Решение:** **ОСТАВИТЬ структуру**, но добавить в `.gitignore`:
- `agents/*/data/` (runtime данные)
- `agents/*/logs/` (логи)

**Статус:** ⏳ Обновить `.gitignore`

---

## 2️⃣ КАТЕГОРИЯ: HEAVY ARTIFACTS (НЕ ПУШИТЬ)

### ❌ models/ (17,500.45 MB)

**Содержимое:**
- `qwen2-triz-merged/` — финальные адаптеры (*.safetensors, конфиги)
- Большие файлы моделей

**Решение:** **НЕ ПУШИТЬ** (добавлено в `.gitignore`)
- ✅ `*.safetensors` — уже игнорируется
- ✅ Исключение `!**/saves/**/*.safetensors` — финальные адаптеры разрешены

**Статус:** ✅ Готово (`.gitignore` покрывает)

---

### ❌ archive/ (1,800.37 MB)

**Содержимое:**
- `Qwen2-7B_checkpoint/` — старые чекпоинты обучения
- `TD010v2_triz_extended/` — старые модели

**Решение:** **НЕ ПУШИТЬ** (добавить в `.gitignore`)

**Статус:** ⏳ Добавить `archive/` в `.gitignore`

---

### ❌ production/ (1,141.27 MB)

**Содержимое:**
- Большие production модели

**Решение:** **НЕ ПУШИТЬ** (добавить в `.gitignore`)

**Статус:** ⏳ Добавить `production/` в `.gitignore`

---

### ❌ saves/ (105.80 MB)

**Содержимое:**
- Чекпоинты обучения в `llama_factory/saves/`

**Решение:** **Частично игнорируется**
- ✅ `checkpoint-*/` — уже игнорируется
- ✅ Финальные адаптеры (`adapter_model.safetensors`) — разрешены

**Статус:** ✅ Готово (`.gitignore` покрывает)

---

### ❌ logs/ (0.08 MB)

**Решение:** **НЕ ПУШИТЬ** (уже в `.gitignore`)

**Статус:** ✅ Готово

---

### ❌ llamaboard_cache/ (0.00 MB)

**Решение:** **НЕ ПУШИТЬ** (уже в `.gitignore`)

**Статус:** ✅ Готово

---

## 3️⃣ КАТЕГОРИЯ: SANDBOX / ВРЕМЕННЫЕ

### 🧪 workbench/ (602.39 MB)

**Задача 10.2:** Проверить содержимое на предмет:
1. Скрипты, которые уже в production (`scripts/`)
2. Прототипы Tauri-клиента (уже в `client/`)
3. Временные эксперименты (диагностика, тесты)

**Действия:**
1. Просканировать `workbench/sandbox_main`
2. Перенести полезное в основную структуру (если есть)
3. Остальное — добавить в `.gitignore` или удалить

**Статус:** ⏳ Требует ручной проверки (TASK 10.2)

---

### ❌ client_backup_20251127_160935/ (0.10 MB)

**Решение:** **УДАЛИТЬ** (временный бэкап, созданный во время разработки)

**Статус:** ⏳ УДАЛИТЬ

---

### ⚠️ backups/ (0.13 MB)

**Содержимое:**
- `archived_reports/` — старые MD файлы (RAEDME_legacy, TECHNICAL_REPORT_OBSOLETE)
- `daily/`, `weekly/` — пустые (уже игнорируются)

**Решение:**
- ✅ Оставить структуру `backups/`
- ✅ Оставить `archived_reports/` (полезная история)
- ✅ `daily/`, `weekly/` — уже игнорируются

**Статус:** ✅ Готово

---

## 📝 ФАЙЛЫ В КОРНЕ

### ✅ Оставить:
- `README.md` (главный README — будет переписан в TASK 10.4)
- `PROJECT_MAP.md` (архитектурная карта)
- `PROJECT_STATUS_SNAPSHOT_v3.3.md` (текущий статус)
- `INDEX.md` (индекс документации)
- `MANUAL.md` (руководство пользователя)
- `DOCUMENTATION_CLEANUP_REPORT.md` (отчёт о чистке)
- `.gitattributes`, `.gitignore` (Git конфигурация)

### ❌ Удалить/игнорировать:
- `.webui_secret_key` — уже в `.gitignore` ✅
- `rustup-init.exe` — установщик Rust (добавить в `.gitignore`)
- `project_tree_full.txt` — временный файл аудита (добавить в `.gitignore`)

### ⚠️ Проверить:
- `RAEDME` (опечатка в имени?) — переименовать или удалить

**Статус:** ⏳ Требует действий

---

## 🎯 ДЕЙСТВИЯ ДЛЯ TASK 10.1

### ✅ Уже защищено в `.gitignore`:
- venv, node_modules, __pycache__
- *.safetensors (с исключением для saves/)
- logs/, services/lightrag/data/
- llamaboard_cache/
- .chainlit/, backups/daily/, backups/weekly/
- .webui_secret_key

### ⏳ Добавить в `.gitignore`:
1. `archive/` — старые чекпоинты (1.8 GB)
2. `production/` — production модели (1.1 GB)
3. `workbench/` — песочница (600 MB)
4. `agents/*/data/` и `agents/*/logs/` — runtime данные агентов
5. `rustup-init.exe` — установщик
6. `project_tree_full.txt` — временные файлы аудита
7. `*.exe` — исполняемые файлы (кроме необходимых в .github)

### ⏳ Удалить физически:
1. `client_backup_20251127_160935/` — временный бэкап

### ⏳ Проверить вручную (TASK 10.2):
1. `workbench/sandbox_main/` — что там осталось от разработки?
2. `RAEDME` в корне — переименовать в `README_OLD.md` или удалить?

---

## 📊 ИТОГОВАЯ ОЦЕНКА

**Текущий размер:** ~53 GB  
**После очистки (оценка):** ~50 MB (код + конфиги + docs + library)

**Компоненты финального репозитория:**
- Tauri Desktop Client (исходники)
- Services (Python код без venv)
- Scripts (PowerShell automation)
- Library (TRIZ knowledge base, 7.77 MB)
- Docs (отчёты, спецификации)
- Configs (Ollama Modelfile, LLaMA Factory configs)

**Не войдёт в репозиторий:**
- Большие модели (19 GB)
- Виртуальные окружения (20+ GB)
- Кеши и логи (3+ GB)
- Архивы и бэкапы (2 GB)
- Временные песочницы (600 MB)

---

**Следующий шаг:** TASK 10.2 — Сканирование `workbench/sandbox_main`

**Дата:** 27.11.2025 22:00 UTC
