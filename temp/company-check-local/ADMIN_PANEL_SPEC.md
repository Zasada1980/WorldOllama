# 📋 ТЗ: Админ-панель с визуальным редактором UI

## 🎯 Цель проекта

Создать **встроенную админ-панель** внутри веб-интерфейса CompanyCheck для разработки и управления UI **в реальном времени** без перекомпиляции.

---

## 👤 Роли и доступы

### 1. **Администратор (Full Access)**

**Пароль:** `admin2024` (временный, потом в env)

**Доступные функции:**

- ✅ **Визуальный редактор UI** (drag-and-drop компонентов)
- ✅ **Live Preview** (изменения применяются мгновенно)
- ✅ **CSS Editor** (редактирование Tailwind классов)
- ✅ **API Configuration** (настройка источников данных)
- ✅ **Component Library** (создание/редактирование компонентов)
- ✅ **Theme Editor** (цвета, шрифты, отступы)
- ✅ **User Management** (управление пользователями)
- ✅ **Analytics Dashboard** (статистика использования)
- ✅ **Deploy Manager** (развёртывание на production)

### 2. **Обычный пользователь (Read Only)**

**Доступ:**

- Поиск компаний
- Просмотр данных
- AI-анализ
- История поиска

---

## 🛠️ Архитектура админ-панели

### **Структура файлов**

```
src/
├── App.tsx                    # Основное приложение
├── admin/
│   ├── AdminPanel.tsx         # Главная панель администратора
│   ├── UIEditor.tsx           # Визуальный редактор компонентов
│   ├── ComponentLibrary.tsx   # Библиотека переиспользуемых компонентов
│   ├── ThemeEditor.tsx        # Редактор темы (цвета, шрифты)
│   ├── APIConfig.tsx          # Настройка API endpoints
│   ├── UserManager.tsx        # Управление пользователями
│   ├── Analytics.tsx          # Аналитика и статистика
│   └── DeployManager.tsx      # Развёртывание изменений
│
├── components/
│   ├── DraggableComponent.tsx # Компонент для drag-and-drop
│   ├── CodeEditor.tsx         # Monaco Editor для кода
│   └── LivePreview.tsx        # Превью изменений в реальном времени
│
└── utils/
    ├── componentRegistry.ts   # Реестр доступных компонентов
    └── saveConfig.ts          # Сохранение конфигурации в localStorage/DB
```

---

## 🖼️ UI/UX админ-панели

### **1. Вход в админ-режим**

```tsx
// Скрытая кнопка (Ctrl+Alt+A или тройной клик на логотип)
<button onClick={() => setShowAdminLogin(true)}>
  🔐 Admin Login
</button>

// Модальное окно авторизации
<AdminLoginModal>
  <input type="password" placeholder="Admin Password" />
  <button>Login</button>
</AdminLoginModal>
```

### **2. Главный интерфейс админ-панели**

```
┌─────────────────────────────────────────────────────────────┐
│  CompanyCheck Admin Panel            [User: Admin] [Logout] │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─ Sidebar (Left) ───┐  ┌─ Main Editor (Center) ─────────┐ │
│  │                     │  │                                 │ │
│  │ 🖌️ UI Editor         │  │  ┌─ Component Tree ─────────┐ │ │
│  │ 🎨 Theme Editor     │  │  │ • SearchPanel            │ │ │
│  │ 🔌 API Config       │  │  │   • SearchInput          │ │ │
│  │ 📦 Components       │  │  │   • SearchButton         │ │ │
│  │ 👥 Users            │  │  │ • ResultsPanel           │ │ │
│  │ 📊 Analytics        │  │  │ • Footer                 │ │ │
│  │ 🚀 Deploy           │  │  └─────────────────────────┘ │ │
│  │                     │  │                                 │ │
│  └─────────────────────┘  │  ┌─ Visual Editor ──────────┐ │ │
│                            │  │                           │ │ │
│                            │  │  [Drag & Drop Canvas]     │ │ │
│                            │  │                           │ │ │
│                            │  │  ┌────────────────┐       │ │ │
│                            │  │  │ Search Panel   │       │ │ │
│                            │  │  │                │       │ │ │
│                            │  │  │ [Input] [Btn]  │       │ │ │
│                            │  │  └────────────────┘       │ │ │
│                            │  │                           │ │ │
│                            │  └───────────────────────────┘ │ │
│                            │                                 │ │
│                            │  ┌─ Properties Panel ───────┐  │ │
│                            │  │ Component: SearchPanel    │  │ │
│                            │  │ • Background: bg-white    │  │ │
│                            │  │ • Padding: p-8            │  │ │
│                            │  │ • Border: rounded-2xl     │  │ │
│                            │  │ [Save Changes]            │  │ │
│                            │  └───────────────────────────┘  │ │
│                            └─────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Функциональные модули

### **1. UI Editor (Визуальный редактор)**

**Возможности:**

- Drag & Drop компонентов на canvas
- Изменение Tailwind классов через GUI
- Live Preview (изменения применяются мгновенно)
- Редактирование текста, кнопок, форм
- Добавление/удаление секций

**Технологии:**

- `react-dnd` (drag and drop)
- `react-colorful` (выбор цветов)
- `react-select` (выбор Tailwind классов)

**Пример кода:**

```tsx
import { useDrag, useDrop } from "react-dnd";

function DraggableButton({ text, className }) {
  const [{ isDragging }, drag] = useDrag(() => ({
    type: "BUTTON",
    item: { text, className },
    collect: (monitor) => ({
      isDragging: !!monitor.isDragging(),
    }),
  }));

  return (
    <button ref={drag} className={className}>
      {text}
    </button>
  );
}

function DropZone({ onDrop }) {
  const [{ isOver }, drop] = useDrop(() => ({
    accept: ["BUTTON", "INPUT", "PANEL"],
    drop: (item) => onDrop(item),
    collect: (monitor) => ({
      isOver: !!monitor.isOver(),
    }),
  }));

  return (
    <div ref={drop} className={isOver ? "bg-blue-100" : "bg-gray-50"}>
      Drop components here
    </div>
  );
}
```

---

### **2. Component Library (Библиотека компонентов)**

**Готовые компоненты:**

- 🔘 Buttons (primary, secondary, ghost)
- 📝 Inputs (text, search, number)
- 📦 Panels (card, modal, sidebar)
- 📊 Charts (pie, bar, line)
- 🎨 Icons (Lucide React)
- 📱 Mobile components (hamburger menu, bottom nav)

**Структура:**

```tsx
const COMPONENT_LIBRARY = {
  buttons: [
    {
      id: "btn-primary",
      label: "Primary Button",
      className: "bg-blue-600 text-white px-6 py-3 rounded-lg",
    },
    {
      id: "btn-secondary",
      label: "Secondary Button",
      className: "bg-gray-200 text-gray-800 px-6 py-3 rounded-lg",
    },
  ],
  inputs: [
    {
      id: "input-search",
      label: "Search Input",
      className: "border rounded-lg px-4 py-2",
    },
  ],
  panels: [
    {
      id: "panel-card",
      label: "Card Panel",
      className: "bg-white rounded-2xl shadow-sm p-8",
    },
  ],
};
```

---

### **3. Theme Editor (Редактор темы)**

**Настройки:**

- 🎨 **Цвета:** Primary, Secondary, Accent, Background
- 🔤 **Шрифты:** Font Family, Font Size, Line Height
- 📏 **Отступы:** Padding, Margin, Gap
- 🔲 **Границы:** Border Radius, Border Width
- 🌗 **Тёмная тема:** Toggle Dark Mode

**Сохранение:**

```tsx
const THEME_CONFIG = {
  colors: {
    primary: "#3B82F6",
    secondary: "#10B981",
    background: "#F9FAFB",
  },
  fonts: {
    family: "Inter, sans-serif",
    size: { base: 16, lg: 18 },
  },
  spacing: {
    container: "max-w-6xl",
    padding: "p-8",
  },
};

// Сохранение в localStorage
localStorage.setItem("theme_config", JSON.stringify(THEME_CONFIG));
```

---

### **4. API Config (Настройка API)**

**Параметры:**

- 🔗 **Resource IDs:** Список источников данных Israeli Gov API
- 🔑 **API Keys:** Gemini AI, другие сервисы
- ⏱️ **Rate Limits:** Защита от превышения лимитов
- 🔄 **Fallback Sources:** Резервные источники данных

**UI:**

```tsx
<APIConfigPanel>
  <h3>Israeli Government API</h3>
  <input
    placeholder="Resource ID 1"
    defaultValue="f004176c-b85f-4542-8901-7b3176f9a054"
  />
  <input
    placeholder="Resource ID 2"
    defaultValue="1adb23e3-e6e1-4a83-bf8d-a0d85e71b7cb"
  />

  <h3>Gemini AI API</h3>
  <input type="password" placeholder="API Key" defaultValue="AIzaSyDv1PUz..." />
  <select>
    <option>gemini-2.0-flash</option>
    <option>gemini-1.5-pro</option>
  </select>

  <button>Save Configuration</button>
</APIConfigPanel>
```

---

### **5. User Management (Управление пользователями)**

**Функции:**

- ➕ Создание новых пользователей
- 🔒 Управление ролями (admin, user, viewer)
- 📊 Просмотр активности пользователей
- 🚫 Блокировка/разблокировка аккаунтов

**Роли:**

- **Admin:** Полный доступ
- **User:** Поиск + AI-анализ
- **Viewer:** Только просмотр данных

---

### **6. Analytics Dashboard (Аналитика)**

**Метрики:**

- 📈 Количество поисков (за день/неделю/месяц)
- 🏢 Топ-10 искомых компаний
- 🤖 Использование AI-анализа
- 🌍 География пользователей (by IP)
- ⏱️ Средняя скорость ответа API

**Визуализация:**

- Графики (Recharts)
- Таблицы (React Table)
- Карты (если нужна география)

---

### **7. Deploy Manager (Развёртывание)**

**Процесс:**

1. **Preview Changes** — превью изменений перед деплоем
2. **Build & Test** — компиляция + автотесты
3. **Deploy to Production** — отправка на сервер 46.224.36.109
4. **Rollback Option** — откат к предыдущей версии

**UI:**

```tsx
<DeployPanel>
  <h3>Current Version: v1.2.3</h3>
  <button onClick={buildPreview}>🔍 Preview Changes</button>
  <button onClick={runTests}>🧪 Run E2E Tests</button>
  <button onClick={deployToProduction}>🚀 Deploy to Production</button>
  <button onClick={rollback}>⏪ Rollback to v1.2.2</button>
</DeployPanel>
```

---

## 🔐 Безопасность

### **Аутентификация:**

- Пароль хранится в `localStorage` (для демо)
- Для production: JWT tokens + backend auth
- Session timeout: 30 минут неактивности

### **Защита от несанкционированного доступа:**

```tsx
if (!isAdmin) {
  return <Navigate to="/login" />;
}
```

---

## 📦 Технологический стек

| Функция          | Библиотека             |
| ---------------- | ---------------------- |
| Drag & Drop      | `react-dnd`            |
| Code Editor      | `@monaco-editor/react` |
| Color Picker     | `react-colorful`       |
| Charts           | `recharts`             |
| Icons            | `lucide-react`         |
| State Management | `zustand` (optional)   |
| Forms            | `react-hook-form`      |

---

## 🚀 План разработки (Roadmap)

### **Phase 1: Базовая админ-панель (1-2 дня)**

- ✅ Вход по паролю
- ✅ Базовый UI редактор (изменение текста/цветов)
- ✅ Theme Editor (цвета, шрифты)
- ✅ API Config (Resource IDs, Gemini key)

### **Phase 2: Визуальный редактор (3-5 дней)**

- ✅ Drag & Drop компонентов
- ✅ Live Preview
- ✅ Component Library
- ✅ Сохранение конфигурации

### **Phase 3: Продвинутые функции (5-7 дней)**

- ✅ User Management
- ✅ Analytics Dashboard
- ✅ Deploy Manager
- ✅ Code Editor (Monaco)

### **Phase 4: Production Ready (2-3 дня)**

- ✅ Backend API для сохранения изменений
- ✅ JWT аутентификация
- ✅ E2E тесты для админ-панели
- ✅ Документация

---

## 📝 Пример использования

### **Сценарий 1: Изменить цвет кнопки поиска**

1. Админ входит в панель (пароль `admin2024`)
2. Открывает **UI Editor**
3. Выбирает **SearchButton** в Component Tree
4. В Properties Panel меняет `bg-blue-600` → `bg-green-600`
5. Видит изменения в Live Preview
6. Нажимает **Save Changes**
7. Изменения сохраняются в `localStorage`
8. При следующем открытии сайта кнопка зелёная

### **Сценарий 2: Добавить новое поле в форму поиска**

1. Админ открывает **Component Library**
2. Выбирает **Input** компонент
3. Drag & Drop на **SearchPanel**
4. Настраивает placeholder: "Company Name or ID"
5. Сохраняет изменения
6. Поле появляется на странице

---

## 🎓 Обучение администратора

### **Видео-туториалы (планируется):**

- 📹 Как войти в админ-панель
- 📹 Как изменить цвета темы
- 📹 Как добавить новый компонент
- 📹 Как развернуть изменения на production

### **Документация:**

- 📖 Полное руководство по админ-панели
- 📖 API Reference
- 📖 Troubleshooting Guide

---

## ✅ Критерии готовности

- [ ] Админ может войти по паролю
- [ ] Админ видит все компоненты в дереве
- [ ] Админ может изменять Tailwind классы
- [ ] Изменения применяются в реальном времени
- [ ] Конфигурация сохраняется в localStorage
- [ ] Админ может создавать новых пользователей
- [ ] Админ видит аналитику использования
- [ ] Админ может развернуть изменения на production
- [ ] Есть откат к предыдущей версии

---

**Дата создания:** 08.12.2025  
**Версия:** 1.0  
**Автор:** AI Assistant  
**Статус:** Draft (требуется утверждение)
