# ⚔️ ОПЕРАЦИЯ "VALHALLA" - GIT IGNORE (ПРИНЦИП ТРИЗ №11 "ЗАРАНЕЕ ПОДЛОЖЕННАЯ ПОДУШКА")
# Дата создания: 26.11.2025
# Назначение: Защита от случайного push тяжелых файлов в GitHub

# ========================================
# КРИТИЧЕСКАЯ ЗАЩИТА: ВИРТУАЛЬНЫЕ ОКРУЖЕНИЯ
# ========================================
# Принцип: Не пушим 500+ MB зависимостей Python
**/venv/
**/.venv/
**/__pycache__/
*.pyc
*.pyo
*.pyd

# ========================================
# КРИТИЧЕСКАЯ ЗАЩИТА: LLM МОДЕЛИ (OLLAMA)
# ========================================
# Принцип: Не пушим 9 GB весов моделей
*.gguf
*.bin
*.safetensors
# ИСКЛЮЧЕНИЕ: Разрешаем safetensors ТОЛЬКО в saves/ (LoRA адаптеры)
!**/saves/**/*.safetensors

# ========================================
# КРИТИЧЕСКАЯ ЗАЩИТА: LIGHTRAG DATA
# ========================================
# Принцип: Не пушим индексы и кеш (340 KB сейчас, но может вырасти до GB)
services/lightrag/data/
services/lightrag/vdb_*/
services/lightrag/*.graphml
services/lightrag/*.json
# ИСКЛЮЧЕНИЕ: Разрешаем конфигурационные JSON
!services/lightrag/requirements.txt
!services/lightrag/*.py

# ========================================
# ЗАЩИТА: LLAMA FACTORY КЕШИ
# ========================================
llamaboard_cache/
services/llama_factory/.cache/
services/llama_factory/saves/*/*.jsonl
services/llama_factory/saves/*/checkpoint-*/
# ИСКЛЮЧЕНИЕ: Разрешаем финальные адаптеры и конфиги
!services/llama_factory/saves/*/adapter_model.safetensors
!services/llama_factory/saves/*/adapter_config.json
!services/llama_factory/saves/*/training_args.json

# ========================================
# ЗАЩИТА: БИБЛИОТЕКА ДОКУМЕНТОВ (ОПЦИОНАЛЬНО)
# ========================================
# Комментарий: library/ содержит 178 файлов (7.66 MB) - небольшой размер
# Решение: ОСТАВЛЯЕМ для полной репликации knowledge base
# library/raw_documents/*.txt  # Раскомментировать, если хотим исключить

# ========================================
# ЗАЩИТА: ЛОГИ И ВРЕМЕННЫЕ ФАЙЛЫ
# ========================================
*.log
logs/
*.tmp
*.temp
.DS_Store
Thumbs.db

# ========================================
# ЗАЩИТА: IDE И РЕДАКТОРЫ
# ========================================
.vscode/
.idea/
*.swp
*.swo
*~

# ========================================
# ЗАЩИТА: CHAINLIT СЕССИИ
# ========================================
services/neuro_terminal/.chainlit/
services/neuro_terminal/.files/

# ========================================
# ЗАЩИТА: БЭКАПЫ И АРХИВЫ
# ========================================
backups/daily/
backups/weekly/
*.zip
*.tar.gz
*.7z

# ========================================
# ЗАЩИТА: СЕКРЕТНЫЕ КЛЮЧИ
# ========================================
.env
.env.local
*.key
*.pem
.webui_secret_key

# ========================================
# WHITELIST: ЧТО ПУШИМ В GITHUB
# ========================================
# ✅ Исходный код (.py, .ps1, .yaml, .md)
# ✅ Конфигурации (requirements.txt, config.yaml)
# ✅ LoRA адаптеры (adapter_model.safetensors в saves/)
# ✅ Документация (README.md, MANUAL.md, STATE_SNAPSHOT)
# ✅ Скрипты управления (USER/, scripts/)
# ✅ Библиотека знаний (library/raw_documents/ - 7.66 MB)
