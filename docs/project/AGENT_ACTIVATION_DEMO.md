# 🤖 ДЕМОНСТРАЦИЯ АКТИВАЦИИ АГЕНТА

**Дата:** 28 ноября 2025 г.  
**Версия протокола:** 1.0  
**Статус:** ✅ АКТИВЕН

---

## 🎯 НАЗНАЧЕНИЕ

Этот документ демонстрирует **как агент должен работать** с новой системой автоматической сверки с векторными зависимостями.

---

## ✅ ПРИМЕР 1: Запрос про Desktop Client

### Запрос пользователя:
```
"Расскажи подробно про Settings Panel и какие в нём функции"
```

---

### ШАГ 1: Автоматическая сверка (ВНУТРЕННИЙ ПРОЦЕСС)

```markdown
🔄 АКТИВАЦИЯ АГЕНТА:

ЭТАП 1: ЗАГРУЗКА КОНТЕКСТА
☑ 1. Читаю: E:\WORLD_OLLAMA\docs\project\DOCUMENTATION_STRUCTURE_ANALYSIS.md
☑ 2. Категория запроса: Desktop Client / UI → Кластер #3 (Desktop Client Tasks)
☑ 3. Загружаю: docs/tasks/TASKS_CONSOLIDATED_REPORT.md

ЭТАП 2: ПРОВЕРКА ЗАВИСИМОСТЕЙ
☑ 4. Проверяю граф: TASK 5 упоминается в консолидированном отчёте
☑ 5. Актуальность: Консолидированный отчёт от 28.11.2025 — АКТУАЛЬНО

ЭТАП 3: ВЫПОЛНЕНИЕ
☑ 6. Извлекаю информацию о TASK 5 из консолидированного отчёта
☑ 7. Детализация НЕ требуется (достаточно консолидированного)

ЭТАП 4: ОТЧЁТ
☑ 8. Готовлю ответ с подтверждением сверки
☑ 9. Использую маркеры 📌 ЦИТАТА, 💡 ВЫВОД
```

---

### ШАГ 2: Ответ пользователю (ВИДИМЫЙ)

```markdown
✅ Сверка с векторной картой:
- Прочитан: DOCUMENTATION_STRUCTURE_ANALYSIS.md
- Кластер: Desktop Client Tasks (#3)
- Консолидированный отчёт: docs/tasks/TASKS_CONSOLIDATED_REPORT.md
- Связанные документы: client/TASK5_REPORT.md (архив, детализация)

---

## TASK 5: Settings Panel + Agent Profiles

📌 **Источник:** `docs/tasks/TASKS_CONSOLIDATED_REPORT.md`, раздел TASK 5

### Цель задачи
Реализация панели управления настройками и профилями агентов для Desktop Client.

### Основные функции

1. **Управление профилями агентов:**
   - Создание новых профилей (имя, модель, system prompt)
   - Редактирование существующих профилей
   - Удаление профилей
   - Переключение между профилями

2. **Настройки генерации:**
   - Temperature (0.1 - 2.0)
   - Top P (0.0 - 1.0)
   - Max Tokens (256 - 8192)
   - Stream режим (включён по умолчанию)

3. **Системные настройки:**
   - Ollama URL (http://127.0.0.1:11434)
   - Модель по умолчанию (qwen2.5:14b-instruct-q4_k_m)
   - Сохранение в config.json

### Технические детали

**Структура данных (TypeScript):**
```typescript
interface AgentProfile {
  name: string;
  model: string;
  system_prompt: string;
  temperature: number;
  top_p: number;
  max_tokens: number;
}
```

**Tauri команды (Rust):**
- `get_profiles()` → `Vec<AgentProfile>` — получить все профили
- `save_profile(profile: AgentProfile)` → `Result<(), String>` — сохранить профиль
- `delete_profile(name: String)` → `Result<(), String>` — удалить профиль

**Компонент:** `client/src/lib/SettingsPanel.svelte` (~250 строк)

### Статус
✅ **ЗАВЕРШЁН** (релиз v0.1.0)

---

💡 **Дополнительная информация:**

Для детальных TypeScript/Rust структур данных см. архивный документ:  
`client/TASK5_REPORT.md`

Связанные компоненты:
- TASK 4: System Status Panel (мониторинг)
- TASK 8: Commands Panel (управление контекстом)

📦 **Релиз:** v0.1.0 Developer Preview
```

---

## ✅ ПРИМЕР 2: Запрос про модели

### Запрос пользователя:
```
"Какую модель TD-010 использовать для production?"
```

---

### ШАГ 1: Автоматическая сверка (ВНУТРЕННИЙ ПРОЦЕСС)

```markdown
🔄 АКТИВАЦИЯ АГЕНТА:

ЭТАП 1: ЗАГРУЗКА КОНТЕКСТА
☑ 1. Читаю: E:\WORLD_OLLAMA\docs\project\DOCUMENTATION_STRUCTURE_ANALYSIS.md
☑ 2. Категория запроса: Модели / TD-010 → Кластер #4 (Модели и обучение)
☑ 3. Загружаю: docs/models/MODELS_CONSOLIDATED_REPORT.md

ЭТАП 2: ПРОВЕРКА ЗАВИСИМОСТЕЙ
☑ 4. Проверяю граф: TD-010v2 и TD-010v3 описаны в консолидированном отчёте
☑ 5. Актуальность: Консолидированный отчёт от 28.11.2025 — АКТУАЛЬНО

ЭТАП 3: ВЫПОЛНЕНИЕ
☑ 6. Извлекаю сравнительную таблицу и Final Verdict
☑ 7. Детализация НЕ требуется (вердикт есть в консолидированном)

ЭТАП 4: ОТЧЁТ
☑ 8. Готовлю ответ с таблицей метрик
☑ 9. Использую маркеры 📌 ЦИТАТА, 💡 ВЫВОД
```

---

### ШАГ 2: Ответ пользователю (ВИДИМЫЙ)

```markdown
✅ Сверка с векторной картой:
- Прочитан: DOCUMENTATION_STRUCTURE_ANALYSIS.md
- Кластер: Модели и обучение (#4)
- Консолидированный отчёт: docs/models/MODELS_CONSOLIDATED_REPORT.md
- Связанные документы: TD010v2_DEPLOYMENT_COMPLETE.md (детали деплоя)

---

## Рекомендация: TD-010v2 (Qwen2.5-1.5B)

📌 **Источник:** `docs/models/MODELS_CONSOLIDATED_REPORT.md`, раздел "Final Verdict"

### Сравнительная таблица

| Метрика | TD-010v2 (1.5B) | TD-010v3 (3B) |
|---------|-----------------|---------------|
| **eval_loss** | **0.8591** ✅ | 1.0026 |
| **VRAM (training)** | **8 GB** ✅ | 12-14 GB |
| **VRAM (inference)** | **4 GB** ✅ | 6-8 GB |
| **Стабильность** | **Высокая** ✅ | Требует качественного dataset |
| **Статус** | **PRODUCTION** ✅ | RESEARCH 🔬 |

### 💡 Почему TD-010v2?

**1. Лучшие метрики:**
- eval_loss **0.8591** (на 14.3% ниже чем v3)
- Более стабильное обучение
- Меньше чувствителен к noise в dataset

**2. Экономия ресурсов:**
- VRAM training: **8 GB** vs 12-14 GB (экономия 4-6 GB)
- VRAM inference: **4 GB** vs 6-8 GB (экономия 2-4 GB)
- Можно обучать на RTX 3060 12GB

**3. Production-ready:**
- Успешно интегрирован в Desktop Client
- 4 фазы деплоя завершены
- Работает стабильно с qwen2.5:14b

### ⚠️ Когда НЕ использовать TD-010v2:

- Если нужен более сложный reasoning (тогда qwen2.5:14b)
- Если критична скорость inference (тогда nomic-embed-text для embeddings)

### 📦 Production deployment:

```bash
# Путь к модели
E:\WORLD_OLLAMA\models\qwen2-triz-merged\

# Tauri команда
invoke("load_adapter", { adapter_path: "..." })
```

---

💡 **Детальная информация:**

Для полного deployment workflow см.:  
`TD010v2_DEPLOYMENT_COMPLETE.md`

Для research findings TD-010v3 (adamw_8bit optimizer) см.:  
`TD010v3_RESEARCH_ANALYSIS.md`
```

---

## ✅ ПРИМЕР 3: Запрос про инфраструктуру

### Запрос пользователя:
```
"Как настроить CORTEX для production?"
```

---

### ШАГ 1: Автоматическая сверка (ВНУТРЕННИЙ ПРОЦЕСС)

```markdown
🔄 АКТИВАЦИЯ АГЕНТА:

ЭТАП 1: ЗАГРУЗКА КОНТЕКСТА
☑ 1. Читаю: E:\WORLD_OLLAMA\docs\project\DOCUMENTATION_STRUCTURE_ANALYSIS.md
☑ 2. Категория запроса: Инфраструктура / CORTEX → Кластер #5 (Инфраструктура)
☑ 3. Загружаю: docs/infrastructure/INFRASTRUCTURE_CONSOLIDATED_REPORT.md

ЭТАП 2: ПРОВЕРКА ЗАВИСИМОСТЕЙ
☑ 4. Проверяю граф: CORTEX Configuration описан в консолидированном отчёте
☑ 5. Актуальность: Консолидированный отчёт от 28.11.2025 — АКТУАЛЬНО

ЭТАП 3: ВЫПОЛНЕНИЕ
☑ 6. Извлекаю Plan C Baseline конфигурацию
☑ 7. Добавляю Security и RAG Quality из того же отчёта

ЭТАП 4: ОТЧЁТ
☑ 8. Готовлю ответ с code snippets из консолидированного отчёта
☑ 9. Использую маркеры 📌 КОД, 💡 RATIONALE
```

---

### ШАГ 2: Ответ пользователю (ВИДИМЫЙ)

```markdown
✅ Сверка с векторной картой:
- Прочитан: DOCUMENTATION_STRUCTURE_ANALYSIS.md
- Кластер: Инфраструктура (#5)
- Консолидированный отчёт: docs/infrastructure/INFRASTRUCTURE_CONSOLIDATED_REPORT.md
- Связанные документы: CORTEX_CONFIGURATION_REFERENCE.md (детальный референс)

---

## CORTEX Production Configuration (Plan C Baseline)

📌 **Источник:** `docs/infrastructure/INFRASTRUCTURE_CONSOLIDATED_REPORT.md`, раздел "CORTEX Configuration"

### 1. Основные параметры

```python
# lightrag_server.py

CORTEX_CONFIG = {
    # Entity extraction
    "entity_extract_max_gleaning": 2,  # vs 1 default
    
    # Context windows
    "addon_params": {
        "example_number": 3,  # vs 1 default
        "max_token_for_global_context": 6000,  # vs 4096 default
        "max_token_for_local_context": 6000,   # vs 4096 default
        "max_token_for_text_unit": 6000,       # vs 4096 default
    }
}
```

### 💡 Rationale для каждого параметра:

#### `entity_extract_max_gleaning: 2`
- **Зачем:** Улучшает качество извлечения сущностей (second pass)
- **Цена:** +30% времени индексации
- **Эффект:** Более точный граф зависимостей

#### `example_number: 3`
- **Зачем:** Больше примеров для few-shot промптинга
- **Цена:** +20% токенов в промпте
- **Эффект:** Лучше понимание контекста

#### `max_token_for_*_context: 6000`
- **Зачем:** Qwen2.5:14b поддерживает до 32K токенов, используем 6K
- **Цена:** Медленнее на ~15% (больше токенов)
- **Эффект:** Более полные ответы (меньше обрезания контекста)

---

### 2. Security (API Key Protection)

```python
# Middleware FastAPI
from fastapi import Security, HTTPException
from fastapi.security import APIKeyHeader

API_KEY_HEADER = APIKeyHeader(name="X-API-Key")

def verify_api_key(api_key: str = Security(API_KEY_HEADER)):
    if api_key != os.getenv("CORTEX_API_KEY"):
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return api_key

@app.post("/query")
async def query_endpoint(
    request: QueryRequest,
    api_key: str = Depends(verify_api_key)
):
    # ... query logic
```

**Установка ключа:**
```powershell
# В переменные окружения
$env:CORTEX_API_KEY = "your-secret-key-here"

# В Tauri (Desktop Client)
# Использует secure storage OS
```

---

### 3. RAG Quality Metrics (Production Baseline)

📌 **Источник:** 50-query validation dataset

| Метрика | Значение | Статус |
|---------|----------|--------|
| **Precision@5** | 0.184 (18.4%) | ⚠️ Needs improvement |
| **Recall@10** | 0.268 (26.8%) | ⚠️ Needs improvement |
| **Response Time** | 30-90s | ✅ Acceptable |

**Проблемы:**
- Top-k bias (фокус на популярных документах)
- Community-level failures (граф не полный)

**Рекомендации для улучшения:**
1. Увеличить `entity_extract_max_gleaning` до 3 (trade-off: время)
2. Добавить больше примеров в dataset (качество > количество)
3. Периодически переиндексировать граф

---

### 4. Orchestration (Запуск/Остановка)

```powershell
# Запуск всех сервисов
pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1

# Запуск без Neuro-Terminal (для Tauri dev)
pwsh E:\WORLD_OLLAMA\scripts\START_ALL.ps1 -SkipNeuroTerminal

# Остановка всех сервисов
pwsh E:\WORLD_OLLAMA\scripts\STOP_ALL.ps1

# Проверка статуса
pwsh E:\WORLD_OLLAMA\scripts\CHECK_STATUS.ps1
```

**Логика START_ALL.ps1:**
1. Проверить Ollama (порт 11434)
2. Запустить CORTEX (порт 8004) в новом окне
3. Запустить Neuro-Terminal (порт 8501) в новом окне (если не пропущен)
4. Вывести health check всех сервисов

---

### 5. Monitoring

```powershell
# VRAM (модели загружены?)
nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits

# Должно быть > 6000 MB (6 GB) для qwen2.5:14b + nomic-embed-text

# Логи CORTEX
Get-Content E:\WORLD_OLLAMA\services\lightrag\logs\cortex.log -Tail 50

# Статус индексации
$status = Get-Content E:\WORLD_OLLAMA\services\lightrag\data\kv_store_doc_status.json | ConvertFrom-Json
$status.PSObject.Properties.Name.Count  # Количество индексированных документов
```

---

💡 **Детальная информация:**

Для полного референса всех параметров см.:  
`CORTEX_CONFIGURATION_REFERENCE.md`

Для security implementation details см.:  
`SECURE_ENCLAVE_REPORT.md`

Для RAG quality analysis см.:  
`docs/reports/RAG_QUALITY_REPORT.md`
```

---

## ❌ АНТИПАТТЕРН: Ответ БЕЗ сверки

### Запрос пользователя:
```
"Расскажи про Command DSL"
```

---

### ❌ НЕПРАВИЛЬНЫЙ ОТВЕТ (без сверки):

```markdown
Command DSL — это система команд для управления контекстом в чате.

Основные команды:
- /clear — очистить чат
- /context — показать контекст
- /model — сменить модель

Файл: CommandsPanel.svelte

ПРОБЛЕМЫ:
❌ Не загружен DOCUMENTATION_STRUCTURE_ANALYSIS.md
❌ Не прочитан консолидированный отчёт
❌ Нет указания источника
❌ Информация может быть неполной/устаревшей
❌ Не проверены связанные документы
```

---

### ✅ ПРАВИЛЬНЫЙ ОТВЕТ (с сверкой):

```markdown
✅ Сверка с векторной картой:
- Прочитан: DOCUMENTATION_STRUCTURE_ANALYSIS.md
- Кластер: Desktop Client Tasks (#3)
- Консолидированный отчёт: docs/tasks/TASKS_CONSOLIDATED_REPORT.md
- Связанные документы: client/TASK_8_COMPLETION_REPORT.md (детали DSL)

---

## TASK 8: Commands Panel (Command DSL)

📌 **Источник:** `docs/tasks/TASKS_CONSOLIDATED_REPORT.md`, раздел TASK 8

### Цель
Реализация мини-языка команд для управления контекстом чата без кликов в UI.

### Команды DSL

**1. Управление контекстом:**
- `/clear` — очистить историю чата
- `/reset` — сбросить контекст (history + system prompt)
- `/context` — показать текущий контекст

**2. Переключение моделей:**
- `/model <name>` — переключить модель (например: /model qwen2.5:7b)
- `/models` — список доступных моделей

**3. Управление профилями:**
- `/profile <name>` — переключить профиль агента
- `/profiles` — список профилей

**4. Отладка:**
- `/debug` — включить/выключить debug режим
- `/tokens` — показать количество токенов в контексте

### Технические детали

**Парсинг команд (TypeScript):**
```typescript
function parseCommand(input: string): Command | null {
  if (!input.startsWith('/')) return null;
  
  const [cmd, ...args] = input.slice(1).split(' ');
  return { command: cmd, arguments: args };
}
```

**Tauri команды (Rust):**
- `execute_command(cmd: String)` → `Result<String, String>`

**Компонент:** `client/src/lib/CommandsPanel.svelte` (~180 строк)

### Статус
✅ **ЗАВЕРШЁН** (релиз v0.1.0)

---

💡 **Детальная информация:**

Для полной грамматики DSL и примеров см.:  
`client/TASK_8_COMPLETION_REPORT.md`

Связанные компоненты:
- TASK 5: Settings Panel (профили агентов)
- TASK 4: System Status Panel (debug info)
```

---

## 📊 СРАВНЕНИЕ: До vs После

| Аспект | ❌ БЕЗ сверки | ✅ С сверкой |
|--------|---------------|--------------|
| **Загрузка векторной карты** | НЕТ | ✅ Автоматически |
| **Определение кластера** | НЕТ | ✅ Кластер #3/4/5 |
| **Консолидированный отчёт** | НЕТ | ✅ ВСЕГДА первым |
| **Проверка зависимостей** | НЕТ | ✅ Граф проверен |
| **Указание источника** | НЕТ | ✅ Файл + раздел |
| **Актуальность данных** | ❓ Неизвестно | ✅ Подтверждено |
| **Полнота информации** | ⚠️ Может быть неполной | ✅ Все связанные документы |
| **Прозрачность** | ❌ Нет пометок | ✅ 📌/💡/⚠️ маркеры |
| **Проверяемость** | ❌ Не проверить | ✅ Чеклист 8 пунктов |

---

## 🎯 ИТОГИ

### Что гарантирует новая система:

1. ✅ **Агент ВСЕГДА использует актуальные данные** (консолидированные отчёты от 28.11.2025)
2. ✅ **Агент НЕ ПРОПУСКАЕТ связанные документы** (граф зависимостей)
3. ✅ **Агент УКАЗЫВАЕТ источник каждого факта** (прозрачность)
4. ✅ **Агент ПРИЗНАЁТСЯ если данных нет** (честное незнание)
5. ✅ **Пользователь ВИДИТ что агент проверился** ("✅ Сверка с векторной картой")

### Как это работает:

```
Запрос пользователя
       ↓
ЭТАП 1: Читаю DOCUMENTATION_STRUCTURE_ANALYSIS.md
       ↓
ЭТАП 2: Определяю кластер (1-6)
       ↓
ЭТАП 3: Загружаю консолидированный отчёт
       ↓
ЭТАП 4: Проверяю граф зависимостей
       ↓
ЭТАП 5: Извлекаю информацию с источниками
       ↓
ОТВЕТ: "✅ Сверка с векторной картой: ..." + факты с маркерами
```

---

**Дата создания демо:** 28 ноября 2025 г.  
**Версия протокола:** 1.0  
**Статус:** ✅ АКТИВЕН для всех агентов WORLD_OLLAMA

_Эта демонстрация показывает правильную работу агента с новой системой автоматической сверки._
