# TASK 42 — IMPLEMENTATION PLAN  
OLLAMA TRAINING UI (ORDER 42)

Статус: PLANNING  
Версия документа: v1.0  
Ответственный: CODEX Agent + User (review)

---

## 1. Контекст и цель

### 1.1. Контекст

Текущее состояние:

- Backend:
  - `training_manager.rs::run_training()` — уже реализован публичный API обучения.
  - `start_training_job` (Tauri команда) использует `run_training`.
  - `flow_manager.rs::cmd_train()` — TRAIN интегрирован во Flows.
  - Есть flows:
    - `train_default.json`
    - `index_and_train.json`

- UI:
  - `TrainingPanel.svelte` — отображает статусы и прогресс через PULSE v1 snapshot.
  - Flows:
    - `FlowsPanel.svelte` — умеет запускать flows и показывать историю (после ORDER 38).
  - PULSE v2 пока **только на уровне дизайна** (ORDER 41).

Проблема:

- Пользователь **не может "по-человечески" запустить обучение Ollama из UI**:
  - нет нормального выбора профиля/датасета;
  - часть flows (train_default / index_and_train) блокируются UX-проблемами;
  - нет явной точки входа: "я хочу обучить модель".

### 1.2. Цель TASK 42

Сделать так, чтобы:

> *Пользователь, не трогая CLI и конфиги, может из интерфейса:*
> 1. Выбрать профиль и датасет.
> 2. Нажать "Start Training".
> 3. Видеть понятный прогресс и результат обучения.
> 4. Повторно посмотреть историю запусков.

При этом:

- мы **НЕ ломаем** текущую архитектуру (Flows, PULSE v1);
- PULSE v2 остаётся на горизонте, но ORDER 42 не зависит от его реализации.

---

## 2. Scope (в рамках и вне рамок)

### 2.1. В рамках TASK 42

- TrainingPanel:
  - UX выбора профиля/датасета.
  - Запуск обучения через Tauri-команду.
  - Мини-история последних запусков.
- Flows:
  - Рабочий TRAIN-шаг в `train_default.json`.
  - Базовая связка TRAIN шагов с TrainingPanel (по `training_id`).
- Документация:
  - Обновление CHANGELOG/README/PROJECT_STATUS.

### 2.2. Вне рамок TASK 42

- Реализация PULSE v2 (events, advanced timeline).
- Продвинутые метрики (VRAM, GPU, системные показатели).
- Автоматический выбор датасета/профиля на основе эвристик/AI.
- Глубокая интеграция Training с автоматическими Flows (кроме минимального happy path).

---

## 3. Зависимости

### 3.1. Обязательные

- ORDER 39 — v0.3.0-alpha Release Gate:
  - Статус: ✅ COMPLETE (CONDITIONAL PASS).
- ORDER 40 — BUGFIX PACK v0.3.1:
  - Желательно устранить хотя бы HIGH-баги:
    - `index_manager.rs` path resolution.
    - GitPanel CWD bug.
  - **Важно:** TRAIN-путь по возможности должен быть в рабочем состоянии.

### 3.2. Желательные

- Завершённая документация PULSE v2 (ORDER 41):
  - Даёт понимание, как позже эволюционировать TrainingPanel.
  - Но **не блокирует** запуск базового UI обучения.

---

## 4. Разбиение на команды ORDER 42

TASK 42 реализует ORDER 42 через 5 логических команд:

1. **КОМАНДА 42.1 — Training Profiles UX**
2. **КОМАНДА 42.2 — E2E TRAIN через TrainingPanel**
3. **КОМАНДА 42.3 — TRAIN в Flows (happy path)**
4. **КОМАНДА 42.4 — Мини-History для Training**
5. **КОМАНДА 42.5 — Документация и Release Note**

Ниже — детализация.

---

## 5. КОМАНДА 42.1 — TRAINING PROFILES UX

**Цель:**  
Сделать выбор профиля/датасета первым классным гражданином TrainingPanel.

**Файлы:**

- `client/src/lib/components/TrainingPanel.svelte`
- (опционально) `client/src/lib/stores/trainingProfiles.ts` или JSON-конфиг в `config/`.

**Шаги:**

1. Определить модель данных для профилей и датасетов (на фронте):

   ```ts
   type TrainingProfile = {
     id: string;
     name: string;
     description?: string;
     default_epochs: number;
   };

   type TrainingDataset = {
     id: string;
     name: string;
     description?: string;
   };
   ```

2. Добавить в TrainingPanel:
   - выпадающий список / селектор профилей;
   - выпадающий список / селектор датасетов;
   - поле для количества эпох (по умолчанию `default_epochs` выбранного профиля).

3. Условия активации кнопки "Start Training":
   - профиль выбран;
   - датасет выбран;
   - epochs > 0 (и в разумных пределах).

4. Для v0.3.x разрешено:
   - использовать простую in-code конфигурацию (массивы профилей/датасетов);
   - вынести это в файлы/конфиг — отдельный будущий ордер.

5. **Definition of Done:**
   - При открытии TrainingPanel пользователь видит:
     - минимум 1–2 доступных профиля;
     - минимум 1–2 доступных датасета.
   - Кнопка "Start Training" неактивна, пока выбор не сделан.
   - Вёрстка не ломает существующий UI.

---

## 6. КОМАНДА 42.2 — E2E TRAIN ЧЕРЕЗ TRAININGPANEL (БЕЗ FLOWS)

**Цель:**  
Сделать "честный" путь:
профиль + датасет → старт → прогресс → результат.

**Файлы:**

- `TrainingPanel.svelte`
- `client/src-tauri/src/commands.rs` (если нужны изменения сигнатуры команды)
- `client/src-tauri/src/training_manager.rs` (используется `run_training()`)

**Шаги:**

1. В TrainingPanel:
   - собрать конфиг:
     ```ts
     {
       profileId: string;
       datasetId: string;
       epochs: number;
       // возможно, дополнительные поля: mode, lr, batch_size — опционально
     }
     ```
   - дернуть Tauri-команду `start_training_job` с этими параметрами.

2. Убедиться, что:
   - `run_training()` принимает и корректно использует эти параметры;
   - при ошибках (например, нет такого профиля или датасета) возвращается понятное сообщение.

3. Отобразить:
   - статус: `idle → preparing → running → done/error`;
   - прогресс по эпохам (как в PULSE v1: `epoch / total_epochs`);
   - loss (если есть).

4. Обработка ошибок:
   - показать понятный error-banner при:
     - невозможности запуска (невалидные параметры, проблема с backend);
     - ошибках во время обучения.

5. **Definition of Done:**
   - TrainingPanel позволяет:
     - выбрать профиль и датасет,
     - нажать "Start Training",
     - увидеть живой прогресс и финальный результат без участия Flows.
   - В случае ошибки пользователь видит читаемое сообщение.

---

## 7. КОМАНДА 42.3 — TRAIN В FLOWS (HAPPY PATH)

**Цель:**  
Сделать рабочий TRAIN-шаг в Flows как часть сценария.

**Файлы:**

- `client/src-tauri/src/flow_manager.rs`
- `automation/flows/train_default.json`
- `automation/flows/index_and_train.json` (опционально)
- `client/src/lib/components/FlowsPanel.svelte`

**Шаги:**

1. Проверить `cmd_train()` в `flow_manager.rs`:
   - он должен:
     - вызывать тот же backend, что и TrainingPanel (либо `start_training_job`, либо `run_training()` через internal API);
     - получать `training_id` (если уже реализовано);
     - логировать связь `flow_run_id + training_id`.

2. Актуализировать `train_default.json`:
   - шаг TRAIN должен иметь корректные `args`:
     - id профиля;
     - id датасета;
     - epochs (можно использовать профиль `default_epochs`).

3. В `FlowsPanel.svelte`:
   - в деталях FlowRun для TRAIN-шагов:
     - показать статус TRAIN;
     - если есть `training_id`:
       - дать ссылку "Открыть в TrainingPanel" / "Показать детали".

4. **Definition of Done:**
   - Запуск `train_default` через Flows:
     - стартует настоящее обучение;
     - статус TRAIN-шагов корректно отображается в FlowsPanel;
     - при наличии `training_id` есть возможность перейти к подробностям (или хотя бы его увидеть).

---

## 8. КОМАНДА 42.4 — МИНИ-HISTORY ДЛЯ TRAINING

**Цель:**  
Дать пользователю возможность увидеть хотя бы последние несколько запусков.

**Файлы:**

- Rust:
  - `training_manager.rs`
  - (опционально) новый модуль `training_history.rs`
- Логи:
  - `logs/training/training_runs.jsonl` (пример)
- UI:
  - `TrainingPanel.svelte` (history секция)
  - (опционально) отдельный компонент `TrainingHistory.svelte`

**Шаги:**

1. На стороне Rust:
   - определить структуру `TrainingRunSummary`, например:
     ```rust
     pub struct TrainingRunSummary {
         pub training_id: String,
         pub profile_id: String,
         pub dataset_id: String,
         pub status: String,
         pub started_at: i64,
         pub finished_at: Option<i64>,
         pub final_loss: Option<f64>,
     }
     ```
   - при завершении обучения (done/error/cancelled) писать запись в JSONL лог.

2. Реализовать Tauri-команду:
   - `get_training_history(limit: u32) -> Vec<TrainingRunSummary>`.

3. В TrainingPanel:
   - добавить секцию "History":
     - запрос истории через `invoke('get_training_history', { limit: 10 })`;
     - вывод таблички/списка:
       - время,
       - профиль,
       - датасет,
       - статус,
       - final loss.

4. **Definition of Done:**
   - После нескольких запусков обучения пользователь видит список минимум последних 3–10 запусков с ключевыми полями.
   - При отсутствии истории показывается дружелюбное пустое состояние ("Вы ещё не запускали обучение").

---

## 9. КОМАНДА 42.5 — ДОКУМЕНТАЦИЯ И RELEASE NOTE

**Цель:**  
Зафиксировать новую способность системы и сделать её видимой для пользователя и будущих разработчиков.

**Файлы:**

- `CHANGELOG_v0.3.x.md` или `CHANGELOG_v0.4.0.md`
- `README.md`
- `PROJECT_STATUS_SNAPSHOT_*.md`
- (опционально) отдельный `TASK_42_COMPLETION_REPORT.md`

**Шаги:**

1. CHANGELOG:
   - добавить секцию:
     - "Добавлено: Ollama Training via UI (TrainingPanel + Flows)".

2. README:
   - раздел "🎓 Обучение моделей (Training Panel)":
     - где искать TrainingPanel;
     - как выбрать профиль/датасет;
     - как запускать;
     - какие ограничения (нет PULSE v2, базовые метрики).

3. PROJECT_STATUS_SNAPSHOT:
   - обновить статус:
     - ORDER 42 — COMPLETE;
     - "Training via UI: ENABLED".

4. TASK_42_COMPLETION_REPORT.md (рекомендуется):
   - зафиксировать:
     - что сделано (по командам 42.1–42.5);
     - какие Known Issues остаются;
     - ссылки на изменённые файлы.

5. **Definition of Done:**
   - Документация обновлена, статус проекта отражает появление Training UI.
   - Любой новый разработчик/пользователь может по README понять:
     - как запустить обучение через UI,
     - где лежат конфиги,
     - какие есть ограничения.

---

## 10. Оценка трудозатрат (грубая)

- **КОМАНДА 42.1** — 1.5–2 часа (UX + логика выбора).
- **КОМАНДА 42.2** — 2–3 часа (end-to-end запуск, обработка ошибок).
- **КОМАНДА 42.3** — 1.5–2 часа (Flows интеграция TRAIN).
- **КОМАНДА 42.4** — 2–3 часа (history backend + UI).
- **КОМАНДА 42.5** — 1 час (документация, отчёт).

**Итого:** ориентировочно 8–11 часов чистой разработки/документации.

---

## 11. Риски и mitigation

### Основные риски

1. **Сломанный TrainingPanel (регрессии)**
   - Риск: новые элементы UX ломают существующий layout/поведение.
   - Mitigation:
     - пошаговые изменения, snapshot-branch;
     - ручной smoke-test TrainingPanel после каждого крупного изменения.

2. **Несоответствие профиля/датасета backend-логике**
   - Риск: UI шлёт id, которые не понимает backend.
   - Mitigation:
     - временно использовать жёстко согласованные ID;
     - описать это явно в коде и документации.

3. **Flow/Training дублирующая логика**
   - Риск: TRAIN из TrainingPanel и TRAIN в Flows расходятся по поведению.
   - Mitigation:
     - максимально использовать один и тот же backend API (`run_training`);
     - описать контракт в одном месте (TRAINING API).

---

## 12. Definition of Done для TASK 42

TASK 42 считается полностью завершённым, когда:

- ✅ **TrainingPanel:**
  - ✅ Позволяет выбрать профиль и датасет.
  - ✅ Умеет запускать обучение и показывать статус/прогресс.

- ✅ **TRAIN:**
  - ✅ Работает из TrainingPanel (без Flows).
  - ✅ Работает как шаг внутри хотя бы одного Flow (`train_default`).

- ✅ **History:**
  - ✅ Показывается минимальная история запусков Training.

- ✅ **Документация:**
  - ✅ CHANGELOG/README/PROJECT_STATUS обновлены.
  - ✅ TASK_42_COMPLETION_REPORT.md создан.

После этого можно официально говорить:

> "Обучение Ollama подключено через интерфейс (v0.3.x/v0.4.x baseline)".
