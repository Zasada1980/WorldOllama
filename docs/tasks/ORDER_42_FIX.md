# ORDER 42-FIX — Восстановление и Верификация

**Дата создания:** 01.12.2025 00:18  
**Приоритет:** 🔴 CRITICAL  
**Причина:** Рассинхронизация документации и кода после multiple git checkout

---

## 🚨 ПРОБЛЕМА

**Что случилось:**
1. КОМАНДА 42.1 была реализована в коде
2. При попытках реализации 42.2 файл TrainingPanel.svelte ломался
3. Выполнялся `git checkout client/src/lib/components/TrainingPanel.svelte`
4. **ВСЕ изменения 42.1 откачены**, но документы остались со статусом ✅

**Текущее состояние кода (проверено grep):**
- ❌ `canStartTraining` — НЕТ
- ❌ `selectedProfile` computed — НЕТ  
- ❌ `selectedDataset` computed — НЕТ
- ❌ Warning badges — НЕТ
- ❌ Hint badges — НЕТ
- ❌ Selection info cards — НЕТ
- ✅ `startTrainingJob` API — ЕСТЬ (client.ts)

---

## 📋 КОМАНДЫ ВОССТАНОВЛЕНИЯ

### КОМАНДА 42-FIX.1 — Честная Рес

инхронизация Документов

**Цель:** Привести ORDER_42_TRACKING.md в соответствие с реальностью

**Действия:**
1. Обновить `ORDER_42_TRACKING.md`:
   ```markdown
   КОМАНДА 42.1: 🟡 CODE ROLLED BACK (needs reapply)
   ```
2. Изменить общий прогресс ORDER 42: **0% → 5%** (только client.ts готов)
3. Добавить в TRACKING секцию "Known Issues":
   ```markdown
   ## ⚠️ KNOWN ISSUES
   - TrainingPanel.svelte: Changes from 42.1 were rolled back by git checkout
   - Need manual reapply of all 42.1 UI enhancements
   ```

---

### КОМАНДА 42-FIX.2 — Ручное Восстановление 42.1

**Цель:** Восстановить код 42.1 **БЕЗ** использования больших replace_file_content

**Метод:** Использовать `multi_replace_file_content` с МАЛЕНЬКИМИ чанками (по 10-15 строк каждый)

**Файл:** `client/src/lib/components/TrainingPanel.svelte`

**Изменения (разбить на 6 маленьких chunks):**

#### Chunk 1: Добавить computed properties (после строки 190)
```typescript
// ORDER 42.1: Computed - selected profile details
$: selectedProfile = profiles.find((p) => p.id === selectedProfileId);
$: selectedDataset = datasets.find((d) => d.path === selectedDatasetPath);
```

#### Chunk 2: Добавить auto-sync epochs (после Chunk 1)
```typescript
// ORDER 42.1: Auto-update epochs from profile's recommended_epochs
$: if (selectedProfile && selectedProfile.recommended_epochs > 0) {
  epochs = selectedProfile.recommended_epochs;
}
```

#### Chunk 3: Добавить canStartTraining (после Chunk 2)
```typescript
// ORDER 42.1: Validation - disable button if missing profile/dataset or invalid epochs
$: canStartTraining = 
  selectedProfileId && 
  selectedDatasetPath && 
  epochs >= 1 && 
  epochs <= 10 && 
  status?.status !== "running" && 
  !isStarting;
```

#### Chunk 4-6: UI elements (селекторы, badges, info cards)
*(См. ORDER_42_1_COMPLETION_REPORT.md для деталей)*

**ВАЖНО:**
- Каждый chunk — отдельный вызов multi_replace с точным StartLine/EndLine
- После каждого chunk — проверка компиляции `npm run check`
- Если ломается — откат ТОЛЬКО этого chunk и анализ

---

### КОМАНДА 42-FIX.3 — UI Verification

**После восстановления кода:**

1. Запустить dev server: `npm run tauri dev`
2. Пройти по checklist из `ORDER_42_1_VERIFY.md`
3. Зафиксировать результаты в новом файле:
   ```
   docs/tasks/ORDER_42_1_REAPPLY_VERIFICATION.md
   ```

---

### КОМАНДА 42-FIX.4 — Продолжение 42.2

**ТОЛЬКО после подтверждения 42.1:**

1. Убедиться, что `startTrainingJob` есть в client.ts (✅ уже есть)
2. Обновить функцию `startTraining()` в TrainingPanel
3. **Метод:** Использовать МАЛЕНЬКИЙ chunk (только тело функции, ~30 строк)
4. Проверить компиляцию после изменения

---

## 🎯 КРИТЕРИИ УСПЕХА

✅ ORDER_42_TRACKING.md отражает реальное состояние кода  
✅ TrainingPanel.svelte содержит ВСЕ фичи 42.1  
✅ `npm run check` проходит без ошибок  
✅ UI verification показывает все элементы 42.1  
✅ 42.2 реализована через startTrainingJob (не DSL)

---

## 📊 НОВЫЙ REALISTIC ПРОГРЕСС

```
КОМАНДА 42.1: ░░░░░░░░░░░░░░░░░░░░  0% → 🟡 NEEDS REAPPLY
КОМАНДА 42.2: ░░░░░░░░░░░░░░░░░░░░  5% (client.ts done)
КОМАНДА 42.3: ░░░░░░░░░░░░░░░░░░░░  0%
КОМАНДА 42.4: ░░░░░░░░░░░░░░░░░░░░  0%
КОМАНДА 42.5: ░░░░░░░░░░░░░░░░░░░░  0%

ОБЩИЙ ПРОГРЕСС: ░░░░░░░░░░░░░░░░░░░░  1% (только API метод)
```

---

## 💡 LESSONS LEARNED

1. **Git checkout убивает ВСЁ** — не только текущие правки, но и предыдущие незакоммиченные
2. **Большие replace (>50 строк)** в Svelte файлах — ненадёжны
3. **Нужна атомарность:** каждый chunk — отдельный этап с проверкой
4. **Документы ≠ Код:** всегда верифицировать через grep/view после изменений

---

**Следующий шаг:** Выполнить КОМАНДУ 42-FIX.1 (обновить tracking) и решить:
- Восстанавливать 42.1 немедленно (FIX.2)
- Или отложить ORDER 42 до лучших времён
