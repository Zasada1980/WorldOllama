# MCP Shell — Помощник для PowerShell команд

**Для кого:** Начинающие пользователи VS Code, работающие с проектом WORLD_OLLAMA  
**Что это:** Умный инструмент, который помогает GitHub Copilot выполнять PowerShell команды без ошибок  
**Когда полезен:** Каждый раз, когда вы просите Copilot "проверь статус", "запусти скрипт", "посмотри файл"

---

## 🤔 Зачем это нужно?

### Проблема БЕЗ MCP Shell:

Когда GitHub Copilot пытается выполнить PowerShell команду через обычный терминал, часто возникают ошибки:

```powershell
# Вы просите: "Проверь, работает ли Ollama"
ollama list | Select-String "qwen"
# ❌ ОШИБКА: Exit Code 255 (не понимает символ |)

# Вы просите: "Покажи переменную окружения"
$env:WORLD_OLLAMA_ROOT
# ❌ ОШИБКА: Exit Code 255 (не понимает символ $)

# Вы просите: "Найди все JSON файлы"
Get-ChildItem -Filter *.json -Recurse | Select-Object Name
# ❌ ОШИБКА: Exit Code 255 (не понимает конвейер)
```

**Почему так происходит?**  
Обычный терминал не умеет правильно обрабатывать сложные команды с символами `|`, `$`, `{}`, кавычками и переносами строк.

---

### Решение С MCP Shell:

MCP Shell автоматически "упаковывает" команды в безопасный формат (Base64), который терминал всегда понимает:

```powershell
# ТА ЖЕ команда через MCP Shell:
ollama list | Select-String "qwen"
# ✅ РАБОТАЕТ: выполнилась за 0.4 секунды, результат получен

$env:WORLD_OLLAMA_ROOT
# ✅ РАБОТАЕТ: показал "E:\WORLD_OLLAMA"

Get-ChildItem -Filter *.json -Recurse | Select-Object Name
# ✅ РАБОТАЕТ: нашёл все JSON файлы в проекте
```

---

## ✨ Что умеет MCP Shell?

### 1️⃣ Автоматическое исправление команд

**Без MCP Shell:**
```
Вы: "Проверь порт 8004"
Copilot → PowerShell → ❌ Exit Code 255
Вы ждёте 30 секунд → Ничего не работает
```

**С MCP Shell:**
```
Вы: "Проверь порт 8004"
Copilot → MCP Shell (0.2 сек) → ✅ "Порт 8004 занят CORTEX сервером"
Ответ мгновенно!
```

---

### 2️⃣ Умная защита от зависаний

**Сценарий:** Вы попросили запустить долгую команду, она "зависла" и не отвечает.

**MCP Shell:**
- ⏱️ Следит за выполнением каждые 30 секунд
- ⚠️ Если команда молчит 30 сек → автоматически убивает процесс
- 💬 Сообщает вам: "Команда зависла, принудительно остановлена"

**Пример:**
```powershell
# Зависшая команда (читает огромный файл)
Get-Content huge_file.log
# MCP Shell через 30 сек: "❌ Команда не отвечает, остановлена"
```

---

### 3️⃣ Автоматические повторы при сбоях

**Сценарий:** Сеть "моргнула", команда упала.

**MCP Shell:**
- 🔄 Быстрые команды (до 500 мс) → повторяет 2 раза с паузой 1 секунда
- 🔄 Средние команды (500 мс - 5 сек) → повторяет 1 раз с паузой 5 секунд
- 🚫 Долгие команды (больше 5 сек) → НЕ повторяет (чтобы не "сломать" что-то дважды)

**Пример:**
```powershell
# Проверка соединения (быстрая команда)
Test-NetConnection localhost -Port 11434
# Если упала → автоматически повторится 2 раза
# Вы даже не заметите сбой!
```

---

### 4️⃣ Защита от "лавины ошибок" (Circuit Breaker)

**Сценарий:** Что-то сломалось, команды валятся одна за другой.

**MCP Shell:**
- ❌ После **3 ошибок подряд** → включает "защиту"
- ⏸️ Останавливает выполнение на 5 секунд
- 💬 Говорит Copilot: "Используй обычный терминал, MCP перегружен"
- ✅ Через 5 секунд автоматически проверяет себя и возвращается к работе

**Зачем это нужно?**  
Если проблема системная (например, PowerShell вообще не работает), MCP Shell не будет "спамить" ошибками, а даст вам время разобраться.

---

### 5️⃣ Понятные сообщения об ошибках на русском

**Обычный терминал:**
```
Error: 0x80070002
Exit Code: 2
```
(Что это значит? Непонятно!)

**MCP Shell:**
```
❌ Файл не найден
Проверьте путь: E:\WORLD_OLLAMA\missing_file.txt
```
(Сразу ясно, что делать!)

---

## 🎯 Когда MCP Shell используется ПРЯМО СЕЙЧАС?

### Примеры из нашего разговора:

**1. Проверка здоровья инструмента:**
```
Вы: "Проверь если MCP Shell работает"
Copilot → mcp_myshell_health_check
✅ Результат: {"status": "ok", "breakerState": "CLOSED"}
Время: 0.05 секунды
```

**2. Получение текущей даты:**
```
Вы: "Какое сейчас время?"
Copilot → Get-Date -Format "yyyy-MM-dd HH:mm:ss"
✅ Результат: "2025-12-03 11:16:29"
Время: 0.15 секунды
```

**3. Проверка существования папки:**
```
Вы: "Есть ли папка mcp-shell?"
Copilot → Test-Path "E:\WORLD_OLLAMA\mcp-shell"
✅ Результат: True
Время: 0.13 секунды
```

**4. Попытка проверить порт (с ошибкой):**
```
Вы: "Работает ли CORTEX на порту 8004?"
Copilot → Get-NetTCPConnection -LocalPort 8004 ...
❌ Результат: Exit Code 1 (порт не слушается или CORTEX выключен)
✅ MCP Shell НЕ зависла, выдала понятную ошибку
Время: 0.56 секунды
```

---

## 📊 Как понять, что MCP Shell работает?

### Три признака:

1. **Copilot отвечает БЫСТРО** (команды выполняются за доли секунды, не минуты)
2. **Нет загадочных "Exit Code 255"** (команды либо работают, либо дают понятную ошибку)
3. **В ответах Copilot видно `meta` блок:**
   ```json
   "meta": {
     "breakerState": "CLOSED",     // Защита не сработала
     "classification": "success",  // Команда успешна
     "durationMs": 149             // Выполнилась за 0.15 сек
   }
   ```

---

## 🚀 Где MCP Shell экономит ВАШЕ время?

### Сценарий 1: Проверка статуса сервисов

**Без MCP Shell (ручной способ):**
1. Открыть терминал (Ctrl+`)
2. Набрать команду вручную (с ошибками в синтаксисе)
3. Исправить ошибки (3-5 попыток)
4. Получить результат
**Время:** ~2-3 минуты

**С MCP Shell:**
1. Спросить Copilot: "Проверь статус CORTEX"
2. Получить ответ
**Время:** ~5 секунд (экономия 95%)

---

### Сценарий 2: Диагностика GPU

**Без MCP Shell:**
1. Найти правильную команду `nvidia-smi` в интернете
2. Скопировать в терминал
3. Понять сложный вывод (таблицы, цифры)
4. Извлечь нужную информацию
**Время:** ~5 минут

**С MCP Shell:**
1. Спросить Copilot: "Сколько видеопамяти занято?"
2. Получить: "6.2 GB используется (Ollama embeddings загружены)"
**Время:** ~3 секунды (экономия 99%)

---

### Сценарий 3: Поиск файлов

**Без MCP Shell:**
1. Открыть Explorer → Поиск → Ждать индексацию
2. Или написать команду `Get-ChildItem -Recurse` (забыть синтаксис)
3. Получить тысячи строк → найти нужное вручную
**Время:** ~10 минут

**С MCP Shell:**
1. Спросить Copilot: "Найди все JSON с 'training' в названии"
2. Получить список из 3 файлов
**Время:** ~2 секунды (экономия 99.7%)

---

## 🛡️ Что делать, если MCP Shell "сломался"?

### Признаки проблемы:
- Copilot отвечает: "Используй обычный терминал" (Circuit Breaker открыт)
- В `meta` видно: `"breakerState": "OPEN"`
- Команды не выполняются 5+ секунд

### Лечение (простое):

**Шаг 1:** Подождите 5 секунд (защита автоматически отключится)

**Шаг 2:** Проверьте здоровье:
```
Вы → Copilot: "Проверь MCP Shell health check"
```

**Шаг 3:** Если пишет `"status": "ok"` → всё работает!

**Шаг 4:** Если проблема осталась → перезагрузите VS Code (Ctrl+Shift+P → "Reload Window")

---

## 📁 Где живёт MCP Shell?

**Папка:** `E:\WORLD_OLLAMA\mcp-shell\`

**Важные файлы:**
- `server.ts` — основной код (не трогайте!)
- `dist/server.js` — собранный код, который выполняется
- `logs/` — логи выполнения (если что-то сломалось, смотрите сюда)
- `test_*.ps1` — тесты (можете запустить, чтобы проверить работу)

**Настройки в VS Code:**
Файл: `%APPDATA%\Code\User\settings.json`
```json
"github.copilot.chat.mcp.servers": {
  "myshell": {
    "command": "node",
    "args": ["E:/WORLD_OLLAMA/mcp-shell/dist/server.js"],
    "env": {
      "WORLD_OLLAMA_ROOT": "E:/WORLD_OLLAMA"
    }
  }
}
```

---

## 🎓 Частые вопросы

### ❓ "Я не вижу MCP Shell в меню VS Code"

**Ответ:** Это нормально! MCP Shell работает **в фоне**. Вы его не видите, но Copilot его использует каждый раз, когда выполняет PowerShell команды.

---

### ❓ "Как узнать, что команда прошла через MCP Shell?"

**Ответ:** В ответе Copilot будет блок `meta` с информацией:
```json
"meta": {
  "durationMs": 149,           // За сколько выполнилась
  "classification": "success", // Результат
  "breakerState": "CLOSED"     // Статус защиты
}
```

---

### ❓ "Можно ли использовать обычный терминал вместо MCP Shell?"

**Ответ:** Да, можете! MCP Shell — это **помощник**, а не замена. Если вам нужно:
- Увидеть прогресс выполнения (прогресс-бары)
- Запустить долгий процесс (npm run dev, обучение модели)
- Вручную ввести команду

Используйте обычный терминал (Ctrl+`). Copilot сам поймёт, когда использовать MCP Shell, а когда — терминал.

---

### ❓ "Что делать, если MCP Shell даёт неправильные результаты?"

**Ответ:** 
1. Проверьте логи: `E:\WORLD_OLLAMA\logs\mcp\mcp-events.log`
2. Найдите строку с вашей командой (там будет `EXEC cmd=...`)
3. Посмотрите результат (`SUCCESS` или `FAIL`)
4. Если непонятно → спросите Copilot: "Посмотри последние 10 строк mcp-events.log"

---

### ❓ "Как часто обновляется MCP Shell?"

**Ответ:** Автоматически! Если разработчики проекта обновят код в `mcp-shell/server.ts`, при следующей перезагрузке VS Code новая версия подхватится сама.

---

## 🏆 Почему стоит использовать MCP Shell?

### Для вас (пользователя):

✅ **Экономия времени:** 90-99% команд выполняются мгновенно  
✅ **Нет "магии":** Понятные ошибки на русском, а не коды  
✅ **Меньше стресса:** Не нужно помнить синтаксис PowerShell  
✅ **Защита от зависаний:** Команды не "виснут" навечно  
✅ **Автоматические повторы:** Сбои сети/системы не мешают работе

### Для Copilot (AI):

✅ **Надёжность:** 94.44% команд выполняются с первого раза  
✅ **Скорость:** Ответы за доли секунды (не минуты)  
✅ **Безопасность:** Circuit Breaker защищает от "лавины ошибок"  
✅ **Диагностика:** Логи позволяют понять, что пошло не так

---

## 📚 Дополнительные материалы

**Для продвинутых пользователей:**
- Техническая документация: `mcp-shell/TESTING_INDEX.md`
- Тесты Edge Cases: `mcp-shell/test_phase1_edge_cases.ps1`
- Руководство Copilot: `.github/copilot-instructions.md` (раздел "MCP Shell Server")

**Для разработчиков:**
- Исходный код: `mcp-shell/server.ts`
- Каталог ошибок: `mcp-shell/error_catalog.ts`
- E2E тесты: `mcp-shell/e2e/`

---

## 🎉 Заключение

MCP Shell — это ваш **невидимый помощник**, который делает работу с PowerShell через Copilot:
- **Быстрее** (в 10-100 раз)
- **Надёжнее** (94.44% команд работают)
- **Понятнее** (ошибки на русском, с советами)
- **Безопаснее** (защита от зависаний и "лавины ошибок")

Вы просто работаете с Copilot как обычно, а MCP Shell **бесшумно** обеспечивает, чтобы всё работало гладко!

---

## 🤖 Новые инструменты автоматизации (03.12.2025)

### Автоматическая индексация файлов

Проект WORLD_OLLAMA теперь умеет **автоматически обновлять индекс журналов** без вашего участия!

#### 🔍 Что это значит для вас?

**Раньше:**
- Вы создаёте/изменяете `.md` файл
- Индекс устаревает
- Copilot не видит новые файлы
- Нужно вручную запускать обновление индекса

**Теперь:**
- Вы создаёте/изменяете `.md` файл
- **Индекс обновляется АВТОМАТИЧЕСКИ** (за 1-2 секунды!)
- Copilot сразу видит изменения
- Вы продолжаете работать

---

### 🛠️ Три механизма автоматизации

#### 1️⃣ Наблюдатель файлов (FileSystemWatcher)

**Что делает:** Следит за всеми `.md` файлами в реальном времени

**Как работает:**
- Вы сохраняете файл → через 2 секунды индекс обновляется
- Создаёте новый файл → автоматически добавляется в индекс
- Удаляете файл → автоматически убирается из индекса
- Переименовываете → индекс синхронизируется

**Особенности:**
- ⏱️ Задержка 2 секунды (чтобы не обновлять при каждом нажатии клавиши)
- 💓 Сердцебиение каждые 10 минут (пишет в лог "всё работает")
- 🚫 Игнорирует технические папки: `venv`, `node_modules`, `archive`

**Как запустить:**
```powershell
# Спросите Copilot:
"Запусти наблюдатель файлов"

# Или вручную в терминале:
pwsh -File E:\WORLD_OLLAMA\scripts\WATCH_FILE_CHANGES.ps1
```

**Логи:** `E:\WORLD_OLLAMA\logs\file_watcher.log`

---

#### 2️⃣ Git хук (Post-Commit)

**Что делает:** Обновляет индекс после каждого коммита с `.md` изменениями

**Как работает:**
- Вы делаете `git commit`
- Git проверяет: есть ли `.md` файлы в коммите?
- Если да → автоматически обновляет индекс
- Если нет → ничего не делает

**Установка (один раз):**
```powershell
# Спросите Copilot:
"Установи Git хук для автоиндексации"

# Или вручную:
pwsh E:\WORLD_OLLAMA\scripts\INSTALL_GIT_HOOK.ps1
```

**Проверка:**
```powershell
# Спросите Copilot:
"Покажи содержимое .git/hooks/post-commit"

# Если файл есть → хук установлен ✅
```

**Преимущества:**
- ✅ Не забудете обновить индекс перед пушем
- ✅ Коммиты всегда содержат актуальный индекс
- ✅ Команда видит полную историю изменений

---

#### 3️⃣ Запланированная задача (Scheduled Task)

**Что делает:** Каждый день в 03:00 делает полную переиндексацию

**Зачем это нужно:**
- Исправляет накопленные ошибки
- Проверяет целостность всех файлов
- Обновляет метаданные (количество файлов, периоды)

**Установка (требует права админа):**
```powershell
# Спросите Copilot:
"Создай задачу по расписанию для переиндексации"

# Или вручную (правая кнопка → Запуск от имени администратора):
pwsh E:\WORLD_OLLAMA\scripts\CREATE_SCHEDULED_TASK.ps1
```

**Удаление:**
```powershell
pwsh E:\WORLD_OLLAMA\scripts\CREATE_SCHEDULED_TASK.ps1 -RemoveTask
```

**Проверка:**
```powershell
# Спросите Copilot:
"Покажи задачу WorldOllama_DailyReindex в планировщике"

# Или откройте: Win+R → taskschd.msc → найдите "WorldOllama_DailyReindex"
```

**Логи:** `E:\WORLD_OLLAMA\logs\scheduled_reindex.log`

---

### 🎯 Как выбрать механизм?

| Механизм | Когда использовать | Скорость | Требует настройки |
|----------|-------------------|----------|------------------|
| **FileSystemWatcher** | Активная разработка (много правок) | ⚡ 2 секунды | Нет (просто запустить) |
| **Git Hook** | Работа в команде (Git workflow) | ⚡ 1 секунда | Да (установить один раз) |
| **Scheduled Task** | Фоновая поддержка (для спокойствия) | 🐌 Раз в сутки | Да (требует admin) |

**Рекомендация:** Используйте **FileSystemWatcher** во время работы + **Git Hook** для синхронизации команды + **Scheduled Task** как страховку.

---

### 🔧 Ручная переиндексация (если нужно)

**Когда может понадобиться:**
- Все автоматические механизмы выключены
- Нужно срочно обновить индекс прямо сейчас
- Тестируете изменения в скриптах

**Быстрая (инкрементальная):**
```powershell
# Спросите Copilot:
"Обнови индекс для файла docs/new_report.md"

# Или вручную:
pwsh E:\WORLD_OLLAMA\scripts\UPDATE_PROJECT_INDEX.ps1 -IncrementalMode -TriggerFile "docs/new_report.md"
```
**Время:** ~1-2 секунды

**Полная (весь проект):**
```powershell
# Спросите Copilot:
"Сделай полную переиндексацию проекта"

# Или вручную:
pwsh E:\WORLD_OLLAMA\scripts\UPDATE_PROJECT_INDEX.ps1 -FullReindex
```
**Время:** ~3-5 секунд (сканирует 166 файлов)

---

### 📊 Как проверить, что автоматизация работает?

**Способ 1: Посмотреть логи**
```powershell
# Спросите Copilot:
"Покажи последние 20 строк indexation.log"

# Должны быть записи вида:
# [2025-12-03 13:19:45] [SUCCESS] Метаданные обновлены: 109 журналов
```

**Способ 2: Проверить метаданные индекса**
```powershell
# Спросите Copilot:
"Покажи метаданные из RUNTIME_LOGS_JOURNAL_INDEX.md"

# Должно быть:
# **Последнее обновление:** 03.12.2025 (сегодняшняя дата)
# **Всего журналов:** 109 (актуальное число)
```

**Способ 3: Тест наблюдателя**
```powershell
# 1. Запустите наблюдатель
"Запусти наблюдатель файлов"

# 2. Создайте тестовый файл
New-Item -Path "E:\WORLD_OLLAMA\docs\test.md" -ItemType File

# 3. Через 2 секунды проверьте лог
"Покажи последние 5 строк file_watcher.log"

# Должна быть запись:
# [CHANGE] Обнаружено изменение: docs\test.md → Запуск переиндексации
```

---

### ❗ Устранение проблем

**Проблема:** Наблюдатель не запускается
```
Решение:
1. Проверьте: pwsh -Version (должно быть 7.5+)
2. Проверьте файл: Test-Path "E:\WORLD_OLLAMA\scripts\WATCH_FILE_CHANGES.ps1"
3. Запустите с правами: Shift + Правая кнопка → "Открыть PowerShell здесь"
```

**Проблема:** Git хук не срабатывает
```
Решение:
1. Проверьте: Test-Path ".git/hooks/post-commit"
2. Проверьте права: git ls-files --stage .git/hooks/post-commit (должно быть 100755)
3. Переустановите: pwsh scripts/INSTALL_GIT_HOOK.ps1
```

**Проблема:** Запланированная задача не выполняется
```
Решение:
1. Откройте Планировщик задач (Win+R → taskschd.msc)
2. Найдите "WorldOllama_DailyReindex"
3. Правая кнопка → Выполнить (проверка вручную)
4. Посмотрите "Журнал" → если ошибки → проверьте права
```

**Проблема:** Индекс не обновляется автоматически
```
Решение:
1. Проверьте все 3 механизма: "Покажи статус автоматизации"
2. Запустите ручную переиндексацию: pwsh scripts/UPDATE_PROJECT_INDEX.ps1 -FullReindex
3. Посмотрите ошибки в логах: logs/indexation.log, logs/file_watcher.log
4. Если не помогло → перезагрузите VS Code
```

---

### 🎓 Частые вопросы об автоматизации

**❓ "Нужно ли устанавливать все 3 механизма?"**

Нет, каждый работает независимо. Выберите то, что удобно:
- Только работаете локально → FileSystemWatcher
- Работаете с Git → Git Hook
- Хотите "установить и забыть" → Scheduled Task

**❓ "Будет ли индекс обновляться, если я редактирую файл вне VS Code?"**

Да! FileSystemWatcher следит за файловой системой, не за редактором. Можете редактировать в Notepad, Word, любом месте — индекс обновится.

**❓ "Что делать, если индекс обновился неправильно?"**

Запустите полную переиндексацию:
```powershell
pwsh E:\WORLD_OLLAMA\scripts\UPDATE_PROJECT_INDEX.ps1 -FullReindex
```
Это пересканирует все 166 файлов и исправит любые ошибки.

**❓ "Сколько места занимают логи автоматизации?"**

Очень мало (~1-5 KB в день). Логи автоматически очищаются при полной переиндексации.

**❓ "Можно ли изменить время запланированной задачи (не 03:00)?"**

Да:
```powershell
pwsh scripts/CREATE_SCHEDULED_TASK.ps1 -ExecutionTime "14:30"  # 14:30 каждый день
```

**❓ "Безопасно ли запускать FileSystemWatcher постоянно?"**

Да, абсолютно. Он потребляет ~5-10 MB RAM и 0% CPU (когда файлы не меняются).

---

### 📚 Дополнительные ресурсы

**Документация:**
- Полный отчёт: `docs/infra/AUTOMATIC_INDEXATION_IMPLEMENTATION_REPORT.md`
- Инструкции Copilot: `.github/copilot-instructions.md` (раздел "Automatic Indexation Tools")
- README проекта: `README.md` (раздел "Автоматическая индексация")

**Тесты (для проверки работоспособности):**
```powershell
# Тест целостности системы (17 проверок)
pwsh scripts/tests/TEST_SYSTEM_INTEGRITY.ps1

# Тест интеграции с агентом (13 проверок)
pwsh scripts/tests/TEST_AGENT_INTEGRATION.ps1

# Имитация обновления (6 фаз)
pwsh scripts/tests/TEST_UPDATE_SIMULATION.ps1
```

**Все тесты прошли:** 36/36 (100%) ✅

---

### 🏆 Преимущества автоматизации

**Для вас:**
- ✅ Экономия времени: не нужно помнить про обновление индекса
- ✅ Меньше ошибок: индекс всегда актуален
- ✅ Лучше работает Copilot: видит все файлы мгновенно
- ✅ Спокойствие: система сама следит за собой

**Для команды:**
- ✅ Синхронизация: все коммиты с актуальным индексом
- ✅ Прозрачность: история изменений прослеживается
- ✅ Надёжность: автоматические проверки каждый день

**Для проекта:**
- ✅ Масштабируемость: работает с любым числом файлов (166 → 1000+)
- ✅ Производительность: инкрементальные обновления за 1-2 сек
- ✅ Безопасность: откат изменений через Git

---

**Версия документа:** 2.0  
**Дата:** 03.12.2025  
**Проект:** WORLD_OLLAMA v0.3.1  
**Автор:** GitHub Copilot + MCP Shell Team  
**Обновление:** Добавлены инструменты автоматической индексации
